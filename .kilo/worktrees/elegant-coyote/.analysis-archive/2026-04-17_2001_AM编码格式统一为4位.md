<analysis>
context:
  - formatSummaryIndexCode_ACU 函数（helpers-table-lock.ts 第101行）已经是 4 位格式：`AM${String(n).padStart(4, '0')}`，生成 AM0001、AM0002 等。代码层面已正确。
  - chronicle.js 的 DDL CHECK 约束当前为 `code_index GLOB 'AM[0-9]*' AND LENGTH(code_index) >= 4`，这个约束太宽松——AM01（4字符）也能通过，AM0001（6字符）也能通过，没有固定为 4 位数字。
  - chronicle.js 的 note 描述为"格式为 AMXX，XX从01递增"，这是旧的 2 位格式描述。
  - defaults-json.js 中有两个合并纪要提示词（ORM版和SQL版），都包含大量 AM01 格式引用。
  - defaults-json.js 中剧情推进提示词包含 "AM001, AM002, ..."（3位格式），也需要统一为 AM0001。
  - tests/ 目录下有测试文件引用 AM01 格式。

needs: 将所有 AM 编码格式从 2 位（AM01）统一为 4 位（AM0001），防止酒馆世界书关键词前缀匹配冲突。

key_challenges:
  1. defaults-json.js 包含大量转义字符串中的 AM01 引用，需要精确替换。
  2. 提示词中 AM01 出现在多种上下文中。
  3. 剧情推进提示词中用的是 AM001（3位），需要统一为 AM0001（4位）。

confidence: HIGH

approach: 逐文件替换所有 AM 编码格式引用，统一为 4 位格式。
  - 可维护性: 5/5
  - 健壮性: 5/5
  - 可扩展性: 5/5

edge_cases:
  - 已有用户数据中可能存在旧格式编码，但运行时 formatSummaryIndexCode_ACU 已生成 AM0001 格式。

affected_scope:
  - src/shared/table-defaults/chronicle.js
  - src/shared/defaults-json.js
  - plans/optimization_plan.md
  - tests/service/worldbook/pipeline.test.ts
  - tests/service/worldbook/injection-engine-entries.test.ts

execution_plan:
  - step_1: 修改 chronicle.js — DDL CHECK 约束 + note 描述
  - step_2: 修改 defaults-json.js — ORM版合并纪要提示词
  - step_3: 修改 defaults-json.js — SQL版合并纪要提示词
  - step_4: 修改 defaults-json.js — 剧情推进提示词
  - step_5: 修改 pipeline.test.ts 测试数据
  - step_6: 修改 injection-engine-entries.test.ts 测试数据
  - step_7: 修改 optimization_plan.md 计划文档
</analysis>

---
<output_quality_review>
task_summary: 将所有 AM 编码索引格式从 2 位（AM01）/ 3 位（AM001）统一为 4 位（AM0001），防止酒馆世界书关键词前缀匹配冲突。同时将 DDL CHECK 约束从宽松的 `GLOB 'AM[0-9]*' AND LENGTH >= 4` 改为精确的 `GLOB 'AM[0-9][0-9][0-9][0-9]'`。

deliverables:
  - 修改: src/shared/table-defaults/chronicle.js（DDL CHECK 约束 + note 描述）
  - 修改: src/shared/defaults-json.js（ORM版合并纪要提示词 + SQL版合并纪要提示词 + 剧情推进提示词 × 2）
  - 修改: tests/service/worldbook/pipeline.test.ts（测试数据）
  - 修改: tests/service/worldbook/injection-engine-entries.test.ts（测试数据）
  - 修改: plans/optimization_plan.md（计划文档）

metrics:
  total_files_modified: 5
  execution_plan_coverage: 7/7 = 100%
  edge_cases_handled: 1/1 = 100%
  confidence_assessment: HIGH

substance_check: 全部通过
completeness_check: 全部通过
value_density_check: 高价值占比 80%
alignment_check: 完全满足用户需求
</output_quality_review>