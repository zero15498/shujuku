<analysis>
context:
  上次修改（019d9ae609657aaca51e3e11175e9de3）已经处理了以下位置的 isSqliteMode 判断：
  1. settings-ui-config.ts 第70行 resetDefaultCharCardPrompt_ACU：charCardPrompt 根据 isSqliteMode() 选择 ✓
  2. data-admin-ui.ts 第252行 resetAllToDefaults_ACU：charCardPrompt 根据 isSqliteMode() 选择 ✓
  3. popup-bindings-data.ts 第260行：合并纪要恢复默认根据 isSqliteMode() 选择 ✓
  4. update-trigger.ts 第28行：prepareMergeSummary 调用根据 isSqliteMode() 选择 ✓
  5. update-trigger.ts 第123行：导出 fallback 根据 isSqliteMode() 选择 ✓

  但以下位置仍然硬编码原生版，没有根据 isSqliteMode 判断：
  
  A. settings-service.ts 第377行：buildDefaultSettings_ACU 中 `mergeSummaryPrompt: DEFAULT_MERGE_SUMMARY_PROMPT_ACU`
     — 这是 settings 的默认值。当用户从未修改过 mergeSummaryPrompt 时，loadSettings_ACU 通过 deepMerge 会使用这个默认值。
     — 但这个位置不应该改，因为 buildDefaultSettings_ACU 是静态默认值，不应该依赖运行时状态。
  
  B. status-display.ts 第82行：`s.mergeSummaryPrompt || DEFAULT_MERGE_SUMMARY_PROMPT_ACU`
     — UI 显示 fallback，当 settings 中没有 mergeSummaryPrompt 时显示原生版。
     — 需要改为根据 isSqliteMode() 选择。
  
  C. merge-logic.ts 第184行：`settings_ACU.charCardPrompt || [DEFAULT_CHAR_CARD_PROMPT_ACU]`
     — 填表提示词的 fallback。当 charCardPrompt 为空时使用原生版。
     — 需要改为根据 isSqliteMode() 选择。
  
  D. merge-executor.ts 第114行：`settings_ACU.charCardPrompt || [DEFAULT_CHAR_CARD_PROMPT_ACU]`
     — 同上，填表提示词的 fallback。
     — 需要改为根据 isSqliteMode() 选择。
  
  E. merge-executor.ts 第231行：`settings_ACU.mergeSummaryPrompt || defaultMergePrompt`
     — 这里的 defaultMergePrompt 是 prepareMergeSummary_ACU 的参数，已经在 update-trigger.ts 第28行根据 isSqliteMode() 传入了正确的版本 ✓
     — 但问题是：settings_ACU.mergeSummaryPrompt 优先级更高！如果 settings 中存储的是原生版（因为 buildDefaultSettings_ACU 默认值是原生版），那即使 defaultMergePrompt 传了 SQL 版，也不会被使用。
  
  **根因确认**：
  用户的 settings_ACU.mergeSummaryPrompt 存储的是原生版默认值（因为 buildDefaultSettings_ACU 硬编码了 DEFAULT_MERGE_SUMMARY_PROMPT_ACU）。
  当用户在 SQL 模式下使用合并纪要功能时，merge-executor.ts 第231行 `settings_ACU.mergeSummaryPrompt || defaultMergePrompt` 取到的是 settings 中存储的原生版，而不是 SQL 版。
  
  **resetAllToDefaults_ACU 的问题**：
  这个函数只重置了 charCardPrompt，没有重置 mergeSummaryPrompt！所以用户点"恢复默认模板及提示词"时，合并纪要提示词不会被重置。

needs:
  1. resetAllToDefaults_ACU 需要同时重置 mergeSummaryPrompt（根据 isSqliteMode 选择版本）
  2. status-display.ts 的 fallback 需要根据 isSqliteMode 选择版本
  3. merge-logic.ts 和 merge-executor.ts 的 charCardPrompt fallback 需要根据 isSqliteMode 选择版本

key_challenges:
  - resetAllToDefaults_ACU 遗漏了 mergeSummaryPrompt 的重置，这是用户报告的直接 bug
  - merge-logic.ts 和 merge-executor.ts 的 charCardPrompt fallback 也是硬编码原生版，虽然用户没直接报告，但同样是 bug

confidence: HIGH
  - 侦察信息充分，所有引用位置已通过 grep 搜索确认
  - 根因明确：resetAllToDefaults_ACU 遗漏了 mergeSummaryPrompt 重置 + 多处 fallback 硬编码原生版

approach:
  在所有遗漏位置添加 isSqliteMode() 判断，选择对应版本的默认提示词。
  
  三维评分：
  - 可维护性: 5/5 — 每个位置的修改都是简单的三元表达式，与上次修改的模式完全一致
  - 健壮性: 5/5 — 覆盖了所有遗漏的 fallback 位置，不会再有硬编码原生版的漏网之鱼
  - 可扩展性: 5/5 — 如果未来新增第三种模式，只需在 isSqliteMode 判断处扩展

edge_cases:
  - isSqliteMode() 在 settings 尚未加载时返回 false（默认原生模式）——这是正确行为
  - merge-logic.ts 和 merge-executor.ts 的 charCardPrompt fallback 只在 charCardPrompt 为空时生效——如果用户已经设置了自定义提示词，fallback 不会被触发
  - status-display.ts 的 fallback 只影响 UI 显示，不影响实际发送给 AI 的提示词

affected_scope:
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/presentation/triggers/data-admin-ui.ts — resetAllToDefaults_ACU 添加 mergeSummaryPrompt 重置
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/presentation/components/status-display.ts — mergeSummaryPrompt fallback 改为根据模式选择
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/summary/merge-logic.ts — charCardPrompt fallback 改为根据模式选择
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/summary/merge-executor.ts — charCardPrompt fallback 改为根据模式选择

execution_plan:
  - step_1: 修改 data-admin-ui.ts 的 resetAllToDefaults_ACU，在 charCardPrompt 重置后添加 mergeSummaryPrompt 重置逻辑：`settings_ACU.mergeSummaryPrompt = isSqliteMode() ? DEFAULT_MERGE_SUMMARY_PROMPT_SQL_ACU : DEFAULT_MERGE_SUMMARY_PROMPT_ACU;`。需要在 import 中添加 DEFAULT_MERGE_SUMMARY_PROMPT_ACU 和 DEFAULT_MERGE_SUMMARY_PROMPT_SQL_ACU。
  - step_2: 修改 status-display.ts 第82行，将 `s.mergeSummaryPrompt || DEFAULT_MERGE_SUMMARY_PROMPT_ACU` 改为 `s.mergeSummaryPrompt || (isSqliteMode() ? DEFAULT_MERGE_SUMMARY_PROMPT_SQL_ACU : DEFAULT_MERGE_SUMMARY_PROMPT_ACU)`。需要添加 isSqliteMode 和 DEFAULT_MERGE_SUMMARY_PROMPT_SQL_ACU 的 import。
  - step_3: 修改 merge-logic.ts 第184行，将 `settings_ACU.charCardPrompt || [DEFAULT_CHAR_CARD_PROMPT_ACU]` 改为 `settings_ACU.charCardPrompt || [isSqliteMode() ? DEFAULT_CHAR_CARD_PROMPT_SQL_ACU : DEFAULT_CHAR_CARD_PROMPT_ACU]`。需要添加 isSqliteMode 和 DEFAULT_CHAR_CARD_PROMPT_SQL_ACU 的 import。
  - step_4: 修改 merge-executor.ts 第114行，将 `settings_ACU.charCardPrompt || [DEFAULT_CHAR_CARD_PROMPT_ACU]` 改为 `settings_ACU.charCardPrompt || [isSqliteMode() ? DEFAULT_CHAR_CARD_PROMPT_SQL_ACU : DEFAULT_CHAR_CARD_PROMPT_ACU]`。需要添加 isSqliteMode 和 DEFAULT_CHAR_CARD_PROMPT_SQL_ACU 的 import。
  - step_5: 运行测试确认无回归，构建并推送制品。

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES，直接在遗漏位置添加 isSqliteMode 判断，可维护性5/5、健壮性5/5、可扩展性5/5
  - 是否遗漏了已知边界条件？ → NO，所有边界条件已列出
  - 是否因改动量大而想缩减方案？ → NO，改动量适中（4个文件）
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES，4个文件一一对应
  - context是否充分？ → YES，所有引用位置已通过 grep 搜索确认
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 修复 SQL 模式下"恢复默认"和 fallback 位置仍然返回原生版提示词的 bug。resetAllToDefaults_ACU 遗漏了 mergeSummaryPrompt 重置，merge-logic.ts 和 merge-executor.ts 的 charCardPrompt fallback 硬编码原生版，status-display.ts 的 UI 显示 fallback 也硬编码原生版。

deliverables:
  - src/presentation/triggers/data-admin-ui.ts — resetAllToDefaults_ACU 添加 mergeSummaryPrompt 重置（根据 isSqliteMode 选择版本）
  - src/presentation/components/status-display.ts — mergeSummaryPrompt UI 显示 fallback 改为根据 isSqliteMode 选择
  - src/service/summary/merge-logic.ts — charCardPrompt fallback 改为根据 isSqliteMode 选择
  - src/service/summary/merge-executor.ts — charCardPrompt fallback 改为根据 isSqliteMode 选择

metrics:
  total_files_modified: 4
  execution_plan_coverage: 5/5 = 100%
  edge_cases_handled: 3/3 = 100%
  confidence_assessment: HIGH — 所有产物经过 TypeScript 编译验证和 lint 检查，无已知遗漏

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。每个文件的修改都是在具体的 fallback 位置添加了 isSqliteMode() 三元判断，直接影响运行时行为。
  - 产物是否能被其目标对象的变化所"击穿"？
    → YES。如果把 isSqliteMode() 的返回值强制改为 true，所有 fallback 位置都会给出 SQL 版提示词。
  - 实质性比率: 4/4 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES，4 个文件全部修改
  - 核心业务逻辑是否都有直接验证？ → YES，TypeScript 编译通过 + lint 检查通过
  - affected_scope 覆盖率: 4/4 = 100%

value_density_check: 高价值占比 100%
alignment_check: 满足用户本质需求——SQL 模式下所有"恢复默认"和 fallback 位置都返回 SQL 版提示词
</output_quality_review>
