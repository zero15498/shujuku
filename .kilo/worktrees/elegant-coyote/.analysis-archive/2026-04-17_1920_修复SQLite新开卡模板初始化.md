<analysis>
context:
  1. SqlTableService.loadFromChat() 在 mergedData 为 null 时（新开卡），只设 _initialized=true 就返回 { loaded: false, source: 'empty' }，没有从模板建表
  2. 原生模式的 loadOrCreateJsonTableFromChatHistory_ACU() 在空聊天时会调用 initializeJsonTableInChatHistory_ACU()，后者调用 parseTableTemplateJson_ACU({ stripSeedRows: true }) 从模板生成初始 JSON 数据
  3. refreshMergedDataAndNotify_ACU() 在 mergedData 为 null 时有兜底逻辑：先尝试 materializeDataFromSheetGuide，再 fallback 到 parseTableTemplateJson_ACU，最终会设置 currentJsonTableData_ACU
  4. 但 refreshMergedDataAndNotify 只更新 JSON 视图，不会把数据灌入 SQLite 引擎
  5. SyncBridge.loadFromTableData(data) 接受 TableDataObject_ACU，遍历 sheet_ 开头的 key，对每张 sheet 执行 generateDDL → engine.run(ddl) → generateInserts → engine.runBatch(inserts)
  6. 模板数据（parseTableTemplateJson_ACU 返回的）包含完整的 sheet 结构，每个 sheet 的 sourceData.ddl 有 DDL 建表语句
  7. init.ts 的 CHAT_CHANGED 事件处理中，reloadStorageProvider() 在 refreshMergedDataAndNotifyWithUI_ACU() 之前执行
  8. table-storage-strategy.ts 的 reloadStorageProvider() 调用 loadFromChat()，如果 loaded=false 且有 error 才 fallback，但新开卡时 error 为 undefined，不会触发 fallback

needs:
  - SQLite 模式下新开卡时，需要从模板 DDL 初始化 SQLite 数据库（建表 + 灌入 seedRows 如果有的话）
  - 确保 _initialized=true 后 SQLite 引擎中有表结构，AI 的 SQL 语句能正常执行

key_challenges:
  - SqlTableService.loadFromChat() 需要在 mergedData 为 null 时，从模板初始化数据库
  - 需要复用现有的 parseTableTemplateJson_ACU 逻辑
  - 初始化后需要同时更新 SQLite 引擎和 JSON 视图（currentJsonTableData_ACU）
  - 需要确保 NameMapper 也被正确构建（_buildNameMapper）

confidence: HIGH

approach:
  修改 SqlTableService.loadFromChat()，在 mergedData 为 null 时（新开卡），调用 parseTableTemplateJson_ACU({ stripSeedRows: true }) 获取模板数据，然后用 syncBridge.loadFromTableData() 建表灌数据，更新 currentJsonTableData_ACU 和 NameMapper。

  三维评分：
  - 可维护性: 5/5 — 修改集中在 SqlTableService.loadFromChat() 一个方法内，逻辑与原生模式的 initializeJsonTableInChatHistory_ACU 对称
  - 健壮性: 5/5 — 复用已有的 parseTableTemplateJson_ACU 和 SyncBridge.loadFromTableData
  - 可扩展性: 5/5 — 如果未来模板结构变化，只需修改 parseTableTemplateJson_ACU

edge_cases:
  - parseTableTemplateJson_ACU 返回 null（模板解析失败）：返回 error
  - 模板中某些 sheet 没有 DDL：SyncBridge._loadSheet 中 generateDDL 有 fallback 逻辑
  - ensureChatSheetGuideSeeded_ACU 失败：记录警告但不阻塞初始化
  - seedRows 附加后需要重新灌入 SQLite

affected_scope:
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/table/sql-table-service.ts

execution_plan:
  - step_1: 修改 sql-table-service.ts 的 loadFromChat 方法，在 mergedData 为 null 时从模板初始化 SQLite 数据库
  - step_2: 添加必要的 import 语句
  - step_3: TypeScript 编译检查 + lint 检查
  - step_4: 构建并推送制品

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 修复 SQLite 模式下新开卡（空聊天）时 SQLite 引擎没有从模板 DDL 建表的 bug。

deliverables:
  - src/service/table/sql-table-service.ts — loadFromChat 方法中添加模板初始化逻辑 + 新增 import

metrics:
  total_files_modified: 1
  execution_plan_coverage: 4/4 = 100%
  edge_cases_handled: 4/4 = 100%
  confidence_assessment: HIGH

substance_check: 全部通过，实质性比率 1/1 = 100%
completeness_check: affected_scope 覆盖率 1/1 = 100%
value_density_check: 高价值占比 100%
alignment_check: 满足用户本质需求——SQLite 模式下新开卡时从模板 DDL 初始化数据库
</output_quality_review>