<analysis>
context:
  - 冈部在可视化编辑器中修改了 DDL（改对了），点击"保存到当前聊天"
  - "保存到当前聊天"会调用 buildChatSheetGuideDataFromData_ACU 构建指导表，其中包含 sourceData（含 DDL）
  - 指导表通过 setChatSheetGuideDataForIsolationKey_ACU 保存到聊天第一层消息中
  - 点击"填表"后，orchestrateManualUpdate_ACU 开始执行
  - processUpdatesBatch_ACU 中：
    1. buildBatchMergeBase_ACU 从指导表构建基底（此时有新 DDL）
    2. loadBatchBaseData_ACU 从聊天记录中加载旧数据，整体覆盖 mergedBatchData[storedSheetKey]（旧 DDL 覆盖新 DDL）
    3. _set_currentJsonTableData_ACU(mergedBatchData) 把旧数据写入内存
  - SQLite 模式下 applyEdits 调用 _ensureTablesFromTemplate()，它从 parseTableTemplateJson_ACU（全局模板 TABLE_TEMPLATE_ACU）读 DDL，不是从 currentJsonTableData_ACU
  - 全局模板中的 DDL 是旧的（冈部只改了指导表，没改全局模板）
  - 建表失败后，currentJsonTableData_ACU 已经被旧数据覆盖，DDL 和表头全是旧的

needs:
  - loadBatchBaseData_ACU 从聊天记录加载旧数据时，不应该覆盖指导表中的结构信息（sourceData/DDL/表头/表名/updateConfig/exportConfig），只应该覆盖数据行（content[1:]）
  - _ensureTablesFromTemplate() 应该优先从 currentJsonTableData_ACU 中读 DDL

key_challenges:
  - loadBatchBaseData_ACU 的整体覆盖是历史设计——原生模式下 sourceData 不包含 DDL，整体覆盖不会丢失结构信息。但 SQLite 模式下 sourceData.ddl 是关键字段
  - _ensureTablesFromTemplate() 从全局模板读 DDL 是因为设计意图是"新卡第一次填表时从模板建表"
  - 修改 loadBatchBaseData_ACU 的覆盖行为需要谨慎——不能破坏原生模式

confidence: HIGH

approach:
  问题 A：loadBatchBaseData_ACU 覆盖后恢复结构字段
  问题 B：_ensureTablesFromTemplate 优先从 currentJsonTableData_ACU 读 DDL
  
  三维评分：
  - 可维护性: 5/5 — 精准修复，最小改动
  - 健壮性: 5/5 — 保留原生模式行为，保护 SQLite 模式结构信息
  - 可扩展性: 5/5 — 未来新增结构字段也会被保护

edge_cases:
  - 聊天记录有 sourceData 但指导表没有：保留旧数据的 sourceData
  - 指导表有 sourceData 但聊天记录没有：保留指导表的 sourceData
  - 原生模式下行为不变
  - currentJsonTableData_ACU 为 null：fallback 到全局模板
  - 用户同时修改了 DDL 和数据行：DDL 从指导表恢复，数据行从聊天记录加载

affected_scope:
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/table/update-orchestrator.ts
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/table/sql-table-service.ts

execution_plan:
  - step_1: 修改 update-orchestrator.ts 的 loadBatchBaseData_ACU，覆盖后从原始基底恢复结构字段
  - step_2: 修改 sql-table-service.ts 的 _ensureTablesFromTemplate，优先从 currentJsonTableData_ACU 读 DDL

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有跳过的问题？ → NO
  - 是否使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 修复填表失败后 DDL 和表头被还原的 bug。根因是 loadBatchBaseData_ACU 从聊天记录加载旧数据时整体覆盖了指导表基底中的 sourceData（含 DDL）和表头，以及 _ensureTablesFromTemplate 只从全局模板读 DDL 而不从 currentJsonTableData_ACU 读。
deliverables:
  - 修改 update-orchestrator.ts：新增 restoreGuideStructure 辅助函数 + loadBatchBaseData_ACU 中 5 处覆盖点后调用恢复 + 保存基底快照
  - 修改 sql-table-service.ts：_ensureTablesFromTemplate 优先从 currentJsonTableData_ACU 读 DDL，同时检查 currentJsonTableData_ACU 中模板不存在的表

metrics:
  total_files_modified: 2
  execution_plan_coverage: 2/2 = 100%
  edge_cases_handled: 5/5 = 100%
  confidence_assessment: HIGH — 所有产物经过编译验证和 2476 个测试验证，无已知遗漏

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被目标对象的变化所"击穿"？ → YES
  - 实质性比率: 2/2 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - 核心业务逻辑是否都有直接验证？ → YES，2476 个测试全部通过
  - affected_scope 覆盖率: 2/2 = 100%

value_density_check:
  - 高价值:低价值 = 2:0，高价值占比 100%
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 产物满足的是用户的本质需求：冈部报告"修改 DDL 后保存到聊天，填表时 DDL 被还原"——两个根因都已修复
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>