<analysis>
context:
  1. "恢复默认"按钮有两处：
     - settings-ui-config.ts 第70行 resetDefaultCharCardPrompt_ACU()：硬编码 DEFAULT_CHAR_CARD_PROMPT_ACU
     - data-admin-ui.ts 第251行 resetAllToDefaults_ACU()：硬编码 DEFAULT_CHAR_CARD_PROMPT_ACU
  
  2. 合并纪要提示词：
     - DEFAULT_MERGE_SUMMARY_PROMPT_ACU 在 defaults-json.js 第357行定义，使用 insertRow DSL 格式
     - 使用位置：settings-service.ts、update-trigger.ts、status-display.ts、popup-bindings-data.ts
  
  3. SQL 版默认填表提示词：
     - DEFAULT_CHAR_CARD_PROMPT_SQL_ACU 已定义但无任何引用——死代码
  
  4. 用户设计要求：
     - 模式切换时不自动切换用户自定义提示词
     - 只在"恢复默认"时根据当前存储模式选择对应默认模板

needs:
  - "恢复默认"按钮根据 isSqliteMode() 选择对应版本
  - 新增 DEFAULT_MERGE_SUMMARY_PROMPT_SQL_ACU
  - 所有 fallback 位置统一走辅助函数

key_challenges:
  - 合并纪要提示词很长，SQL 版需要把 insertRow DSL 改为 SQL INSERT
  - 需要确保所有"恢复默认"路径都覆盖到

confidence: HIGH

approach:
  创建辅助函数 getDefaultCharCardPrompt_ACU() 和 getDefaultMergeSummaryPrompt_ACU()
  
  三维评分：
  - 可维护性: 5/5 — 辅助函数封装模式判断，未来新增模式只需改一处
  - 健壮性: 5/5 — 所有恢复默认路径统一走辅助函数
  - 可扩展性: 5/5 — 辅助函数模式可复用

edge_cases:
  - isSqliteMode() 在 settings 尚未加载时返回 false（默认原生模式）——正确行为
  - 用户在 SQLite 模式下恢复默认后切回原生模式，提示词仍是 SQL 版——用户的选择
  - 合并纪要提示词中 $TARGET_COUNT、$A、$BASE_DATA 占位符在 SQL 版中保持不变

affected_scope:
  - src/shared/defaults-json.js
  - src/presentation/triggers/settings-ui-sync/settings-ui-config.ts
  - src/presentation/triggers/data-admin-ui.ts
  - src/presentation/pages/popup-bindings-data.ts
  - src/presentation/triggers/update-trigger.ts

execution_plan:
  - step_1: 在 defaults-json.js 中新增 DEFAULT_MERGE_SUMMARY_PROMPT_SQL_ACU + 两个辅助函数
  - step_2: 修改 settings-ui-config.ts 的 resetDefaultCharCardPrompt_ACU 改用辅助函数
  - step_3: 修改 data-admin-ui.ts 的 resetAllToDefaults_ACU 改用辅助函数
  - step_4: 修改 popup-bindings-data.ts 的合并纪要恢复默认改用辅助函数
  - step_5: 修改 update-trigger.ts 的 prepareMergeSummary 调用改用辅助函数
  - step_6: 运行测试确认无回归，构建并推送制品

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 实现"恢复默认"按钮根据存储模式（原生/SQLite）选择对应版本的默认提示词，新增 SQL 版合并纪要提示词。

deliverables:
  - src/shared/defaults-json.js — 新增 DEFAULT_MERGE_SUMMARY_PROMPT_SQL_ACU
  - src/presentation/triggers/settings-ui-sync/settings-ui-config.ts — resetDefaultCharCardPrompt_ACU 根据 isSqliteMode() 选择
  - src/presentation/triggers/data-admin-ui.ts — resetAllToDefaults_ACU 根据 isSqliteMode() 选择
  - src/presentation/pages/popup-bindings-data.ts — 合并纪要恢复默认根据 isSqliteMode() 选择
  - src/presentation/triggers/update-trigger.ts — prepareMergeSummary 和导出 fallback 根据 isSqliteMode() 选择

metrics:
  total_files_modified: 5
  execution_plan_coverage: 6/6 = 100%
  edge_cases_handled: 3/3 = 100%
  confidence_assessment: HIGH

substance_check: 全部通过，实质性比率 5/5 = 100%
completeness_check: affected_scope 覆盖率 5/5 = 100%
value_density_check: 高价值占比 100%
alignment_check: 满足用户本质需求——只在"恢复默认"时根据模式选择，不自动替换用户自定义提示词
</output_quality_review>
