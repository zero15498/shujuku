<analysis>
context:
  经过完整侦察，确认以下关键事实：
  1. `main-popup-status.ts` 第108-109行定义了手动更新表选择区的"全选"(`#${PREFIX}-manual-table-select-all`)和"全不选"(`#${PREFIX}-manual-table-select-none`)两个按钮的HTML。
  2. `popup-bindings.ts` 第72-73行通过 `_assignUIPlaceholders_ACU` 将这两个按钮的jQuery引用赋值给了 `$manualTableSelectAll_ACU` 和 `$manualTableSelectNone_ACU`，存储在 `ui-refs.ts` 中。
  3. `table-selector.ts` 第245-251行导出了 `handleManualSelectAll_ACU()` 和 `handleManualSelectNone_ACU()` 两个处理函数，分别调用 `manualSelector.selectAll()` 和 `manualSelector.selectNone()`。
  4. `popup-bindings-status.ts` 是状态标签页的事件绑定文件，其中绑定了大量按钮和输入框的事件，但**完全没有**导入和绑定 `$manualTableSelectAll_ACU`/`$manualTableSelectNone_ACU` 的点击事件。
  5. 同样的问题也存在于导入表选择器：`$importTableSelectAll_ACU`/`$importTableSelectNone_ACU` 在 `popup-bindings.ts` 被收集，`handleImportSelectAll_ACU()`/`handleImportSelectNone_ACU()` 在 `table-selector.ts` 被导出，但 `popup-bindings-data.ts` 中没有绑定这两个按钮的点击事件。
  6. 结论：按钮的HTML存在，jQuery引用存在，处理函数存在，但**没有任何代码将点击事件和处理函数连接起来**。这就是全选/全不选按钮失效的根因。

needs: 用户报告的是填表功能手动更新表选择的全选和全不选按钮失效。本质需求是让这两个按钮恢复功能，同时一并修复导入表选择器中相同的问题。

key_challenges:
  1. 需要在正确的事件绑定文件中添加click handler，且不破坏现有的事件绑定结构
  2. 手动更新表选择按钮属于"状态"标签页，其事件绑定在 `popup-bindings-status.ts`；导入表选择按钮属于"数据"标签页，其事件绑定在 `popup-bindings-data.ts`
  3. 需要在各自的绑定文件中导入对应的 handler 函数和 jQuery 引用

confidence: HIGH
  - 侦察信息充分，根因完全确认：handler 函数已定义但未被绑定到按钮
  - 方案有明确的工程依据：只需添加缺失的 click 事件绑定
  - 修复范围精确，不涉及逻辑变更

approach:
  在两个事件绑定文件中添加缺失的 click 事件绑定。
  三维评分：
  - 可维护性: 5/5 — 遵循现有的模块化事件绑定模式（在对应标签页的 binding 文件中绑定事件），与已有代码风格完全一致
  - 健壮性: 5/5 — 使用标准的 jQuery `.length` 检查和 `.on('click')` 绑定，与项目中其他按钮绑定方式一致
  - 可扩展性: 5/5 — 不引入新的耦合，复用已有的 handler 函数

edge_cases:
  1. 按钮在DOM中不存在时（`.length` 检查为0）不应报错
  2. 重复绑定问题：由于 `_assignUIPlaceholders_ACU` 只在初始化时调用一次，不会导致重复绑定
  3. 导入表选择器可能没有数据源，点击全选时 `selectAll()` 方法内部已有 null 检查（table-selector.ts 第109-110行）

affected_scope:
  - src/presentation/pages/popup-bindings-status.ts（添加手动表选择全选/全不选事件绑定）
  - src/presentation/pages/popup-bindings-data.ts（添加导入表选择全选/全不选事件绑定）
  - index.js（打包输出）

execution_plan:
  - step_1: 在 `popup-bindings-status.ts` 中导入 `$manualTableSelectAll_ACU`、`$manualTableSelectNone_ACU`（从 `ui-refs.ts`）和 `handleManualSelectAll_ACU`、`handleManualSelectNone_ACU`（从 `table-selector.ts`），并在 `bindStatusEvents_ACU()` 函数中添加两个按钮的 click 事件绑定
  - step_2: 在 `popup-bindings-data.ts` 中导入 `$importTableSelectAll_ACU`、`$importTableSelectNone_ACU`（从 `ui-refs.ts`）和 `handleImportSelectAll_ACU`、`handleImportSelectNone_ACU`（从 `table-selector.ts`），并在 `bindDataEvents_ACU()` 函数中添加两个按钮的 click 事件绑定
  - step_3: 执行打包命令重新生成 dist/index.bundle.js 并复制到 index.js

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES。可维护性5/5、健壮性5/5、可扩展性5/5。直接修复根因，不引入额外复杂度。
  - 是否遗漏了已知边界条件？ → NO。按钮不存在时的 `.length` 检查在标准模式中已覆盖。
  - 是否因改动量大而想缩减方案？ → NO。每个文件只需加两行 import 和三行事件绑定。
  - 是否打算跳过某些文件？ → NO。同时修复手动和导入两个选择器。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。三个文件全覆盖。
  - context是否充分？ → YES。已完整追踪了从 HTML 生成到事件绑定的全链路。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO。全部使用标准编辑工具。
</analysis>
---
<output_quality_review>
task_summary: 修复填表功能手动更新表选择的全选/全不选按钮失效bug。根因是 handler 函数已定义但未被绑定到按钮的 click 事件。同时修复了导入表选择器中相同的问题。重新打包生成了 index.js。

deliverables:
  - src/presentation/pages/popup-bindings-status.ts（新增 $manualTableSelectAll_ACU/$manualTableSelectNone_ACU 的 import 和 click 绑定）
  - src/presentation/pages/popup-bindings-data.ts（新增 $importTableSelectAll_ACU/$importTableSelectNone_ACU 的 import 和 click 绑定）
  - dist/index.bundle.js（重新打包）
  - index.js（从 dist 复制更新）

metrics:
  total_files_modified: 4
  execution_plan_coverage: 3/3 = 100%
  edge_cases_handled: 3/3 = 100%
  confidence_assessment: HIGH
    - 所有产物经过验证（TypeScript 编译通过，grep 确认新代码已出现在打包输出中），无已知遗漏

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO。每个修改都引入了实际的功能代码（click 事件绑定），删除这些代码会导致按钮重新失效。
  - 产物是否能被其目标对象的变化所"击穿"？ → YES。如果 `handleManualSelectAll_ACU`/`handleManualSelectNone_ACU` 函数被删除或改名，新增的 import 语句会编译报错；如果按钮 ID 变更，jQuery 查找会返回空集，绑定不生效。两种情况都能被检测到。
  - 实质性比率: 4/4 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO。两个选择器（手动+导入）的全选/全不选都已修复。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？ → YES。popup-bindings-status.ts、popup-bindings-data.ts、index.js 全部覆盖。
  - 核心业务逻辑是否都有直接验证？ → YES。TypeScript 编译通过确认 import 路径和类型正确；grep 确认打包输出包含新增的事件绑定代码。
  - affected_scope 覆盖率: 3/3 = 100%

value_density_check:
  - 产物中高价值内容与低价值内容的比例 → 高价值:低价值 = 4:0，高价值占比 100%。每处修改都是解决用户报告的核心bug所必需的。
  - 是否存在"用数量掩盖质量"的模式？ → NO。

alignment_check:
  - 产物满足的是用户的本质需求还是字面需求？ → 本质需求。用户报告了手动更新表选择的全选/全不选按钮失效，本质原因是事件绑定缺失。修复直接解决了根因，同时一并修复了导入表选择器中同样的问题。
  - "如果这是别人交给我的，我会接受吗？" → YES。修改量最小、精准定位根因、与现有代码风格一致、TypeScript 编译通过、打包输出验证通过。
</output_quality_review>
