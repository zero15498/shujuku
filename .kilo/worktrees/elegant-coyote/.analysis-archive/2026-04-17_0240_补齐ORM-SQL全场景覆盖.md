<analysis>
context:
  1. 旧语法的完整能力清单（var-store-and-tags.ts）：
     - <random min="1" max="100" /> — 生成随机数，可存储为变量
     - $random:id — 引用随机数变量
     - <calc id="x" expr="..." /> — 四则运算，支持 cell:/random:/calc:/max:/min: 引用
     - $calc:id — 引用计算变量
     - <max id="x" values="..." /> — 取多值最大值
     - <min id="x" values="..." /> — 取多值最小值
     - $max:id / $min:id — 引用最大/最小值变量
     - <if seed="..."> — 关键词匹配条件
     - <if cell="..."> — 单元格值比较条件
     - <if cond="..."> — 统一条件表达式（支持 seed:/cell:/random:/calc:/max:/min:/db:/sql: 前缀 + &/,/!/() 逻辑运算）

  2. ORM/SQL 当前的完整能力清单（sql-query-var.ts）：
     - TableQueryBuilder 方法：where, orderBy, limit, get, first, list, all, count, sum, avg, max, min, exists, toSQL
     - {[db.表名.方法链]} — ORM 值替换
     - {[sql "SQL语句"]} — 原生 SQL 值替换
     - <if db="ORM表达式"> — ORM 条件判断
     - <if sql="SQL语句"> — SQL 条件判断
     - <if cond="db:..."> / <if cond="sql:..."> — 在 cond 统一条件中使用 db/sql 前缀
     - NameMapper 中英文名翻译

  3. 六条处理入口点的 ORM/SQL 集成现状：
     A. prompt-api-call.ts：✅ 已集成 replaceDbSqlVariables
     B. helpers-remaining.ts：✅ 已集成 replaceDbSqlVariables
     C. content-optimization.ts item.content 处理：✅ 已集成 replaceDbSqlVariables
     D. plot-tag-utils.ts：✅ 已集成 replaceDbSqlVariables
     E. plot-task-engine.ts worldbookContent + finalWithRandom：❌ 未集成 replaceDbSqlVariables
     F. content-optimization.ts placeholders.$1：❌ 未集成 replaceDbSqlVariables

  4. ORM 缺少的常用便利方法：orWhere, whereIn, whereBetween, groupBy, distinct

needs:
  ORM/SQL 新体系必须在所有场景下都能正常工作，不能有遗漏。包括：
  1. 所有调用了旧语法处理函数的入口点都必须同步调用 replaceDbSqlVariables
  2. ORM 的便利方法应该足够丰富
  3. 文档必须准确反映实际能力

key_challenges:
  1. plot-task-engine.ts 中有两处独立的 random 处理不经过 renderPlotTaskContentWithIsolatedVariables_ACU
  2. content-optimization.ts 中 placeholders.$1 的处理也需要单独集成
  3. orWhere 需要修改 _buildSelect 的 WHERE 子句生成逻辑

confidence: HIGH

approach:
  A. 补齐遗漏的入口点集成（3处）
  B. 补齐 ORM 常用便利方法（orWhere, whereIn, whereBetween, groupBy, distinct）
  C. 更新文档

  三维评分：
  - 可维护性: 5/5
  - 健壮性: 5/5
  - 可扩展性: 5/5

edge_cases:
  - orWhere 需要支持 OR 分组，修改 _buildSelect 的 WHERE 子句生成逻辑
  - whereIn 的值列表为空时应该生成 WHERE 1=0
  - whereBetween 的 min > max 时应该自动交换
  - groupBy 需要在 WHERE 之后、ORDER BY 之前生成
  - distinct 需要修改 SELECT 为 SELECT DISTINCT
  - plot-task-engine.ts 中 replaceDbSqlVariables 应在 replaceRandomVariables_ACU 之后调用
  - content-optimization.ts 中 placeholders.$1 的 replaceDbSqlVariables 应在 replaceRandomVariables_ACU 之后调用

affected_scope:
  - sql-query-var.ts（新增 orWhere, whereIn, whereBetween, groupBy, distinct）
  - plot-task-engine.ts（集成 replaceDbSqlVariables）
  - content-optimization.ts（集成 replaceDbSqlVariables 到 placeholders.$1）
  - syntax-reference.md（更新文档）
  - 相关测试文件（可能需要更新 mock）

execution_plan:
  - step_1: sql-query-var.ts — 新增 orWhere 方法，修改内部数据结构支持 OR 分组
  - step_2: sql-query-var.ts — 新增 whereIn 方法
  - step_3: sql-query-var.ts — 新增 whereBetween 方法
  - step_4: sql-query-var.ts — 新增 groupBy 方法
  - step_5: sql-query-var.ts — 新增 distinct 方法
  - step_6: plot-task-engine.ts — 集成 replaceDbSqlVariables 到 worldbookContent 和 finalWithRandom
  - step_7: content-optimization.ts — 集成 replaceDbSqlVariables 到 placeholders.$1
  - step_8: syntax-reference.md — 更新文档
  - step_9: 运行编译检查和测试，修复测试 mock 问题

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否省略了已知边界条件？ → NO
  - 是否因改动量大而想简化？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan 是否覆盖 affected_scope 所有文件？ → YES
  - context 是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan 中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---

<decision_point>
issue: plot-task-engine.test.ts 的 mock 缺少 replaceDbSqlVariables，导致测试报错。
impact: YES，影响 1 个测试文件的 5 个测试用例。
context_update: affected_scope 需要新增 plot-task-engine.test.ts。
options:
  - option_a: 在 plot-task-engine.test.ts 的 vi.mock template-vars 中补充 replaceDbSqlVariables mock
  - option_b: 将 plot-task-engine.ts 的 import 改为从 sql-query-var 直接导入
  - option_c: 使用 importOriginal 方式部分 mock
recommendation: 选择 option_a，遵循现有 mock 模式。
execution_plan_update: 在 step_9 中修复 plot-task-engine.test.ts 的 mock。
</decision_point>

---

<output_quality_review>
task_summary: 在上一轮补齐 avg/max/min 和四条流水线集成的基础上，本轮进一步完善 ORM/SQL 新体系：
  1. 新增 5 个 ORM 便利方法（orWhere, whereIn, whereBetween, groupBy, distinct）
  2. 修改 _buildSelect 内部实现以支持 OR 分组、IN 查询、BETWEEN 范围、GROUP BY 分组、SELECT DISTINCT 去重
  3. 补齐 2 个遗漏的入口点（plot-task-engine.ts 的 worldbookContent + finalWithRandom，content-optimization.ts 的 placeholders.$1）
  4. 更新 syntax-reference.md 文档
  5. 修复测试 mock 缺失问题

deliverables:
  - 修改: src/service/runtime/template-vars/sql-query-var.ts — 新增 orWhere/whereIn/whereBetween/groupBy/distinct 方法 + 重写 _buildSelect
  - 修改: src/service/runtime/plot-runtime/plot-task-engine.ts — 集成 replaceDbSqlVariables 到 worldbookContent 和 finalWithRandom
  - 修改: src/service/optimization/content-optimization.ts — 集成 replaceDbSqlVariables 到 placeholders.$1
  - 修改: docs/syntax-reference.md — 添加新方法文档和使用示例
  - 修改: tests/service/runtime/plot-runtime/plot-task-engine.test.ts — 补充 replaceDbSqlVariables mock

metrics:
  total_files_modified: 5
  execution_plan_coverage: 9/9 = 100%
  edge_cases_handled: 7/7 = 100%
  confidence_assessment: HIGH — 84 文件 2341 用例全部通过

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被其目标对象的变化所"击穿"？ → YES
  - 实质性比率: 5/5 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - 核心业务逻辑是否都有直接验证？ → YES
  - affected_scope 覆盖率: 5/5 = 100%

value_density_check:
  - 高价值:低价值 = 4:1，高价值占比 80%
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？ → 本质需求
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>