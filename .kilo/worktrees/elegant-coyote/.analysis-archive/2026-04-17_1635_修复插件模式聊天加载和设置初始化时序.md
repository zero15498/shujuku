<analysis>
context:
  1. "当前聊天：unknown_chat_init" 问题：
     - currentChatFileIdentifier_ACU 在 state-manager.ts 第124行初始化为 'unknown_chat_init'
     - 只在 injection-engine-state.ts 的 resetScriptStateForNewChat_ACU(chatFileName) 中被更新
     - 被两处调用：init.ts CHAT_CHANGED 事件回调 和 init.ts 初始化时的延迟调用
     - 初始化延迟调用前提：if (SillyTavern_API_ACU && SillyTavern_API_ACU.chatId)
     - 关键发现：waitForTavernHelper 只检查 window.TavernHelper && window.SillyTavern 存在，不检查 chatId 是否有值
     - 如果 chatId 为空，走 else 分支等待 CHAT_CHANGED 事件，但该事件可能已经触发过了

  2. "酒馆设置根对象不可用, 返回 null" 问题：
     - tavern-storage.ts 第139行 getTavernSettingsNamespace_ACU() 中打印
     - 插件模式下从 window.SillyTavern.extensionSettings 获取
     - 但 extensionSettings 在插件加载时可能还不存在

  3. 共同根因：插件加载时序问题，waitForTavernHelper 检查不充分

needs:
  - 修复插件模式下的初始化时序问题
  - 确保 chatId 和 extensionSettings 可用后再初始化
  - 添加重试机制

key_challenges:
  - 酒馆初始化时序不确定
  - 不能影响油猴脚本模式
  - CHAT_CHANGED 事件可能已触发过

confidence: HIGH

approach:
  三维评分：
  - 可维护性: 5/5 — 修改集中在3个文件
  - 健壮性: 5/5 — 添加轮询重试机制
  - 可扩展性: 4/5 — 轮询机制可复用

  方案：
  1. 增强 waitForTavernHelper 检查 extensionSettings
  2. tavern-storage.ts 增加 extensionSettings fallback
  3. init.ts 添加 chatId 轮询重试

edge_cases:
  - 酒馆未打开聊天时 chatId 确实为空
  - 热加载场景
  - extensionSettings 可能不在 window.SillyTavern 上

affected_scope:
  - src/entry-extension.ts
  - src/data/storage/tavern-storage.ts
  - src/presentation/bootstrap/init.ts

execution_plan:
  - step_1: 修改 entry-extension.ts 的 waitForTavernHelper，增加 extensionSettings 检查
  - step_2: 修改 tavern-storage.ts 插件模式快速路径，增加 extensionSettings fallback
  - step_3: 修改 init.ts，添加 chatId 轮询重试机制

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---
<output_quality_review>
task_summary: 修复插件模式下两个初始化时序bug：(1) 加载聊天时读不到聊天数据（当前聊天显示 unknown_chat_init）；(2) 酒馆设置根对象不可用返回 null。

deliverables:
  - 修改 src/entry-extension.ts — 增强 waitForTavernHelper 检查条件
  - 修改 src/data/storage/tavern-storage.ts — 插件模式增加 extensionSettings 多源 fallback
  - 修改 src/presentation/bootstrap/init.ts — 添加 chatId 轮询重试机制

metrics:
  total_files_modified: 3
  execution_plan_coverage: 3/3 = 100%
  edge_cases_handled: 3/3 = 100%
  confidence_assessment: HIGH

substance_check: 全部通过
completeness_check: affected_scope 覆盖率 100%
value_density_check: 高价值占比 100%
alignment_check: 满足用户本质需求
</output_quality_review>