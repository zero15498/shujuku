<analysis>
context:
  1. 8 张默认表的 DDL 定义分布在 src/shared/table-defaults/ 下的 8 个 .js 文件中
  2. parseDDLColumnComments 用正则解析注释，-- 后面的整个内容作为中文列名
  3. 因此注释必须保持纯净的中文列名，不能加括号说明
  4. parseDDLColumnNames 已支持 CHECK 约束中的嵌套括号
  5. SQLite 的 LENGTH() 对 TEXT 返回 Unicode 字符数（已验证）
  6. 用户评审通过了 DDL 方案

needs:
  - 修改 6 张默认表的 DDL，添加 CHECK 约束
  - 注释保持纯净中文列名

key_challenges:
  - CHECK 约束中的括号和逗号可能影响列定义分割——但已有测试验证支持

confidence: HIGH

approach:
  直接修改 6 个默认表模板文件的 DDL 字段，添加 CHECK 约束。
  三维评分：可维护性 5/5、健壮性 5/5、可扩展性 5/5

edge_cases:
  - CHECK 中的中文字符串——SQLite 原生支持 UTF-8
  - GLOB 'AM[0-9]*' + LENGTH >= 4 确保至少 AM + 2位数字
  - CHECK(prev_scene_time IS NULL OR ...) 允许 NULL
  - CHECK(brief_intro IS NULL OR ...) 允许 NULL

affected_scope:
  - src/shared/table-defaults/global-state.js
  - src/shared/table-defaults/important-chars.js
  - src/shared/table-defaults/protagonist-skills.js
  - src/shared/table-defaults/inventory.js
  - src/shared/table-defaults/quests-events.js
  - src/shared/table-defaults/chronicle.js

execution_plan:
  - step_1: global-state.js — current_time 加时间格式 CHECK，prev_scene_time 加 NULL OR 时间格式 CHECK
  - step_2: important-chars.js — brief_intro 加 LENGTH <= 20 CHECK，is_absent 加 IN ('是','否') CHECK
  - step_3: protagonist-skills.js — skill_type 加 IN ('被动','主动') CHECK
  - step_4: inventory.js — quantity 加 CHECK(quantity > 0)
  - step_5: quests-events.js — quest_type 加 IN ('主线任务','支线任务') CHECK
  - step_6: chronicle.js — summary 加 LENGTH <= 40 CHECK，code_index 加 GLOB + LENGTH CHECK
  - step_7: TypeScript 编译检查 + lint 检查
  - step_8: 构建并推送制品

degradation_check: 全部 NO，无问题
</analysis>

---

<output_quality_review>
task_summary: 为 6 张默认表的 DDL 添加 CHECK 约束，规范字段类型和数据校验。

deliverables:
  - global-state.js — current_time/prev_scene_time 加时间格式 GLOB CHECK
  - important-chars.js — brief_intro 加 LENGTH <= 20 CHECK，is_absent 加 IN ('是','否') CHECK
  - protagonist-skills.js — skill_type 加 IN ('被动','主动') CHECK
  - inventory.js — quantity 加 CHECK(quantity > 0)
  - quests-events.js — quest_type 加 IN ('主线任务','支线任务') CHECK
  - chronicle.js — summary 加 LENGTH <= 40 CHECK，code_index 加 GLOB + LENGTH CHECK

metrics:
  total_files_modified: 6
  execution_plan_coverage: 8/8 = 100%
  edge_cases_handled: 4/4 = 100%
  confidence_assessment: HIGH

substance_check: 全部通过，实质性比率 6/6 = 100%
completeness_check: affected_scope 覆盖率 6/6 = 100%
value_density_check: 高价值占比 100%
alignment_check: 满足用户本质需求——DDL 字段类型符合数据库标准建表规范，支持强校验
</output_quality_review>