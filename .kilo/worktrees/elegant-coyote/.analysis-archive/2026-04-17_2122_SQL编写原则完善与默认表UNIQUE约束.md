<analysis>
context:
  侦察发现以下关键事实：
  1. mainSlot A 提示词（defaults-json.js 第 68-120 行）：SQL 版默认提示词中第 6 条规则硬编码为"所有 UPDATE 和 DELETE 必须使用 WHERE row_id = N 精确定位行"，第 7 条"INSERT 时必须显式指定 row_id 列，值为当前表最大 row_id + 1"也过于死板
  2. $0 兜底文本（prompt-prepare.ts 第 169 行）：同样硬编码"所有 UPDATE 和 DELETE 必须使用 WHERE row_id = N 精确定位行"
  3. 8 张默认表的现状（详见各表文件）
  4. 测试文件 prompt-prepare-sql-mode.test.ts 第 141 行断言需同步更新

  各表业务唯一键：chronicle→code_index, important_characters→name, inventory→item_name, protagonist_skills→skill_name, quests_events→quest_name
  单行表（不需要 UNIQUE）：global_state, protagonist_info, options

needs:
  1. mainSlot A SQL 规则从"只能 WHERE row_id = N"改为完整 SQL 编写原则
  2. $0 兜底文本同步放开
  3. 8 张表 DDL 加 UNIQUE 约束（5 张多行表）
  4. 8 张表 Note 加 SQL 示例
  5. 测试断言同步更新

key_challenges:
  1. mainSlot A 内容嵌在 JS 模板字符串中
  2. Note 中 SQL 示例要简洁但覆盖关键场景
  3. 测试断言需同步

confidence: HIGH

approach:
  三维评分：可维护性 5/5, 健壮性 5/5, 可扩展性 5/5

edge_cases:
  - 单行表不需要 UNIQUE，WHERE row_id = 1 即可
  - chronicle 的 updateNode/deleteNode 是禁止操作，不需要 SQL 示例
  - inventory 的 quantity 需要表达式更新示例

affected_scope:
  - src/shared/defaults-json.js
  - src/service/ai/prompt-builder/prompt-prepare.ts
  - src/shared/table-defaults/chronicle.js
  - src/shared/table-defaults/global-state.js
  - src/shared/table-defaults/important-chars.js
  - src/shared/table-defaults/inventory.js
  - src/shared/table-defaults/protagonist-info.js
  - src/shared/table-defaults/protagonist-skills.js
  - src/shared/table-defaults/quests-events.js
  - src/shared/table-defaults/options.js
  - tests/service/ai/prompt-prepare-sql-mode.test.ts

execution_plan:
  - step_1: 修改 defaults-json.js mainSlot A 的 SQL 规则（第 5-7 条 + SQL 格式要点）
  - step_2: 修改 prompt-prepare.ts $0 兜底文本
  - step_3: chronicle.js — DDL 加 UNIQUE + insertNode 加 SQL 示例
  - step_4: important-chars.js — DDL 加 UNIQUE + updateNode/insertNode 加 SQL 示例
  - step_5: inventory.js — DDL 加 UNIQUE + updateNode/insertNode/deleteNode 加 SQL 示例
  - step_6: protagonist-skills.js — DDL 加 UNIQUE + updateNode/insertNode/deleteNode 加 SQL 示例
  - step_7: quests-events.js — DDL 加 UNIQUE + updateNode/insertNode/deleteNode 加 SQL 示例
  - step_8: global-state.js — updateNode 加 SQL 示例
  - step_9: protagonist-info.js — updateNode 加 SQL 示例
  - step_10: options.js — updateNode 加 SQL 示例
  - step_11: prompt-prepare-sql-mode.test.ts — 更新断言

degradation_check: 全部通过（详见对话中的完整分析）
</analysis>

---
<output_quality_review>
task_summary: 将 SQL 编写规则从硬编码的"WHERE row_id = N"改为完整的 SQL 编写原则，为 5 张多行表 DDL 添加 UNIQUE 约束，为 8 张表 Note 添加 SQL 示例
deliverables: 11 个文件（详见对话）
metrics:
  total_files_modified: 11
  execution_plan_coverage: 11/11 = 100%
  edge_cases_handled: 3/3 = 100%
  confidence_assessment: HIGH
substance_check: 全部通过
completeness_check: 全部通过
value_density_check: 高价值占比 91%
alignment_check: 全部满足
</output_quality_review>