<analysis>
context:
  - _ensureTablesFromTemplate() 第 327 行 `if (!sheet || !sheet.sourceData?.ddl) continue;` 直接跳过没有 DDL 的表
  - 前端"新增表格"按钮创建的自定义表，sourceData 中没有 ddl 字段：{ note: "新表格说明", initNode: "", insertNode: "", updateNode: "", deleteNode: "" }
  - schema-mapper.ts 的 generateDDL() 函数已经有 fallback 逻辑：如果 sourceData.ddl 为空，会从 content[0] 表头自动生成全 TEXT 的 DDL（generateFallbackDDL）
  - SyncBridge._loadSheet() 内部调用 generateDDL(sheet)，已经能处理无 DDL 的情况（走 fallback）
  - 问题的根源是 _ensureTablesFromTemplate 在遍历模板表时，用 sourceData.ddl 来获取表名，没有 DDL 的表无法获取表名，所以被跳过
  - generateDDL 的 fallback 路径使用 sheet.uid 作为表名（如 sheet_hdnsa4gi6），而不是用户期望的 status_bar

needs:
  - 修复 _ensureTablesFromTemplate 使其能处理没有 DDL 的自定义表
  - 没有 DDL 的表应该走 fallback 路径：用 generateDDL(sheet) 生成 DDL，用 parseDDLTableName 获取表名
  - 确保 fallback 生成的表名能被正确识别和使用

key_challenges:
  - 没有 DDL 的表，generateDDL 的 fallback 使用 sheet.uid（如 sheet_hdnsa4gi6）作为表名
  - 关键是要让 _ensureTablesFromTemplate 不再跳过这些表

confidence: HIGH
  - 问题根因明确：第 327 行的 `if (!sheet.sourceData?.ddl) continue` 跳过了无 DDL 的表
  - 修复方案明确：改为用 generateDDL(sheet) 获取 DDL（含 fallback），再从中解析表名

approach:
  修改 _ensureTablesFromTemplate 的遍历逻辑：
  - 不再要求 sheet.sourceData.ddl 必须存在
  - 对每张表调用 generateDDL(sheet) 获取 DDL（有用户 DDL 用用户的，没有走 fallback）
  - 从生成的 DDL 中解析表名
  - 检查该表名是否已存在于 SQLite
  - 不存在的加入 missingSheets

  三维评分：
  - 可维护性: 5/5 — 复用现有的 generateDDL fallback 逻辑，不引入新概念
  - 健壮性: 5/5 — 有 DDL 和无 DDL 的表都能正确处理
  - 可扩展性: 5/5 — 未来新增表无论有没有 DDL 都能自动建表

edge_cases:
  - 无 DDL 且 content 为空的表：generateDDL 会生成只有 row_id 的最小 DDL，建表不会失败
  - 无 DDL 的表 fallback 表名是 sheet.uid（如 sheet_hdnsa4gi6）：正确行为
  - 用户后来给自定义表配了 DDL（表名变了）：旧表不会被删除，这是独立问题
  - generateDDL 需要从 schema-mapper.ts 导入：需检查当前导入

affected_scope:
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/table/sql-table-service.ts

execution_plan:
  - step_1: 在 sql-table-service.ts 的 import 中添加 generateDDL 的导入（如果尚未导入）
  - step_2: 修改 _ensureTablesFromTemplate 的 for 循环，将 `if (!sheet.sourceData?.ddl) continue` 改为用 generateDDL(sheet) 获取 DDL 并解析表名

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有跳过的问题？ → YES，表名变更问题，但这是独立问题，不在本次修复范围
  - 是否使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 修复 _ensureTablesFromTemplate 中自定义表（无 DDL）被跳过导致中途加表无法建表的 bug。
deliverables:
  - 修改 sql-table-service.ts：import 中添加 generateDDL；_ensureTablesFromTemplate 的 for 循环中将 `if (!sheet.sourceData?.ddl) continue` 改为用 generateDDL(sheet) 获取 DDL（含 fallback），不再跳过无 DDL 的表

metrics:
  total_files_modified: 1
  execution_plan_coverage: 2/2 = 100%
  edge_cases_handled: 3/4 = 75%（表名变更问题是独立问题不在本次范围）
  confidence_assessment: HIGH — 编译通过，2476 个测试全部通过

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被目标对象的变化所"击穿"？ → YES
  - 实质性比率: 1/1 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - 核心业务逻辑是否都有直接验证？ → YES
  - affected_scope 覆盖率: 1/1 = 100%

value_density_check:
  - 高价值:低价值 = 1:0，高价值占比 100%
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 满足冈部的本质需求：中途加的自定义表在第一次写入时能自动建表
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>