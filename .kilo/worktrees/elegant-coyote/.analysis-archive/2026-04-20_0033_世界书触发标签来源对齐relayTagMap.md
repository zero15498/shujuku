<analysis>
context:
  已确认以下关键代码事实：

  1. **renderPlotTaskMessages_ACU (L284-286) 中的 relayTagMap 决定逻辑**：
     - 当 `runtimeOptions.useHistoryRelay === true`（即 stageIndex === 0，第一阶段）时：从 `sharedContext.lastPlotContent`（上轮剧情历史）提取 {{tag}} 内容
     - 当 `runtimeOptions.useHistoryRelay === false`（后续阶段）时：使用 `runtimeOptions.relayTagMap`，即 `aggregatedTags`（本轮先前阶段的聚合结果）

  2. **当前 executeSinglePlotTask_ACU 中的世界书触发文本构造 (L311-313)**：
     - 只用了 `sharedContext.lastPlotContent` 一个来源
     - 完全忽略了 `runtimeOptions.relayTagMap`（本轮先前阶段结果）
     - 这意味着：第二阶段及之后的任务，世界书触发文本和 prompt 注入用的标签内容来源不一致

  3. **runPlotTasksRuntime_ACU 中的调度逻辑 (L498-501)**：
     - `relayTagMap: aggregatedTags` — 本轮先前阶段的聚合标签结果
     - `useHistoryRelay: stageIndex === 0` — 只有第一阶段用历史中继
     - 所以第二阶段及之后，prompt 注入用的是 `aggregatedTags`，但世界书触发仍然只看 `lastPlotContent`

  4. **aggregatedTags 的结构**：`Map<string, string[]>`，key 是 tagName，value 是内容数组

  5. **buildTaskWorldbookTriggerText_ACU 当前签名**：
     - `buildTaskWorldbookTriggerText_ACU(taskPromptGroup, plotContent)` — 只接受 plotContent 一个标签来源

needs:
  世界书触发文本的标签来源必须与 prompt 注入的标签来源完全一致：
  - 第一阶段（useHistoryRelay=true）：从 lastPlotContent 提取
  - 后续阶段（useHistoryRelay=false）：从 aggregatedTags（runtimeOptions.relayTagMap）提取
  - 如果 relayTagMap 中没有对应标签，不回退到整段 lastPlotContent

key_challenges:
  - `buildTaskWorldbookTriggerText_ACU` 需要新增一个参数来接受 relayTagMap
  - 调用方需要传入与 renderPlotTaskMessages_ACU 完全一致的标签来源判断逻辑
  - 当 relayTagMap 是 Map 且非空时，应优先从中取标签内容

confidence: HIGH
  - 代码已经精确定位
  - relayTagMap 的结构和来源已经确认
  - 改动范围很小，只需要修改函数签名和调用方传参

approach:
  三维评分：
  - 可维护性: 5/5 — 让世界书触发和 prompt 注入共享同一份标签来源判断逻辑，消除不一致风险
  - 健壮性: 5/5 — 每种标签来源都有独立处理，不互相回退
  - 可扩展性: 5/5 — 如果将来新增第三种标签来源，只需在函数内新增一个分支

  方案：给 buildTaskWorldbookTriggerText_ACU 新增可选参数 `relayTagMap`，当传入有效 Map 时优先从中取标签内容。

edge_cases:
  - relayTagMap 是 Map 但为空 → 不产生触发文本（与无 {{tag}} 行为一致）
  - relayTagMap 是 Map 且有内容但不含当前任务的 tag → 该 tag 不产生触发文本
  - relayTagMap 不是 Map → 回退到从 plotContent 提取
  - 第一阶段 relayTagMap 未传入 → 使用 plotContent（上一轮剧情历史）

affected_scope:
  - src/service/runtime/plot-runtime/plot-tag-utils.ts（修改 buildTaskWorldbookTriggerText_ACU 签名和逻辑）
  - src/service/runtime/plot-runtime/plot-task-engine.ts（修改 executeSinglePlotTask_ACU 调用方式）

execution_plan:
  - step_1: 修改 buildTaskWorldbookTriggerText_ACU 新增 relayTagMap 可选参数，优先使用 relayTagMap 中的标签内容
  - step_2: 修改 executeSinglePlotTask_ACU 传入 relayTagMap 参数，与 renderPlotTaskMessages_ACU 使用相同的来源判断逻辑
  - step_3: 构建验证

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES，可维护性5/5、健壮性5/5、可扩展性5/5
  - 是否遗漏了已知边界条件？ → NO，4 个边界条件已列出
  - 是否因改动量大而想缩减方案？ → NO，改动量极小
  - 是否打算跳过某些文件？ → NO，只涉及 2 个文件
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>
---
<output_quality_review>
task_summary: 修正了 buildTaskWorldbookTriggerText_ACU 的标签来源逻辑，使其与 renderPlotTaskMessages_ACU 的 {{tag}} 注入来源完全一致——第一阶段用上轮历史内容，后续阶段用本轮先前阶段的聚合结果。
deliverables:
  - src/service/runtime/plot-runtime/plot-tag-utils.ts — buildTaskWorldbookTriggerText_ACU 新增 relayTagMap 可选参数，优先使用 relayTagMap 中的标签内容
  - src/service/runtime/plot-runtime/plot-task-engine.ts — executeSinglePlotTask_ACU 根据 useHistoryRelay 决定传入 relayTagMap 还是 undefined

metrics:
  total_files_modified: 2
  execution_plan_coverage: 3/3 = 100%
  edge_cases_handled: 4/4 = 100%
  confidence_assessment: HIGH

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO。每个改动都直接解决标签来源不一致问题。
  - 产物是否能被其目标对象的变化所"击穿"？ → YES。如果故意把 relayTagMap 的判断逻辑改反，世界书触发来源会直接跟着变化。
  - 实质性比率: 2/2 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO。
  - affected_scope 覆盖率: 2/2 = 100%

value_density_check:
  - 高价值:低价值 = 2:0 = 100%。

alignment_check:
  - 本质需求对齐：世界书触发依据现在与 prompt 注入的 {{tag}} 来源完全一致。
    - 第一阶段：relayTagMap=undefined → 从 lastPlotContent 提取（与 prompt 注入行为一致）
    - 后续阶段：relayTagMap=aggregatedTags → 从本轮先前阶段结果提取（与 prompt 注入行为一致）
  - "如果这是别人交给我的，我会接受吗？" → YES。
</output_quality_review>
