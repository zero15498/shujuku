<analysis>
context:
  1. 错误来自 update-orchestrator.ts 第283行的重试循环，调用 parseAndApplyTableEdits_ACU 时触发
  2. parseAndApplyTableEdits_ACU 检测到 SQL 内容后调用 getStorageProvider().applyEdits()
  3. getStorageProvider() 懒初始化只创建了 SqlTableService 实例，没有调用 loadFromChat()
  4. SqlTableService.applyEdits() 内部调用 _ensureInitialized()，检查 _initialized 为 false → 抛出错误
  5. _initialized 只在 loadFromChat() 成功执行后才被设为 true
  6. loadFromChat() 负责：engine.init() → mergeAllIndependentTables → syncBridge.loadFromTableData → _initialized = true
  7. initStorageProvider() 是唯一会调用 loadFromChat() 的入口，但它没有被任何启动流程调用
  8. init.ts 的 CHAT_CHANGED 事件处理中，只在楼层修改事件（MESSAGE_DELETED/CHAT_SWIPE等）时调用 reloadStorageProvider()
  9. 聊天切换时只调用了 refreshMergedDataAndNotifyWithUI_ACU()，这个函数直接调用 mergeAllIndependentTables_ACU() 更新 JSON 视图，完全绕过了 StorageProvider
  10. 因此 SQLite 模式下，聊天切换后 JSON 数据有了，但 SQLite 引擎没有初始化

needs:
  - 确保 SQLite 模式下，在任何 applyEdits/executeQuery/executeMutation 被调用之前，SQLite 引擎已经初始化
  - 最小改动原则：不重构整个启动流程，只在关键路径上补上初始化

key_challenges:
  - getStorageProvider() 是同步函数，但 loadFromChat() 是异步的，不能在 getStorageProvider 内部直接调用
  - 多处代码直接调用 loadOrCreateJsonTableFromChatHistory_ACU 绕过 StorageProvider，全部改掉改动量太大
  - 需要确保初始化只执行一次，不会重复初始化

confidence: HIGH
  - 根因明确：SQLite 引擎未初始化
  - 修复路径清晰：在聊天切换时也调用 reloadStorageProvider()

approach:
  在 init.ts 的 CHAT_CHANGED 事件处理中，SQLite 模式下也调用 reloadStorageProvider()（和楼层修改事件的处理方式一致）。
  
  三维评分：
  - 可维护性: 5/5 — 与现有楼层修改事件的处理模式完全一致，代码风格统一
  - 健壮性: 5/5 — reloadStorageProvider 内部已有完整的错误处理和 fallback 机制
  - 可扩展性: 5/5 — 如果未来新增其他需要重建数据库的事件，只需复制同样的模式

edge_cases:
  - reloadStorageProvider 内部会先 dispose 旧实例再创建新实例，不会重复初始化
  - 如果 SQLite 初始化失败，reloadStorageProvider 会自动 fallback 到原生模式
  - 非 SQLite 模式下不调用 reloadStorageProvider，不影响原生模式

affected_scope:
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/presentation/bootstrap/init.ts

execution_plan:
  - step_1: 修改 init.ts 的 CHAT_CHANGED 事件处理中的 setTimeout 回调，在 refreshMergedDataAndNotifyWithUI_ACU() 之前，如果是 SQLite 模式，先调用 reloadStorageProvider()
  - step_2: TypeScript 编译检查 + lint 检查
  - step_3: 构建并推送制品

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → YES — 多处代码直接调用 loadOrCreateJsonTableFromChatHistory_ACU 绕过 StorageProvider，但这些是架构层面的技术债，不是当前 bug 的原因
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 修复 SQLite 模式下聊天切换后 SQLite 引擎未初始化导致 applyEdits 报错的 bug。

deliverables:
  - src/presentation/bootstrap/init.ts — CHAT_CHANGED 事件处理中添加 SQLite 模式的 reloadStorageProvider() 调用

metrics:
  total_files_modified: 1
  execution_plan_coverage: 3/3 = 100%
  edge_cases_handled: 3/3 = 100%
  confidence_assessment: HIGH

substance_check: 全部通过，实质性比率 1/1 = 100%
completeness_check: affected_scope 覆盖率 1/1 = 100%
value_density_check: 高价值占比 100%
alignment_check: 满足用户本质需求——SQLite 模式下聊天切换后填表功能正常工作
</output_quality_review>