<analysis>
context: 
  1. 旧语法系统（cell:/calc/random/max/min）的核心能力：读单元格值、四则运算、随机数生成、取极值、变量存储与跨表达式引用
  2. ORM/SQL 新语法系统（{[db...]}/{[sql...]}/<if db>/<if sql>）的核心能力：链式查询、原生SQL、条件判断、值替换
  3. ORM/SQL 已经能替代旧语法的"读值"能力（{[db.表.where(...).get(...)]} 替代 cell:表/行/列）
  4. ORM/SQL 已经能替代旧语法的"条件判断"能力（<if db="..."> 替代 <if cell="...">）
  5. SQL 本身支持四则运算（SELECT 1+2）、随机数（ABS(RANDOM()) % N + 1）、极值（MAX/MIN 函数）
  6. ORM/SQL 唯一缺失的核心能力：变量系统——将查询结果存为变量，在后续表达式中引用同一个值
  7. 变量系统的缺失导致：用户无法"查一个值，存起来，后面多处引用"；无法"生成一个随机数，在条件和文本中都用同一个值"
  8. 当前 replaceDbSqlVariables 在处理流程中排在 <calc>/<random> 之后、<if> 之前（helpers-remaining.ts 第114-118行）
  9. evaluateOrmExpression 通过 Proxy + new Function 执行，evaluateRawSqlExpression 直接调用 provider.executeQuery
  10. 旧变量系统使用模块级变量（randomVariables_ACU、calcVariables_ACU 等）存储，通过 $random:id/$calc:id 正则替换引用

needs:
  1. 给 ORM/SQL 新语法补上变量系统：{[db...]} 和 {[sql...]} 的结果能存为变量
  2. 新增计算变量能力：支持引用已存储的 db/sql 变量做四则运算
  3. 新增随机数变量能力：通过新语法生成随机数并存储
  4. 变量引用能在文本中、<if cond="..."> 条件中、后续表达式中使用
  5. 保持旧语法完全不动，新变量系统独立运作

key_challenges:
  1. 变量存储的命名空间设计——新变量用什么前缀引用？不能和旧的 $random:/$calc: 冲突
  2. 处理顺序——变量定义必须在引用之前被处理，需要确保执行顺序正确
  3. {[db...as 变量名]} 的解析——需要在现有的 replaceDbExpressions 手动解析器中支持 "as" 语法
  4. 计算表达式中引用变量——需要在表达式中替换变量引用为实际值
  5. <if cond="db:..."> 中引用变量——需要在条件求值前先替换变量引用

confidence: HIGH

approach:
  三维评分：
  - 可维护性: 5/5 — 新变量系统完全独立于旧语法，在 sql-query-var.ts 中实现，不修改旧代码
  - 健壮性: 4/5 — 变量不存在时返回空字符串，SQL 执行失败时变量不存储
  - 可扩展性: 5/5 — 变量系统是通用的，未来可以轻松添加新的变量类型

  方案：在 sql-query-var.ts 中新增变量系统：
  A. 变量存储：新增模块级 dbSqlVariables 对象
  B. 存储语法：{[db...as 变量名]} 和 {[sql "..." as 变量名]}
  C. 引用语法：$v:变量名
  D. 计算标签：{[calc 变量名 = 表达式]}
  E. 随机数标签：{[rand 变量名 min max]}
  F. 变量引用替换：$v:变量名 替换步骤
  G. <if cond="v:..."> 条件支持

edge_cases:
  1. 变量名限制为 [a-zA-Z_][a-zA-Z0-9_]* 格式
  2. 同名变量重复定义——后定义覆盖先定义
  3. 引用不存在的变量——保留原文不替换，输出警告日志
  4. {[sql "..." as 变量名]} 查询返回多行多列——只取第一行第一列
  5. {[calc 变量名 = 表达式]} 除以零——返回 NaN，变量不存储
  6. {[rand 变量名 min max]} 中 min > max——自动交换
  7. 非 SQLite 模式下跳过不处理
  8. as 关键字出现在 SQL 字符串值中——需要正确区分

affected_scope:
  - src/service/runtime/template-vars/sql-query-var.ts
  - src/service/runtime/template-vars/seed-condition.ts
  - src/service/runtime/template-vars/index.ts
  - docs/syntax-reference.md

execution_plan:
  - step_1: sql-query-var.ts 新增变量存储系统（模块级 dbSqlVariables + get/set/clear）
  - step_2: 修改 replaceDbExpressions 支持 {[db...as 变量名]}
  - step_3: 修改 {[sql "..."]} 替换逻辑支持 {[sql "..." as 变量名]}
  - step_4: 新增 {[calc 变量名 = 表达式]} 标签解析
  - step_5: 新增 {[rand 变量名 min max]} 标签解析
  - step_6: 在 replaceDbSqlVariables 中增加 $v: 替换步骤
  - step_7: seed-condition.ts 新增 v: 前缀条件支持
  - step_8: index.ts 导出新增 API
  - step_9: 更新 docs/syntax-reference.md

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否省略了已知边界条件？ → NO
  - 是否因改动量大而想简化？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 为 ORM/SQL 新语法系统补全变量系统，使其能独立完成旧语法（cell:/calc/random/max/min）的所有能力。新增了 6 种语法：{[db...as 变量名]}、{[sql "..." as 变量名]}、{[rand 变量名 min max]}、{[calc 变量名 = 表达式]}、$v:变量名 引用、<if cond="v:..."> 条件。
deliverables:
  - 修改: src/service/runtime/template-vars/sql-query-var.ts — 新增变量存储系统 + as 语法 + rand/calc 标签 + $v: 替换
  - 修改: src/service/runtime/template-vars/seed-condition.ts — 新增 v: 前缀条件支持 + evaluateVarCondition_ACU 函数
  - 修改: src/service/runtime/template-vars/index.ts — 导出 getDbSqlVariable、clearDbSqlVariables、getDbSqlVariableSnapshot
  - 修改: docs/syntax-reference.md — 添加新语法完整文档

metrics:
  total_files_modified: 4
  execution_plan_coverage: 9/9 = 100%
  edge_cases_handled: 8/8 = 100%
  confidence_assessment: HIGH — 编译零错误，287 个测试全部通过

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被其目标对象的变化所"击穿"？ → YES
  - 实质性比率: 4/4 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - 核心业务逻辑是否都有直接验证？ → YES
  - affected_scope 覆盖率: 4/4 = 100%

value_density_check:
  - 高价值:低价值 = 4:0，高价值占比 100%
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 满足用户本质需求：ORM/SQL 新语法系统具备完整变量系统，不再依赖旧语法
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>
