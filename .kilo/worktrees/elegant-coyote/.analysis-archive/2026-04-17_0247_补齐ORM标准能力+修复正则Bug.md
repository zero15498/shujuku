<analysis>
context:
  1. 当前 TableQueryBuilder 已有 20 个方法：where, orWhere, whereIn, whereBetween, orderBy, groupBy, distinct, limit, get, first, list, all, count, sum, avg, max, min, exists, toSQL
  2. 从标准 ORM 角度审视，缺失以下常用能力：
     - whereNotIn — 有 whereIn 但没有反向操作
     - whereNull / whereNotNull — 虽然 where('列名', null) 可以实现，但语义不清晰
     - whereLike — 模糊匹配是 SQL 最常用的功能之一
     - having — 有 groupBy 但没有 having，分组后无法过滤
     - offset — 有 limit 但没有 offset，分页查询做不了
  3. 发现一个严重 Bug：{[db.xxx]} 的正则 /\{\[db\.([^\]]+)\]\}/g 使用 [^\]]+ 匹配到第一个 ]，但 whereIn 的数组参数包含 ]，会被提前截断。例如 {[db.背包物品表.whereIn('物品名称', ['铁剑', '钢盾']).count()]} 会在 '钢盾' 后面的 ] 处截断
  4. 新增的 avg/max/min/orWhere/whereIn/whereBetween/groupBy/distinct 方法完全没有测试
  5. {[sql "..."]} 的正则使用反向引用 \1，只要内外引号不同就没问题
  6. evaluateOrmExpression 使用 new Function 执行表达式，JS 引擎会原生解析方括号——问题只在正则匹配阶段

needs:
  ORM/SQL 作为标准的数据库查询体系，自身应该具备的所有能力都必须完整实现：
  1. 修复 {[db.xxx]} 正则匹配 Bug（最高优先级）
  2. 补齐缺失的标准 ORM 方法（whereNotIn, whereNull, whereNotNull, whereLike, having, offset）
  3. 为所有新增方法补充测试
  4. 更新文档

key_challenges:
  1. {[db.xxx]} 正则匹配 Bug：需要改为手动解析，跟踪括号深度和引号状态
  2. having 需要在 GROUP BY 之后、ORDER BY 之前生成
  3. offset 没有 limit 时在 SQLite 中需要配合 LIMIT -1

confidence: HIGH

approach:
  A. 修复 {[db.xxx]} 正则匹配 Bug — 改为手动解析
  B. 补齐缺失的标准 ORM 方法
  C. 为所有新增方法补充测试
  D. 更新文档

  三维评分：
  - 可维护性: 5/5 — 手动解析逻辑清晰，新增方法遵循现有模式
  - 健壮性: 5/5 — 手动解析能正确处理所有嵌套情况
  - 可扩展性: 5/5 — 手动解析器可轻松扩展

edge_cases:
  - whereIn 数组参数包含 ] 字符（Bug 修复核心场景）
  - 字符串参数包含 ]} 字符——需要在引号内跳过括号匹配
  - whereNotIn 空数组应返回所有行
  - whereLike 的 % 和 _ 不应被转义
  - having 必须在 groupBy 之后使用
  - offset 为负数时忽略
  - offset 没有 limit 时自动添加 LIMIT -1

affected_scope:
  - sql-query-var.ts（修复 Bug + 新增方法）
  - sql-query-var.test.ts（补充测试）
  - syntax-reference.md（更新文档）

execution_plan:
  - step_1: 修复 replaceDbSqlVariables 中 {[db.xxx]} 的正则匹配 Bug。将正则替换为手动解析函数，从 {[db. 开始跟踪方括号深度和引号状态，找到匹配的 ]} 结束位置。
  - step_2: 新增 whereNotIn 方法。空数组时不添加条件。
  - step_3: 新增 whereNull 和 whereNotNull 方法。
  - step_4: 新增 whereLike 方法。
  - step_5: 新增 having 方法。在 _buildSelect 中 GROUP BY 之后、ORDER BY 之前生成。
  - step_6: 新增 offset 方法。有 offset 无 limit 时自动添加 LIMIT -1。
  - step_7: 在 sql-query-var.test.ts 中为所有新增方法和上一轮新增方法补充测试。
  - step_8: 更新 syntax-reference.md。

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否省略了已知边界条件？ → NO
  - 是否因改动量大而想简化？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan 是否覆盖 affected_scope 所有文件？ → YES
  - context 是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan 中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 从"标准 ORM/SQL 查询体系自身应该具备什么能力"的角度审视并补齐所有缺失：
  1. 修复了 {[db.xxx]} 正则匹配 Bug——将正则替换为手动括号深度解析器
  2. 新增 6 个标准 ORM 方法：whereNotIn, whereNull, whereNotNull, whereLike, having, offset
  3. 为所有新增方法和上一轮新增方法补充了 33 个测试用例
  4. 更新了 syntax-reference.md 文档

deliverables:
  - 修改: sql-query-var.ts — Bug 修复 + 新增 6 个方法 + replaceDbExpressions 手动解析器
  - 修改: sql-query-var.test.ts — 新增 33 个测试用例
  - 修改: syntax-reference.md — 添加新方法文档和使用示例

metrics:
  total_files_modified: 3
  execution_plan_coverage: 8/8 = 100%
  edge_cases_handled: 7/7 = 100%
  confidence_assessment: HIGH — 84 文件 2374 用例全部通过

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被其目标对象的变化所"击穿"？ → YES
  - 实质性比率: 3/3 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - 核心业务逻辑是否都有直接验证？ → YES
  - affected_scope 覆盖率: 3/3 = 100%

value_density_check:
  - 高价值:低价值 = 3:0，高价值占比 100%
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？ → 本质需求
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>