# Analysis Cache

<analysis>
context:
  1. 模式切换事件位于 `src/presentation/pages/popup-bindings-status.ts` 第 160-195 行。当前流程：读取 selectedMode → 如果与 previousMode 相同则 return → toast 提示 → 设置 settings_ACU.storageMode → saveSettingsAndNotify_ACU() → await switchStorageMode(selectedMode) → 成功 toast / 失败回退 radio 和设置。
  2. 已有 `resetDefaultCharCardPrompt_ACU()` 在 `src/presentation/triggers/settings-ui-sync/settings-ui-config.ts` 第 69-75 行。它通过 `isSqliteMode()` 判断当前模式，设置对应的默认提示词，然后 save + loadUI。但这个函数读取的是"当前模式"，不适合用于"目标模式还没切换但需要知道目标模式对应的默认值"的场景。
  3. 默认提示词常量：`DEFAULT_CHAR_CARD_PROMPT_ACU`（原生模式）和 `DEFAULT_CHAR_CARD_PROMPT_SQL_ACU`（SQL 模式）定义在 `src/shared/defaults-json.js` 第 9-143 行。
  4. `StorageMode` 类型定义在 `src/shared/table-storage-provider.ts`，值为 `'native' | 'sqlite'`。
  5. `saveSettingsAndNotify_ACU()` 定义在 `src/presentation/components/settings-ui-helpers.ts` 第 22-31 行，内部调用 `saveSettings_ACU()` 并根据结果弹 toast。
  6. `loadSettingsAndRefreshUI_ACU()` 定义在同一文件第 13-16 行，内部调用 `loadSettings_ACU()` + `syncAllSettingsToUI_ACU(settings_ACU)`。
  7. `resetDefaultCharCardPrompt_ACU` 通过 `settings-ui-sync/index.ts` 导出，被 `popup-bindings-data.ts` 的"恢复默认提示词"按钮使用。
  8. 项目中已有多处使用 `confirm(...)` 同步确认框的先例（计划中已确认）。

needs:
  核心需求是：用户切换存储模式时，弹出确认框询问是否恢复目标模式的默认填表提示词。如果用户选"是"，在模式切换成功后恢复对应默认提示词并刷新 UI；如果选"否"，保持当前提示词不变；如果模式切换失败，提示词不得被修改。
  额外必须保证的：
  - 恢复默认操作必须在 switchStorageMode 成功之后执行，不能抢跑
  - UI（提示词编辑区）必须同步刷新
  - 功能通过共用 popup 绑定路径实现，插件版和 JS 版自动获得

key_challenges:
  1. 时序安全：确认框必须在 switchStorageMode 之前弹出（记录意图），但实际恢复默认操作必须在 switchStorageMode 成功之后执行。如果在切换前就改提示词，切换失败时提示词已经被污染。
  2. 新增 helper 函数需要接收目标模式参数而非读取当前模式：现有 `resetDefaultCharCardPrompt_ACU()` 读的是 `isSqliteMode()`（即当前运行时模式），但场景需要的是"目标模式"的默认值。需要新增一个按目标模式恢复的 helper。
  3. UI 刷新：恢复默认后需要调用 `loadSettingsAndRefreshUI_ACU()` 刷新提示词编辑区。

confidence: HIGH
  - 侦察信息充分：已读取了模式切换事件完整代码、默认提示词常量、已有恢复默认函数、settings-ui-helpers 的 save/load 函数、导出路径
  - 方案有明确工程依据：改动范围限定在 2 个文件
  - 边界条件已全部识别

approach:
  三维评分：
  - 可维护性: 5/5 — 新增的 helper 函数复用现有模式映射逻辑，不硬编码到事件处理器中；与 resetDefaultCharCardPrompt_ACU 的风格一致
  - 健壮性: 5/5 — 恢复默认操作严格在 switchStorageMode 成功后执行；失败路径不触及提示词；confirm 为同步阻塞不影响异步流程
  - 可扩展性: 4/5 — 如果未来需要恢复更多字段（如 mergeSummaryPrompt），只需扩展 helper 即可

  方案：
  1. 在 settings-ui-config.ts 中新增 `applyModeDefaultCharCardPrompt_ACU(mode: 'native' | 'sqlite')` 函数，按传入模式恢复默认 charCardPrompt。
  2. 在 index.ts 导出该新函数。
  3. 在 popup-bindings-status.ts 的模式切换 change 事件中，在 `if (selectedMode === previousMode) return;` 之后插入确认框逻辑，记录用户意图。在 switchStorageMode 成功后，根据意图调用新 helper 并刷新 UI。

edge_cases:
  - 用户选"否"：当前提示词完全不变，现有模式切换逻辑不受影响
  - switchStorageMode 失败：提示词不得被修改，确认框记录的意图不执行
  - selectedMode === previousMode：已有 early return，确认框不会弹出
  - confirm 对话框被浏览器阻止（极少见）：confirm 返回 false，等同选"否"，安全降级

affected_scope:
  - src/presentation/triggers/settings-ui-sync/settings-ui-config.ts — 新增 applyModeDefaultCharCardPrompt_ACU 函数
  - src/presentation/triggers/settings-ui-sync/index.ts — 导出新函数
  - src/presentation/pages/popup-bindings-status.ts — 在模式切换事件中加入确认流程和恢复默认逻辑

execution_plan:
  - step_1: 在 settings-ui-config.ts 中新增 `applyModeDefaultCharCardPrompt_ACU(mode: 'native' | 'sqlite')` 函数。该函数接收目标模式参数，根据 mode 设置 `settings_ACU.charCardPrompt` 为对应的默认值（native → DEFAULT_CHAR_CARD_PROMPT_ACU, sqlite → DEFAULT_CHAR_CARD_PROMPT_SQL_ACU），然后调用 `saveSettingsAndNotify_ACU()` 和 `loadSettingsAndRefreshUI_ACU()`。函数放在 `resetDefaultCharCardPrompt_ACU` 之后。
  - step_2: 在 settings-ui-sync/index.ts 的导出中增加 `applyModeDefaultCharCardPrompt_ACU`。
  - step_3: 修改 popup-bindings-status.ts 的模式切换 change 事件：
    (a) 在文件顶部 import 中增加 `applyModeDefaultCharCardPrompt_ACU` 和 `loadSettingsAndRefreshUI_ACU`
    (b) 在 `if (selectedMode === previousMode) return;` 之后，弹出 confirm 确认框
    (c) 在 switchStorageMode 成功后，根据用户意图决定是否调用 helper，并调整 toast 文案
    (d) 失败路径不变

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES。可维护性 5/5（helper 复用、不硬编码）、健壮性 5/5（时序安全、失败不污染）、可扩展性 4/5（未来扩展方便）。
  - 是否遗漏了已知边界条件？ → NO。四个边界条件全部在 execution_plan 中覆盖。
  - 是否因改动量大而想缩减方案？ → NO。只改 3 个文件，改动量很小。
  - 是否打算跳过某些文件？ → NO。affected_scope 只有 3 个文件，全部在 execution_plan 中。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1 覆盖 settings-ui-config.ts，step_2 覆盖 index.ts，step_3 覆盖 popup-bindings-status.ts。
  - context是否充分？ → YES。已读取所有相关文件的完整内容。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO。全部使用标准编辑工具。
</analysis>

---

<output_quality_review>
task_summary: 在模式切换（原生/SQLite）事件中新增确认弹框，询问用户是否恢复目标模式的默认填表提示词。实现包含：新增按目标模式恢复默认提示词的 helper 函数、在模式切换成功后根据用户意图执行恢复操作。
deliverables:
  - src/presentation/triggers/settings-ui-sync/settings-ui-config.ts — 新增 applyModeDefaultCharCardPrompt_ACU 函数
  - src/presentation/triggers/settings-ui-sync/index.ts — 导出新函数
  - src/presentation/pages/popup-bindings-status.ts — 模式切换事件加入 confirm 确认框 + 恢复默认逻辑

metrics:
  total_files_modified: 3
  execution_plan_coverage: 3/3 = 100%
  edge_cases_handled: 4/4 = 100%
  confidence_assessment: HIGH
    - 所有产物经过 TypeScript 编译验证（零错误），逻辑时序经逐行审查确认安全

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。
    - applyModeDefaultCharCardPrompt_ACU：按 mode 参数映射到正确的默认常量，调用 save + loadUI 刷新，逻辑完整且有明确职责
    - popup-bindings-status.ts 的 confirm 逻辑：在正确的时序位置（switchStorageMode 成功后）执行恢复，失败路径不触及提示词
  - 产物是否能被其目标对象的变化所"击穿"？
    → YES。
    - 如果把 DEFAULT_CHAR_CARD_PROMPT_SQL_ACU 的内容改为空数组，applyModeDefaultCharCardPrompt_ACU('sqlite') 会将 charCardPrompt 设为空数组，行为会直接变化
    - 如果把 switchStorageMode 改为总是抛错，恢复默认逻辑永远不会执行
  - 实质性比率: 3/3 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。三个文件全部修改完成。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。settings-ui-config.ts、index.ts、popup-bindings-status.ts 全部覆盖。
  - 核心业务逻辑是否都有直接验证？
    → YES。TypeScript 编译零错误确认类型安全；逻辑审查确认时序正确。
  - affected_scope 覆盖率: 3/3 = 100%

value_density_check:
  - 产物中高价值内容与低价值内容的比例是多少？
    → 高价值:低价值 = 3:0，高价值占比 100%。每个改动都直接服务于核心需求。
  - 是否存在"用数量掩盖质量"的模式？
    → NO。总共只修改了 3 个文件，每处改动都有明确的工程理由。

alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 本质需求。用户需要的是"模式切换时有机会恢复到目标模式的默认提示词"，实现覆盖了：确认弹框、时序安全（切换成功后才恢复）、UI 刷新、失败路径不污染提示词。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。代码风格与项目现有约定一致（confirm、showToastr、saveSettingsAndNotify 的使用方式与项目其他地方相同），helper 函数复用了已有的常量映射模式，不引入新的耦合或副作用。
</output_quality_review>
