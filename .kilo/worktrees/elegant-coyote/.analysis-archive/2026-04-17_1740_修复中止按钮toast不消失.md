<analysis>
context:
  1. plot-planning-ui.ts 的 runOptimizationLogicWithUI_ACU 流程：
     - 第30-40行：创建进度 toast（timeOut:0, tapToDismiss:false, closeButton:false）— 永不自动消失
     - 第43-70行：200ms 后绑定中止按钮 click handler
     - 第73行：await runOptimizationLogic_ACU(userMessage, options) — 阻塞等待
     - 第76行：try { if ($toast) toastr_API_ACU.clear($toast); } catch (e) {} — 清除 toast
  
  2. 中止按钮 click handler（第44-66行）：
     - 调用 abortController_ACU.abort()
     - 调用 toastr_API_ACU.clear($toast) 清除 toast
     - 设置 _set_isProcessing_Plot_ACU(false)
     - 500ms 后弹出"规划任务已被用户中止"的 info toast
  
  3. plot-task-engine.ts 的重试循环（第320-358行）：
     - 每次重试前调用 checkPlotAbortRequested_ACU() 检查 abort 信号
     - callApi_ACU 传递了 abortSignal 给 fetch（第65行 signal: abortSignal）
     - 重试间隔：await new Promise(resolve => setTimeout(resolve, 5000)) — 这个 setTimeout 不响应 abort 信号
     - 403 错误被 catch 住后，apiError 被设置，logWarn 输出日志，然后进入下一次重试
  
  4. 关键发现 — 两个根因：
     a) 重试等待的 setTimeout 不响应 abort 信号，用户点中止后最多等 5 秒才生效
     b) toastr_API_ACU.clear() 可能在某些边界条件下不生效，需要 DOM 级兜底

needs:
  - 用户点击中止按钮后，toast 必须立即消失
  - abort 信号必须能中断重试等待的 setTimeout

key_challenges:
  - setTimeout 不支持 AbortSignal（原生 JS 限制），需要用 AbortSignal 监听器 + clearTimeout 实现可中断等待
  - toastr 的 clear 方法可能在某些边界条件下不生效，需要 DOM 级兜底

confidence: HIGH

approach:
  三维评分：
  - 可维护性: 5/5 — 修改集中在两个文件，逻辑清晰
  - 健壮性: 5/5 — 双重保障
  - 可扩展性: 4/5 — 可中断等待模式可复用

edge_cases:
  - abortController_ACU 为 null 时
  - abort 信号在 setTimeout 刚开始时触发 vs 即将结束时触发
  - toastr_API_ACU 本身为 null 时的兜底

affected_scope:
  - src/service/runtime/plot-runtime/plot-task-engine.ts
  - src/presentation/components/plot-planning-ui.ts

execution_plan:
  - step_1: 修改 plot-task-engine.ts，将重试等待的 setTimeout 替换为可被 abortController 中断的等待
  - step_2: 修改 plot-planning-ui.ts 的中止按钮 click handler，增加 DOM 级兜底移除
  - step_3: 运行测试确认无回归

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
</analysis>

---

<output_quality_review>
task_summary: 修复剧情推进中止按钮点击后 toast 不消失的问题。根因是重试等待的 setTimeout 不响应 abort 信号 + toast 清理缺少 DOM 级兜底。
deliverables:
  - src/service/runtime/plot-runtime/plot-task-engine.ts — 重试等待改为可被 abort 信号中断
  - src/presentation/components/plot-planning-ui.ts — 中止按钮增加 DOM 级兜底移除

metrics:
  total_files_modified: 2
  execution_plan_coverage: 3/3 = 100%
  edge_cases_handled: 3/3 = 100%
  confidence_assessment: HIGH

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被目标对象的变化所"击穿"？ → YES
  - 实质性比率: 2/2 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - affected_scope 覆盖率: 2/2 = 100%

value_density_check:
  - 高价值:低价值 = 2:0，高价值占比 100%

alignment_check:
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>