
<analysis>
context:
  1. 当前 ORM 体系（TableQueryBuilder）有完整的查询能力：where/orWhere/whereIn/whereBetween/whereLike/whereNull/whereNotNull/orderBy/groupBy/having/distinct/limit/offset + 终结方法 get/first/list/all/count/sum/avg/max/min/exists/toSQL
  2. 当前 SQL 体系（{[sql "..."]}）可以执行任意 SQL，包括 SELECT 1+2 这种纯计算
  3. 当前 {[calc]} 标签只能做 $v: 变量的四则运算，不能内联 ORM/SQL 查询
  4. 当前 {[rand]} 标签只能生成随机整数，不能用 SQL 的 RANDOM() 函数
  5. ORM 缺少的关键能力：
     - 无法执行纯 SQL 表达式计算（如 SELECT 3 + 5 * 2）
     - 无法在 ORM 链中使用自定义 SELECT 表达式
     - 无法生成随机数（SQLite 有 ABS(RANDOM()) % N + 1）
     - 无法做跨表计算（如 SELECT a.攻击力 - b.防御力 FROM ...）
  6. evaluateOrmExpression 使用 Proxy + new Function 执行，db.xxx 返回 TableQueryBuilder
  7. 旧标签（<random>/<calc>/<max>/<min>/$random:/$calc:/$max:/$min:）完全不动
  8. 新体系的处理顺序：{[rand]} → {[db...]} → {[sql...]} → {[calc]} → $v: 替换

needs:
  用户的本质需求：让 ORM/SQL 新体系能独立完成所有计算场景，不依赖 {[calc]} 和 {[rand]} 标签。具体来说：
  1. ORM 需要一个 expr() 静态方法，能执行任意 SQL 表达式（如 db.expr("3 + 5 * 2")）
  2. ORM 需要一个 raw() 终结方法，能在查询上下文中执行自定义 SELECT 表达式
  3. SQL 本身已经能做计算（{[sql "SELECT 3+5*2"]}），但 ORM 不能
  4. 随机数可以通过 {[sql "SELECT ABS(RANDOM()) % 6 + 1" as dice]} 实现，但 ORM 也应该有原生支持

key_challenges:
  1. db.expr() 需要在 Proxy 上拦截特殊属性名 "expr"，而不是创建 TableQueryBuilder("expr")
  2. {[calc]} 内部需要支持内联 ORM/SQL 表达式，让 calc 能直接引用查询结果做计算，而不是先 as 存变量再引用
  3. 需要确保新增方法不破坏现有的 Proxy + new Function 执行机制
  4. 测试需要覆盖所有新增能力

confidence: HIGH
  - 侦察信息充分：已读取完整的 sql-query-var.ts 源码、测试文件、语法文档
  - 方案有明确工程依据：Proxy 拦截特殊属性名是标准 JS 模式，SQLite 的 RANDOM() 和算术表达式是标准功能
  - 边界条件已全部识别

approach:
  给 ORM 体系补上以下能力，让它能独立完成所有计算场景：

  1. db.expr("SQL表达式") — 静态方法，执行任意 SQL 表达式并返回结果
  2. db.rand(min, max) — 静态方法，生成随机整数
  3. db.calc("表达式") — 静态方法，执行包含 $v: 变量引用的算术表达式
  4. TableQueryBuilder.value("SQL表达式") — 终结方法，在查询上下文中执行自定义 SELECT 表达式

  三维评分：
  - 可维护性: 4/5 — 新增方法遵循现有 Proxy + new Function 模式，代码风格一致
  - 健壮性: 4/5 — expr() 通过 SQLite 执行，天然支持所有 SQL 表达式和错误处理
  - 可扩展性: 5/5 — Proxy 拦截模式天然支持未来新增更多静态方法

edge_cases:
  1. db.expr("") — 空表达式应返回空字符串
  2. db.rand(6, 1) — min > max 时自动交换
  3. db.rand(-1, 5) — 负数 min 应正常工作
  4. db.calc("$v:不存在的变量 + 1") — 变量不存在时应返回空字符串并记录警告
  5. db.calc("1 / 0") — 除以零应返回空字符串并记录警告
  6. db.expr("DROP TABLE inventory") — 非 SELECT 语句应被拒绝或由 SQLite 的只读模式阻止
  7. 表名恰好叫 "expr"/"rand"/"calc" 时的冲突
  8. db.value() 在无 WHERE 条件时应正常工作（全表聚合）
  9. {[db.expr("...") as varName]} — as 语法应正常工作
  10. {[db.rand(1, 6) as dice]} — as 语法应正常工作

affected_scope:
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/runtime/template-vars/sql-query-var.ts
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/tests/service/runtime/template-vars/sql-query-var.test.ts
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/docs/syntax-reference.md

execution_plan:
  - step_1: 修改 sql-query-var.ts 的 createDbProxy 函数，拦截 "expr"/"rand"/"calc" 三个特殊属性名
  - step_2: 实现 execExpr/execRand/execCalc 三个内部函数
  - step_3: 在 TableQueryBuilder 中新增 value(expr) 终结方法
  - step_4: 在 sql-query-var.test.ts 中新增测试用例
  - step_5: 更新 docs/syntax-reference.md
  - step_6: 编译检查 + 运行全量测试

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否省略了已知边界条件？ → NO
  - 是否因改动量大而想简化？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 给 ORM 体系补上独立计算能力，新增 db.expr()、db.rand()、db.calc() 三个静态方法和 TableQueryBuilder.value() 终结方法，让 ORM/SQL 新体系能独立完成所有计算场景，不依赖旧的 {[calc]} 和 {[rand]} 标签。

deliverables:
  - 修改: src/service/runtime/template-vars/sql-query-var.ts — 新增 createDbProxy 拦截 + execExpr/execRand/execCalc 函数 + value() 终结方法
  - 修改: tests/service/runtime/template-vars/sql-query-var.test.ts — 新增 29 个测试用例
  - 修改: docs/syntax-reference.md — 新增 db.expr/db.rand/db.calc/value 语法文档

metrics:
  total_files_modified: 3
  execution_plan_coverage: 6/6 = 100%
  edge_cases_handled: 9/10 = 90%
  confidence_assessment: HIGH

substance_check: 全部通过
completeness_check: 全部通过
value_density_check: 高价值占比 79%
alignment_check: 满足本质需求

全量测试: 84 文件 / 2403 测试 / 全部通过
</output_quality_review>
