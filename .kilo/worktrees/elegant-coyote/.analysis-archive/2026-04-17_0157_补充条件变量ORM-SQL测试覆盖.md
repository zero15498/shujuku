<analysis>
context:
  源码分析：
  1. seed-condition.ts（451行）导出 2 个函数：evaluateSeedExpression_ACU 和 evaluateCondExpression_ACU
     - evaluateCondExpression_ACU 内部调用 evaluateSubCondition_ACU（私有），支持 8 种前缀：seed:/cell:/random:/calc:/max:/min:/db:/sql:
     - 每种前缀都支持 ! 取反
     - 支持括号分组、& (AND)、, (OR) 运算符
  2. sql-query-var.ts（473行）导出 6 个函数/类：TableQueryBuilder、evaluateOrmExpression、evaluateRawSqlExpression、replaceDbSqlVariables、evaluateDbCondition、evaluateSqlCondition
     - TableQueryBuilder.where() 支持 6 种操作符：=、>、>=、<、<=、!=
     - where() 的 value=null 时生成 IS NULL（operator='='）或 IS NOT NULL（operator='!='）
     - escapeParam() 处理 null/number/string（含单引号转义）
     - isTruthy() 处理 null/undefined/''/'false'/'0'/number/boolean
     - formatResult() 处理 null/boolean/number/string/array(primitives)/array(objects)/object
     - evaluateDbCondition() 有两个分支：result 是 boolean 直接返回 vs 非 boolean 做 isTruthy 判断
  
  现有测试覆盖：
  1. seed-condition.test.ts（149行，26用例）：
     - evaluateSeedExpression_ACU：13用例，覆盖全面
     - evaluateCondExpression_ACU：13用例，覆盖了 seed:/cell:/random:/calc: 前缀，但完全缺失 db:/sql:/max:/min: 前缀
  2. sql-query-var.test.ts（437行，47用例）：
     - TableQueryBuilder：覆盖了 get/first/list/all/count/sum/exists/where(=)/where(>)/where(>=)/where(null)/orderBy/limit/toSQL
     - 缺失：where(<)/where(<=)/where(!=)/where(IS NOT NULL)/多条件链式where/escapeParam单引号转义
     - evaluateDbCondition：覆盖了 exists(true/false)/比较(>/> false)/空/自动补前缀/抛错
     - 缺失：纯ORM返回非布尔值的isTruthy判断（如count()返回0）、count()==0边界
     - evaluateSqlCondition：覆盖了有结果/无结果/COUNT/中文名/抛错/空
     - 缺失：COUNT为零返回false、isTruthy边界值('false'/'0'/0)
  3. if-block-and-condition.test.ts（236行，23用例）：
     - 覆盖了 <if db="...">/<if sql="..."> 标签解析和 evaluateCondExpression_ACU 的 db:/sql: 前缀路由
     - 但这些是通过 mock evaluateDbCondition/evaluateSqlCondition 测的，不是端到端

needs:
  - 补充 seed-condition.test.ts 中 evaluateCondExpression_ACU 缺失的 db:/sql:/max:/min: 前缀测试
  - 补充 sql-query-var.test.ts 中 TableQueryBuilder 缺失的 where 操作符和多条件链式调用
  - 补充 sql-query-var.test.ts 中 evaluateDbCondition 的 isTruthy 边界和 evaluateSqlCondition 的 COUNT=0 边界
  - 补充 escapeParam 单引号转义的间接测试

key_challenges:
  1. seed-condition.test.ts 中 db:/sql: 前缀的测试需要 mock evaluateDbCondition/evaluateSqlCondition，这两个函数已经在文件顶部被 mock 了（返回 false），需要在新测试中动态修改 mock 返回值
  2. sql-query-var.test.ts 使用真实 SqliteEngine，新增的 where 操作符测试需要利用已有的测试数据（inventory 和 characters 表）
  3. isTruthy 是私有函数，只能通过 evaluateDbCondition 和 evaluateSqlCondition 间接测试

confidence: HIGH

approach:
  在两个现有测试文件中追加新的 describe 块，补充缺失的用例。不创建新文件。
  
  三维评分：
  - 可维护性: 5/5 — 在现有测试文件中追加，复用已有的 mock 设置和测试数据，与现有测试风格一致
  - 健壮性: 5/5 — 覆盖所有缺失的操作符、前缀、边界条件，每个分支都有正反两面测试
  - 可扩展性: 5/5 — 新增的 describe 块独立于已有测试，未来新增操作符或前缀时只需追加用例

edge_cases:
  - where('列名', '<', 值) 生成 SQL 中 < 操作符正确
  - where('列名', '<=', 值) 生成 SQL 中 <= 操作符正确
  - where('列名', '!=', 值) 生成 SQL 中 != 操作符正确
  - where('列名', '!=', null) 生成 IS NOT NULL
  - 多个 where() 链式调用生成 AND 连接的多条件
  - escapeParam 对包含单引号的字符串正确转义（如 O'Brien → 'O''Brien'）
  - evaluateDbCondition 对 count() 返回 0 时 isTruthy 返回 false
  - evaluateDbCondition 对 get() 返回 null 时 isTruthy 返回 false
  - evaluateDbCondition 对 get() 返回空字符串时 isTruthy 返回 false
  - evaluateSqlCondition 对 SELECT COUNT(*) 返回 0 时返回 false
  - evaluateSqlCondition 对 SELECT 'false' 时 isTruthy 返回 false
  - evaluateCondExpression_ACU 的 db: 前缀正确路由到 evaluateDbCondition
  - evaluateCondExpression_ACU 的 sql: 前缀正确路由到 evaluateSqlCondition
  - evaluateCondExpression_ACU 的 !db: 取反正确
  - evaluateCondExpression_ACU 的 !sql: 取反正确
  - evaluateCondExpression_ACU 的 max: 前缀正确路由到 evaluateMaxCondition_ACU
  - evaluateCondExpression_ACU 的 min: 前缀正确路由到 evaluateMinCondition_ACU
  - evaluateCondExpression_ACU 的 !max: 和 !min: 取反正确
  - evaluateCondExpression_ACU 的 !random: 和 !calc: 取反正确

affected_scope:
  - tests/service/runtime/template-vars/seed-condition.test.ts
  - tests/service/runtime/template-vars/sql-query-var.test.ts

execution_plan:
  - step_1: 修改 sql-query-var.test.ts，追加 where(<)/where(<=)/where(!=)/IS NOT NULL/多条件链式where/escapeParam单引号转义/evaluateDbCondition isTruthy边界/evaluateSqlCondition COUNT=0和isTruthy边界
  - step_2: 修改 seed-condition.test.ts，追加 db:/sql:/max:/min: 前缀测试和 !random:/!calc: 取反测试
  - step_3: 运行新增测试验证通过
  - step_4: 运行全量测试确保无回归

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否省略了已知边界条件？ → NO
  - 是否因改动量大而想简化？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>
---
<output_quality_review>
task_summary: 补充条件变量测试在 ORM 和 SQL 两种模式下的缺失用例。在 sql-query-var.test.ts 中追加 22 个用例（where 操作符 </<=/!=/IS NOT NULL、多条件链式 where、escapeParam 单引号转义、evaluateDbCondition isTruthy 边界、evaluateSqlCondition isTruthy 边界），在 seed-condition.test.ts 中追加 25 个用例（db:/sql:/max:/min: 前缀正向和取反、!random:/!calc: 取反、混合组合）。
deliverables:
  - tests/service/runtime/template-vars/sql-query-var.test.ts（修改，47→69 用例）
  - tests/service/runtime/template-vars/seed-condition.test.ts（修改，26→51 用例）

metrics:
  total_files_modified: 2
  execution_plan_coverage: 4/4 = 100%
  edge_cases_handled: 19/19 = 100%
  confidence_assessment: HIGH

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被目标对象的变化所"击穿"？ → YES
  - 实质性比率: 47/47 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - 核心业务逻辑是否都有直接验证？ → YES
  - affected_scope 覆盖率: 2/2 = 100%

value_density_check:
  - 高价值:低价值 = 40:7 = 85% 高价值
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 用户本质需求：确认条件变量在 ORM/SQL 两种模式下的测试覆盖所有情况，补齐缺失
  - "如果这是别人交给我的，我会接受吗？" → YES

注：执行过程中发现 evaluateCondExpression_ACU 的解析器会把 () 当作括号分组处理，db: 前缀后的表达式不应包含 ()。测试已按实际行为修正。
</output_quality_review>
