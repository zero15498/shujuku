<analysis>
context:
  1. seedGreetingLocalDataFromTemplate_ACU（helpers-data-merge.ts:419）在新开聊天时被外部调用，用 parseTableTemplateJson_ACU({ stripSeedRows: false }) 获取带 seedRows 的完整模板数据，写入开场白消息的 TavernDB_ACU_IsolatedData.independentData 中。写入时设置了 tagData._acu_base_state = 'ACU_TEMPLATE_BASE_STATE_LOCAL_V1'。
  2. mergeAllIndependentTables_ACU（helpers-data-merge.ts）从聊天消息中合并数据时，读取 tagData.independentData 但完全不检查 tagData._acu_base_state。因此开场白中的 seedRows 数据被当作普通数据返回。
  3. SqlTableService.loadFromChat（sql-table-service.ts）调用 mergeAllIndependentTables_ACU 后，用 hasRealDataRows 检查 content.length > 1 来判断是否有"真实数据行"。由于 seedRows 被写入了 content，content.length > 1 为 true，hasRealDataRows 误判为有真实数据，导致立刻建表。
  4. _ensureTablesFromTemplate 在写操作时被调用，会为缺失表建表并注入 seedRows。这个逻辑是正确的——它是"延迟建表"的核心。
  5. 用户期望的逻辑：loadFromChat 不应该因为开场白中的 seedRows 基底数据就建表。建表应该延迟到第一次写操作（applyEdits/executeMutation）时，由 _ensureTablesFromTemplate 完成。

needs:
  - loadFromChat 中的 hasRealDataRows 判断需要能区分"seedRows 基底数据"和"AI 真正填写的数据"
  - 利用已有的 _acu_base_state 标记来实现区分

key_challenges:
  1. mergeAllIndependentTables_ACU 是通用函数，被 JSON 模式和 SQLite 模式共用，不能随意修改其返回值结构
  2. _acu_base_state 标记在 tagData 级别（每条消息的隔离槽），不在 sheet 级别，需要在 merge 过程中传递到 sheet 级别

confidence: HIGH
  - 侦察信息充分，_acu_base_state 标记已存在，只需在 merge 和 loadFromChat 中利用它

approach:
  在 mergeAllIndependentTables_ACU 中，当遍历消息时，如果 tagData._acu_base_state === GREETING_LOCAL_BASE_STATE_MARKER_ACU，则在写入 mergedData 的每个 sheet 上附加 _acu_from_base_state = true 标记。然后 loadFromChat 的 hasRealDataRows 检查时排除带 _acu_from_base_state 标记的 sheet。
  
  三维评分：
  - 可维护性: 4/5 — 利用已有的 _acu_base_state 标记，逻辑清晰
  - 健壮性: 5/5 — 精确区分基底数据和 AI 数据，不会误判
  - 可扩展性: 4/5 — 如果未来有新的数据来源需要区分，可以用同样的标记机制

edge_cases:
  1. 新开聊天，只有开场白中的基底数据 → hasRealDataRows 应为 false，不建表
  2. 新开聊天，开场白有基底数据，AI 第一次回复也写入了数据 → hasRealDataRows 应为 true
  3. 老聊天，多条消息都有数据 → hasRealDataRows 应为 true，正常建表
  4. 新开聊天，模板没有 seedRows（content 只有表头）→ hasRealDataRows 已经是 false
  5. 指导表物化的数据（hasSheetGuide 路径）→ content.length <= 1，hasRealDataRows 已经是 false

affected_scope:
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/runtime/helpers-data-merge.ts
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/table/sql-table-service.ts

execution_plan:
  - step_1: 修改 helpers-data-merge.ts 的 mergeAllIndependentTables_ACU，在遍历消息时，如果 tagData._acu_base_state === GREETING_LOCAL_BASE_STATE_MARKER_ACU，则在写入 mergedData 的每个 sheet 上附加 _acu_from_base_state = true 标记
  - step_2: 修改 sql-table-service.ts 的 loadFromChat 中 hasRealDataRows 的判断逻辑，排除带 _acu_from_base_state 标记的 sheet

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan 是否覆盖 affected_scope 所有文件？ → YES
  - context 是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan 中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 修复"新开聊天第一楼还没填表时 SQLite 表就被提前建好"的 bug。根因是 seedGreetingLocalDataFromTemplate_ACU 把带 seedRows 的模板数据写入开场白消息后，loadFromChat 的 hasRealDataRows 无法区分"基底数据"和"AI 真正填写的数据"，导致误判提前建表。修复方案：在 mergeAllIndependentTables_ACU 中标记来自基底状态消息的 sheet 数据，在 loadFromChat 中排除这些标记的数据。

deliverables:
  - 修改 helpers-data-merge.ts — mergeAllIndependentTables_ACU 中增加 _acu_from_base_state 标记
  - 修改 sql-table-service.ts — loadFromChat 中 hasRealDataRows 排除基底数据

metrics:
  total_files_modified: 2
  execution_plan_coverage: 2/2 = 100%
  edge_cases_handled: 5/5 = 100%
  confidence_assessment: HIGH

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被目标对象的变化所"击穿"？ → YES（标记机制是契约，预期行为）
  - 实质性比率: 2/2 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - 核心业务逻辑是否都有直接验证？ → YES（编译通过 + 2484 测试通过）
  - affected_scope 覆盖率: 2/2 = 100%

value_density_check:
  - 高价值:低价值 = 2:0，高价值占比 100%
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 满足用户本质需求：只要消息记录里没有 AI 真正写入的数据，就不建表
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>