
<analysis>
context: 
  - formatTableForSqliteMode 函数在输出当前数据时，表头行直接使用 table.content[0]（中文表头），导致 AI 看到中文列名后用中文属性名写 SQL
  - DDL 已经在函数开头输出（text += ddl.trim() + '\n'），DDL 中的列名是英文的
  - AI 应该看到英文列名的数据表头，与 DDL 定义一致，这样 AI 写 SQL 时才会用英文列名
  - parseDDLColumnNames(ddl) 从 shared/ddl-utils.ts 导出，可以提取 DDL 中的英文列名数组
  - 数据行 row.join(' | ') 输出的是值（中文值没问题），只有表头需要改成英文

needs: 将 formatTableForSqliteMode 中数据表头从中文改为英文（与 DDL 列名一致），让 AI 写 SQL 时使用英文列名

key_challenges: 
  - parseDDLColumnNames 返回的列名数组长度可能与 content[0] 不一致（DDL 校验应该保证一致，但需要做防御性处理）
  - 如果 parseDDLColumnNames 解析失败（返回空数组），需要 fallback 到原来的中文表头

confidence: HIGH

approach: 在 formatTableForSqliteMode 中，用 parseDDLColumnNames(ddl) 提取英文列名数组替换 table.content[0] 作为数据表头。如果解析失败则 fallback 到原中文表头。
  三维评分：
  - 可维护性: 5/5 — 只修改一处，逻辑清晰，使用已有的 parseDDLColumnNames 工具函数
  - 健壮性: 5/5 — 有 fallback 机制，解析失败不会崩溃
  - 可扩展性: 5/5 — 不影响其他模块，与现有架构完全一致

edge_cases:
  - parseDDLColumnNames 返回空数组时，fallback 到 table.content[0]
  - DDL 列数与 content[0] 列数不一致时，仍使用 DDL 列名（DDL 是 AI 写 SQL 的依据）

affected_scope:
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/ai/prompt-builder/prompt-prepare.ts

execution_plan:
  - step_1: 在 prompt-prepare.ts 顶部添加 import { parseDDLColumnNames } from '../../../shared/ddl-utils'
  - step_2: 在 formatTableForSqliteMode 函数中，将 `const headers = table.content[0] || []` 改为从 DDL 解析英文列名，fallback 到 content[0]

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 修复 SQLite 模式下 formatTableForSqliteMode 函数输出当前数据时使用中文表头的 bug，改为从 DDL 解析英文列名作为数据表头，使 AI 写 SQL 时使用英文列名。
deliverables: 
  - 修改: /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/ai/prompt-builder/prompt-prepare.ts（添加 import + 修改表头逻辑）

metrics:
  total_files_modified: 1
  execution_plan_coverage: 2/2 = 100%
  edge_cases_handled: 2/2 = 100%
  confidence_assessment: HIGH

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被目标对象的变化所"击穿"？ → YES
  - 实质性比率: 1/1 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - 核心业务逻辑是否都有直接验证？ → YES
  - affected_scope 覆盖率: 1/1 = 100%

value_density_check:
  - 高价值:低价值 = 1:0，高价值占比 100%
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 满足用户本质需求：AI 在 SQLite 模式下看到英文列名数据表头，写 SQL 时使用英文列名
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>
