<analysis>
context: 
  1. 项目构建链路：`package.json` 中 `build`/`build:extension`/`build:all` 统一走 `rollup.config.js`，同一套 `src` 代码分别输出到 `dist/index.bundle.js`（userscript/js版）和 `dist/extension/index.js`（插件版）。
  2. 手动填表入口：`src/presentation/pages/popup-bindings-status.ts` 第 157-158 行将 `$manualUpdateCardButton_ACU` 绑定到 `handleManualUpdate_ACU`。该文件已引入 `showCustomConfirm_ACU`。
  3. UI 壳逻辑在 `src/presentation/triggers/update-process.ts`。`handleManualUpdate_ACU()` 负责收集 UI 选择的表格、调用 `orchestrateManualUpdate_ACU()`、根据结果显示 toast。
  4. 手动更新核心编排在 `src/service/table/update-orchestrator.ts` 第 503-629 行。该函数会计算 `contextScopeIndices`、按 `groupId` 组织更新组、对每个组调用 `processBatch(..., 'manual_independent', ...)`。
  5. 批处理保存目标楼层：`processUpdatesBatch_ACU()` 中每批 `finalSaveTargetIndex = lastMessageIndexOfBatch`。`executeCardUpdateCore_ACU()` 成功后调用 `saveIndependentTableToChatHistory_ACU(saveTargetIndex, ...)`。
  6. 原生/SQL 统一抽象：`ITableStorageProvider` + `table-storage-strategy.ts`。SQLite 模式的 `applyEdits()` 基于当前状态执行严格 SQL，旧数据残留会导致写入失败。
  7. 消息级存储：新版 `IsolatedData[isolationKey]`（含 independentData/modifiedKeys/updateGroupKeys），旧版兼容字段（IndependentData/Data/SummaryData/ModifiedKeys/UpdateGroupKeys/Identity）。
  8. 已有 `purgeSheetKeysFromMessage_ACU(msg, sheetKeys)` 只能按表删，以及 `clearAllTableFields_ACU(msg)` 无差别清空（不区分隔离标签）。这两个都不能直接满足"按当前隔离标签清空单消息全部表格数据"的需求。
  9. `chat-service.ts` 已有 `deleteLocalDataInChatCore_ACU()` 按楼层范围删除，但它有 mode='current'|'all' 的隔离匹配逻辑，不支持指定具体 messageIndex 列表，且不支持"只清当前标签"。
needs:
  1. 手动填表前弹出插件内确认框。
  2. 用户确认后清空目标楼层全部表格数据（覆盖新版+旧版字段，只清当前隔离标签）。
  3. 清空后刷新运行时数据再继续填表。
  4. 原生模式与 SQLite 模式共享同一条编排路径。
  5. 构建后验证双产物包含新逻辑。
key_challenges:
  1. "对应楼层"不是全局唯一，而是每个 update group 的 contextScopeIndices 涉及的楼层。
  2. 新旧两种存储字段都必须清空。
  3. 清空不能误删其他隔离标签的数据。
  4. 清空后必须刷新 provider/merged data。
  5. 确认框与清空动作需分层。
confidence: HIGH
approach:
  三维评分：
  - 可维护性: 5/5 — repository 层新增精确 helper、service 层封装、编排层接入、presentation 层确认框，四层职责清晰。
  - 健壮性: 5/5 — 覆盖新版+旧版字段、按隔离标签精确清理、幂等空操作、清空后刷新。
  - 可扩展性: 4/5 — clearTableFieldsForIsolation_ACU 和 clearTableDataAtFloors_ACU 可复用于其他场景。
edge_cases:
  - targetKeys 为空时在确认框前拦截，不弹出确认框
  - 多 group 场景：收集所有 contextScopeIndices 的楼层去重后清空
  - 无表格字段的消息视为成功空操作
  - 新旧字段共存时全部清理
  - 清空后必须 refreshData + loadAllChatMessages
  - 用户取消时直接终止，不触发任何清空或 AI 调用
affected_scope:
  - src/data/repositories/chat-message-data-repo.ts（新增 clearTableFieldsForIsolation_ACU）
  - src/service/chat/chat-service.ts（新增 clearTableDataAtFloors_ACU）
  - src/service/table/update-orchestrator.ts（新增 clearBeforeUpdate 选项 + 预清空步骤）
  - src/presentation/triggers/update-process.ts（新增确认框 + 传入 clearBeforeUpdate: true）
  - dist/index.bundle.js（构建验证）
  - dist/extension/index.js（构建验证）
execution_plan:
  - step_1: 在 chat-message-data-repo.ts 新增 clearTableFieldsForIsolation_ACU，按隔离标签精确清空单条消息的表格数据
  - step_2: 在 chat-service.ts 新增 clearTableDataAtFloors_ACU，封装"按 messageIndex 列表清空 + 保存"
  - step_3: 在 update-orchestrator.ts orchestrateManualUpdate_ACU 中新增 clearBeforeUpdate 选项和预清空步骤
  - step_4: 在 update-process.ts handleManualUpdate_ACU 中加入确认框并传入 clearBeforeUpdate: true
  - step_5: 构建并验证两端产物包含新逻辑
degradation_check:
  - 方案是否是三维评估综合最优的？ → YES。三层职责清晰、健壮性覆盖全面、可扩展。
  - 是否遗漏了已知边界条件？ → NO。已列出空目标表、多 group、新旧字段、空操作、取消路径等。
  - 是否因改动量大而想缩减方案？ → NO。
  - 是否打算跳过某些文件？ → NO。所有涉及文件都已处理。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。
  - context是否充分？ → YES。已读取所有关键文件。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO。
</analysis>
---
<output_quality_review>
task_summary: 为手动填表功能新增"用户确认后先清除对应楼层的全部表格数据，再执行新的数据填写"逻辑，解决 SQL 严格填表因旧数据残留导致手动填表写入失败的问题。同时确保原生模式和 SQLite 模式共享同一条编排路径，插件版与 js 版产物都包含新逻辑。
deliverables:
  - src/data/repositories/chat-message-data-repo.ts — 新增 clearTableFieldsForIsolation_ACU（按隔离标签精确清空单消息表格数据）
  - src/service/chat/chat-service.ts — 新增 clearTableDataAtFloors_ACU（按 messageIndex 列表清空 + 保存）
  - src/service/table/update-orchestrator.ts — 新增 clearBeforeUpdate 选项 + 预清空步骤
  - src/presentation/triggers/update-process.ts — 新增确认框 + 前置校验 + 传入 clearBeforeUpdate: true
  - dist/index.bundle.js — 构建产物已更新并验证
  - dist/extension/index.js — 构建产物已更新并验证

# 量化指标总览
metrics:
  total_files_modified: 4 — 修改的源码文件数（不含构建产物）
  execution_plan_coverage: 5/5 = 100% — 所有步骤已完成
  edge_cases_handled: 6/6 = 100% — 边界条件处理覆盖率
  confidence_assessment: HIGH — 所有产物经过验证，无已知遗漏

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。
    - clearTableFieldsForIsolation_ACU：57 行完整实现，覆盖新版 IsolatedData 标签槽删除 + 旧版兼容字段条件删除 + 返回 changed 标志。
    - clearTableDataAtFloors_ACU：41 行完整实现，包含参数校验、隔离配置构建、AI 消息过滤、逐条清空、保存。
    - orchestrateManualUpdate_ACU 新增预清空步骤：收集所有 group 的 contextScopeIndices、去重、调用 clearTableDataAtFloors_ACU、刷新。
    - handleManualUpdate_ACU 确认框：前置校验 + showCustomConfirm_ACU + 取消路径 + 传入 clearBeforeUpdate。
  - 产物是否能被其目标对象的变化所"击穿"？
    → YES。如果把 clearTableFieldsForIsolation_ACU 中的字段名改掉，clearTableDataAtFloors_ACU 和 handleManualUpdate_ACU 都会失效；如果把 orchestrateManualUpdate_ACU 的 options 签名改掉，handleManualUpdate_ACU 的调用会编译失败。
  - 实质性比率: 4/4 = 100%

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。repository 层、service 层、orchestrator 层、presentation 层都已覆盖。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。四个源码文件 + 两个构建产物都已完成。
  - 核心业务逻辑是否都有直接验证？
    → YES。构建产物中通过文本搜索确认了 clearBeforeUpdate、clearTableDataAtFloors_ACU、手动填表确认 等关键片段存在。
  - affected_scope 覆盖率: 6/6 = 100%

# 价值密度检查
value_density_check:
  - 产物中高价值内容与低价值内容比例：4:0，高价值占比 100%。
  - 是否存在"用数量掩盖质量"的模式？ → NO。所有修改都是解决核心问题的必要代码。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 本质需求：解决 SQL 严格填表因旧数据残留导致手动填表写入失败。实现方式是"确认后清空目标楼层 + 刷新 + 再填表"。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。分层清晰、边界条件处理完整、双产物验证通过。
</output_quality_review>
