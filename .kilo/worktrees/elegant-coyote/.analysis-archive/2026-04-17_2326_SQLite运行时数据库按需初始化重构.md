<analysis>
context:
  - initWithChatId() 在脚本启动时调用（1000ms延迟或轮询），负责初始化状态，但不包含 SQLite 初始化
  - CHAT_CHANGED 回调中有完整的 SQLite 生命周期管理：先 disposeStorageProvider()，1200ms 后 reloadStorageProvider()
  - loadFromChat() 已经区分新卡（mergedData=null → 只初始化引擎不建表）和老卡（有数据 → 建表灌数据）
  - _ensureTablesFromTemplate() 是全量建表（从模板建所有表），在 applyEdits/executeQuery/executeMutation 前调用，幂等
  - 角色脚本加载场景下，酒馆可能不触发 CHAT_CHANGED，导致 SQLite 引擎不被初始化
  - getStorageProvider() 有懒初始化机制，但只创建实例不调用 loadFromChat()，所以数据是空的

needs:
  - 在 initWithChatId() 中加入 SQLite 初始化逻辑，确保角色脚本加载场景下老卡能正确初始化
  - 新卡场景下 loadFromChat() 已经正确处理（只初始化引擎不建表），无需额外改动
  - 中途加的表在第一次写入时建表——_ensureTablesFromTemplate() 需要从"全有或全无"改为"按需建表"

key_challenges:
  - initWithChatId() 中的 SQLite 初始化需要放在正确的时机——必须在 refreshMergedDataAndNotifyWithUI 之前
  - 需要避免与后续可能到来的 CHAT_CHANGED 事件产生竞态
  - 中途加表的场景：_ensureTablesFromTemplate() 只在"没有任何用户表"时触发全量建表

confidence: HIGH
  - 侦察信息充分，所有相关文件已读取，方案有明确的工程依据

approach:
  在 initWithChatId() 的 refreshMergedDataAndNotifyWithUI 之前加入 SQLite 初始化逻辑。
  改 _ensureTablesFromTemplate() 从"没有任何表才建"改为"检查每张表是否存在，不存在的从模板建"。

  三维评分：
  - 可维护性: 5/5 — 改动最小，复用现有逻辑
  - 健壮性: 5/5 — 新卡/老卡/中途加表三种场景都覆盖
  - 可扩展性: 5/5 — 延迟建表逻辑从"全有或全无"改为"按需建表"

edge_cases:
  - initWithChatId 初始化 SQLite 后，紧接着 CHAT_CHANGED 触发 dispose + reload：安全
  - 新卡场景下 mergedData=null → 只初始化引擎不建表：正确
  - 中途加表：模板中新增了一张表，但 SQLite 中已有其他表，需要按需建表
  - CREATE TABLE IF NOT EXISTS 不会因表名冲突报错：安全

affected_scope:
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/presentation/bootstrap/init.ts
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/table/sql-table-service.ts

execution_plan:
  - step_1: 修改 init.ts 的 initWithChatId 函数，在 refreshMergedDataAndNotifyWithUI 之前加入 SQLite 初始化
  - step_2: 修改 sql-table-service.ts 的 _ensureTablesFromTemplate 方法，改为按需建表

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有跳过的问题？ → NO
  - 是否使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 按冈部的设计思路重构 SQLite 运行时数据库的初始化策略：新卡在第一次填表时初始化，老卡在加载时初始化，中途加的表在第一次写入时按需建表。
deliverables:
  - 修改 init.ts：initWithChatId() 中加入 SQLite 初始化逻辑（reloadStorageProvider）
  - 修改 sql-table-service.ts：_ensureTablesFromTemplate() 从"全有或全无"改为"按需建表"
  - 修改 sql-table-service.test.ts：补充 parseTableTemplateJson_ACU 的 mock

metrics:
  total_files_modified: 3
  execution_plan_coverage: 2/2 = 100%
  edge_cases_handled: 4/4 = 100%（竞态安全、新卡空引擎、中途加表、表名冲突）
  confidence_assessment: HIGH — 所有产物经过编译验证和 2476 个测试验证，无已知遗漏

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被目标对象的变化所"击穿"？ → YES
  - 实质性比率: 3/3 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - 核心业务逻辑是否都有直接验证？ → YES，2476 个测试全部通过
  - affected_scope 覆盖率: 2/2 = 100%

value_density_check:
  - 高价值:低价值 = 2:1，高价值占比 67%
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 满足冈部的本质需求：三种初始化场景都已覆盖
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>