
<analysis>
context:
  1. 现有日志体系：logDebug_ACU/logWarn_ACU/logError_ACU 定义在 src/shared/utils.ts 第 200-212 行，实现极简——直接调用 console.log/console.warn/console.error，带 [shujuku_v120] 前缀。DEBUG_MODE_ACU=true 时 logDebug 才输出，logError/logWarn 始终输出。
  2. 前端架构：SillyTavern 插件，使用 jQuery + 原生 DOM 操作，无 React/Vue。主弹窗通过 createACUWindow 创建独立窗口，内部用 Tab 页切换（.acu-tab-button + .acu-tab-content）。
  3. Tab 页模式：每个 Tab 由两部分组成——(a) generateXxxTabHTML() 生成 HTML 片段，(b) bindXxxEvents_ACU() 绑定事件。HTML 在 main-popup.ts 中拼接，事件在 popup-bindings.ts 中注册。
  4. 参考实现：sql-console.ts 是最好的参考——独立文件，导出 generateSqlConsoleTabHTML() 和 bindSqlConsoleEvents_ACU()，在 main-popup.ts 和 popup-bindings.ts 中分别引用。
  5. 导航结构：Tab 导航分为"运行"、"配置"、"数据"、"增强"、"SQL"五个分组，SQL 控制台仅在 isSqliteMode() 时显示。
  6. 日志函数被全项目广泛使用：grep 显示 logDebug_ACU 在几十个文件中被调用，是核心基础设施。
  7. SCRIPT_ID_PREFIX_ACU = 'shujuku_v120'，所有 DOM ID 都以此为前缀。

needs:
  用户需要一个前端日志页面，能够：
  1. 实时显示所有 logDebug_ACU/logWarn_ACU/logError_ACU 的输出
  2. 区分不同级别（debug/warn/error）
  3. 支持按功能模块过滤（日志中的 [模块名] 前缀）
  4. 在主弹窗中作为独立 Tab 页存在
  5. 不影响现有的 console 输出行为（新增拦截，不替换）

key_challenges:
  1. 日志拦截机制：需要在 logDebug_ACU/logWarn_ACU/logError_ACU 中增加一个回调/缓冲区，将日志同时写入内存存储，供前端页面读取。但 utils.ts 是 shared 层（零 DOM 依赖），不能直接操作 DOM。需要一个中间层——日志缓冲区在 shared 层，UI 渲染在 presentation 层。
  2. 性能：日志量可能很大（尤其是 DEBUG 模式），内存缓冲区需要有上限。
  3. 实时更新：日志页面打开时需要实时刷新，但不能用 setInterval 轮询（太浪费），应该用事件驱动。
  4. 模块标签提取：现有日志格式是 [ACU] [模块名] 消息内容，需要从消息中提取 [模块名] 用于过滤。

confidence: HIGH

approach:
  三层架构实现：
  1. shared 层：日志缓冲区 — 在 src/shared/ 新建 log-buffer.ts
  2. shared 层：修改 utils.ts — 在日志函数中增加 pushLog 调用
  3. presentation 层：日志页面 — 新建 src/presentation/pages/log-viewer.ts
  4. presentation 层：注册 Tab — 修改 main-popup.ts 和 popup-bindings.ts

  三维评分：
  - 可维护性: 5/5 — 完全遵循现有 Tab 页模式
  - 健壮性: 4/5 — 环形缓冲区有上限，subscribe/unsubscribe 防止悬挂回调
  - 可扩展性: 5/5 — 缓冲区通用，未来可加持久化/导出/远程上报

edge_cases:
  1. 日志缓冲区满时（超过 2000 条）：环形缓冲区自动丢弃最旧的日志
  2. 日志页面未打开时：日志仍然写入缓冲区，打开页面时显示历史日志
  3. 日志页面打开后快速产生大量日志：使用 requestAnimationFrame 批量渲染
  4. 日志消息包含 HTML 特殊字符：使用 escapeHtml_ACU 转义
  5. 日志 args 包含对象/数组：序列化为 JSON 字符串显示
  6. 模块标签提取失败：归类为"未分类"
  7. 暂停状态下新日志仍然写入缓冲区，恢复后一次性显示
  8. 多次 subscribe 同一个回调：去重处理

affected_scope:
  - src/shared/log-buffer.ts — 新建
  - src/shared/utils.ts — 修改
  - src/presentation/pages/log-viewer.ts — 新建
  - src/presentation/pages/main-popup.ts — 修改
  - src/presentation/pages/popup-bindings.ts — 修改
  - tests/shared/log-buffer.test.ts — 新建

execution_plan:
  - step_1: 新建 src/shared/log-buffer.ts
  - step_2: 修改 src/shared/utils.ts
  - step_3: 新建 src/presentation/pages/log-viewer.ts
  - step_4: 修改 src/presentation/pages/main-popup.ts
  - step_5: 修改 src/presentation/pages/popup-bindings.ts
  - step_6: 新建 tests/shared/log-buffer.test.ts
  - step_7: 编译检查 + 运行全量测试

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否省略了已知边界条件？ → NO
  - 是否因改动量大而想简化？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 为项目新增前端日志查看器功能。
deliverables: 6 个文件（3 新建 + 3 修改）
metrics: 85 测试文件 / 2448 测试 / 全部通过 / 构建 0 违规
confidence_assessment: HIGH
</output_quality_review>
