<analysis>
context: 经过完整侦察，原生模式手动填表错列的根因已完全确认。具体事实如下：

1. `src/shared/defaults-json.js` 第 37 行，`DEFAULT_CHAR_CARD_PROMPT_ACU` 的 mainSlot A 段落已使用 0 基对象键示例：`insertRow(表格ID, {"0":"字段0值","1":"字段1值","2":"字段2值"})`。这是正确的——原生 DSL 解析器确实按 0 基映射。

2. `src/service/ai/prompt-builder/prompt-prepare.ts` 第 82 行和第 93 行，`prepareAIInput_ACU()` 在生成 $0（当前表格数据文本）时，列头标注使用 `[i + 1]` 编号，即：
   - `[1:时间], [2:地点], [3:纪要]`
   而不是 `[0:时间], [1:地点], [2:纪要]`。

3. `src/service/ai/prompt-builder/table-edit-parser.ts` 第 354-364 行，原生模式 `insertRow` 解析逻辑使用 `headers.forEach((_, colIndex) => { data[colIndex] / data[String(colIndex)] })`，即 **colIndex 从 0 开始**。

4. 这三者的矛盾就是错列根因：
   - AI 看到列头 `[1:时间], [2:地点]...`，把列头 1 跟对象键 "1" 做映射
   - 但解析器把 "1" 映射到 headers[1]，即第二列（地点），不是第一列（时间）
   - 结果所有数据整体右移一列

5. `popup-bindings-data.ts` 第 887-890 行的合并总结 prompt 说明已标注 colIndex 从 0 开始，示例 `insertRow(10, {"0": "数据1", "1": 100})` 是正确的。

6. 项目内不存在其他使用 `{"1":...}` 格式的硬编码 1 基示例（通过 grep 在 src 内搜索无结果确认）。

7. SQLite 模式使用 SQL INSERT 语句而非 DSL 对象键，不受列头编号影响。

needs: 需求的本质不仅是修复错列，而是让 AI 在原生模式下看到的"列头编号"与"DSL 对象键语义"完全一致。如果两者编号体系不同，即使提示词反复强调"从 0 开始"，模型也会被列头 1 基编号误导。
key_challenges: 只有一个核心改动点——`prompt-prepare.ts` 中两处 `i + 1` 改为 `i`——但必须确认这个改动不会破坏 SQLite 模式下的列头展示。SQLite 模式走 `formatTableForSqliteMode()` 分支（第 62-64 行），不走 `i + 1` 的列头标注路径，所以不受影响。
confidence: HIGH
  - HIGH: 侦察信息充分，根因已完全确认（列头 1 基 vs DSL 0 基的编号不一致），方案有明确的代码级证据支持，边界条件已全部识别。
approach: 将 `prompt-prepare.ts` 中两处列头标注从 `[i + 1]` 改为 `[i]`，使列头编号与原生 DSL 0 基对象键语义一致。三维评分：
  - 可维护性: 5/5 — 改动只有一处（一个文件两行），语义变更极小，列头编号从此与 DSL 键语义统一，后续维护者不会再面对"列头是 1 基还是 0 基"的歧义。
  - 健壮性: 5/5 — SQLite 模式走独立分支不受影响；原生模式下列头编号与解析器语义完全对齐，消除了模型因编号不一致而产错列数据的风险。
  - 可扩展性: 5/5 — 不引入新的抽象或分支，不改变任何接口，只是修正一个已经错误的编号策略。
edge_cases:
  - SQLite 模式的 `formatTableForSqliteMode()` 函数（第 179-237 行）不使用这个列头标注，完全不受影响。
  - 列头为空或只有 row_id 一列时，`slice(1)` 后结果为空数组，`map` 产生空字符串，`.join` 返回空字符串——不受 `i` 值变化影响。
  - 已保存在聊天历史中的旧数据不依赖列头编号（列头只是给 AI 看的文本描述），所以这次修改不会破坏已有数据。
  - `popup-bindings-data.ts` 中的合并总结 prompt 已经写了 colIndex 从 0 开始，不需要改。
affected_scope:
  - E:\xiangmu\星河璀璨数据库\shujuku\src\service\ai\prompt-builder\prompt-prepare.ts
execution_plan:
  - step_1: 修改 `src/service/ai/prompt-builder/prompt-prepare.ts` 第 82 行，将 `[${i + 1}:${h}]` 改为 `[${i}:${h}]`（空表格列头标注分支）。
  - step_2: 修改 `src/service/ai/prompt-builder/prompt-prepare.ts` 第 93 行，将 `[${i + 1}:${h}]` 改为 `[${i}:${h}]`（有数据表格列头标注分支）。
  - step_3: 构建项目（`npm run build:all`），验证 `dist/index.bundle.js` 和 `dist/extension/index.js` 都包含修改后的列头编号。
  - step_4: 在构建产物中搜索关键片段，确认列头标注已从 `[1:` 变为 `[0:`。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES，三维评分 5/5/5，方案直接修正错误根因（列头编号与 DSL 语义不一致），不引入额外复杂度，不影响 SQLite 分支，不存在更优方案。
  - 是否遗漏了已知边界条件？ → NO，已列出 SQLite 分支独立性、空列头、旧数据兼容性、合并 prompt 一致性四个边界条件，均已确认不受影响。
  - 是否因改动量大而想缩减方案？ → NO，改动量极小（1 个文件 2 行），不存在缩减动机。
  - 是否打算跳过某些文件？ → NO，affected_scope 只有 1 个文件，已完全覆盖。
  - execution_plan是否覆盖affected_scope所有文件？ → YES，step_1 和 step_2 完全覆盖 affected_scope 中唯一的文件。
  - context是否充分？是否有未读但可能相关的文件？ → YES，context 充分。已确认 SQLite 模式分支不受影响，不需要额外读取。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO，所有发现均已纳入分析。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO，所有修改使用标准编辑工具。
</analysis>

---

<output_quality_review>
task_summary: 修复原生模式手动填表错列问题。根因是 `prompt-prepare.ts` 中列头标注使用 1 基编号（[1:列名], [2:列名]...），而原生 DSL 解析器使用 0 基对象键（"0", "1", "2"...），导致 AI 模型把列头编号 1 跟对象键 "1" 做映射，所有数据整体右移一列。修复方式是将列头标注改为 0 基编号，使列头编号与 DSL 对象键语义一致。
deliverables:
  - src/service/ai/prompt-builder/prompt-prepare.ts（修改 2 处列头标注从 `[i+1]` 改为 `[i]`）
  - dist/index.bundle.js（构建产物已更新）
  - dist/extension/index.js（构建产物已更新）

# 量化指标总览
metrics:
  total_files_modified: 1 — 源码修改文件数（不含构建产物）
  execution_plan_coverage: 4/4 = 100% — execution_plan 执行覆盖率
  edge_cases_handled: 4/4 = 100% — 边界条件处理覆盖率
  confidence_assessment: HIGH — 所有产物经过验证，无已知遗漏

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。修改的两行代码直接改变了 AI 看到的列头编号，从 `[1:时间], [2:地点]...` 变为 `[0:时间], [1:地点]...`，与 DSL 解析器的 0 基语义完全对齐。如果删除这个修改，原生模式手动填表会恢复错列行为。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → YES。如果故意把 `table-edit-parser.ts` 第 358 行的 `data[colIndex]` 改为 `data[colIndex + 1]`（让解析器也右移一列），那么这次修复就不再能解决错列——说明这个修复确实是针对特定根因的。
  - 实质性比率: 1/1 = 100%

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。affected_scope 只有 1 个文件，已完整修改。SQLite 模式的 `formatTableForSqliteMode()` 分支已确认不使用这个列头标注逻辑，不需要修改。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。affected_scope 列出 `prompt-prepare.ts`，修改已完成。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。构建产物已通过 grep 验证包含 `${i}:${h}]` 格式的列头标注代码，两端产物（userscript 和 extension）均已确认。
  - affected_scope 覆盖率: 1/1 = 100%

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 1:0，高价值占比 100%。唯一的修改直接修复了错列根因。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。修改量极小但精确。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 本质需求。用户说"原生模式的默认提示词恢复为从列0开始"，但根因实际上不在提示词本身（`DEFAULT_CHAR_CARD_PROMPT_ACU` 已经是 0 基），而在 `prompt-prepare.ts` 动态生成的列头标注。修复后，AI 看到的列头编号从 `[1:xxx]` 变为 `[0:xxx]`，与提示词中 `{"0":"..."}` 的 0 基格式一致，不再产生错列。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。改动精确、根因明确、影响范围可控、SQLite 模式不受影响、构建产物已验证。
</output_quality_review>
