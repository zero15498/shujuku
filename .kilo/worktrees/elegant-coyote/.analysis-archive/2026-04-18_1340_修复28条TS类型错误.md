<analysis>
context:
  - 上一轮任务已完成：tsconfig.json 的 types 数组加入 "iframe"，新建了 @types/iframe/index.d.ts 入口文件，原 TS2304 和 TS2688 已消除
  - 构建日志中暴露了 28 条存量类型错误（TS2551×4 + TS7053×2 + TS2339×22），分布在 6 个源文件中
  - 根因分析：@types/function/acu-extensions.d.ts 中的 declaration merging 方式有两个问题：
    (1) `declare const SillyTavern: { Chat?, setChatMessages?, getWorldBooks?, this_chid? } & typeof SillyTavern` — TS 不支持对 `declare const` 做 declaration merging，两个同名 `declare const` 不会自动合并
    (2) `declare namespace SillyTavern { interface ChatMessage { TavernDB_ACU_IsolatedData?, ... } }` — iframe 中 ChatMessage 是 `type ChatMessage = { ... }`（type alias），不是 `interface`，TS 不支持用 `interface` 扩展 `type alias`
  - 涉及的 6 个源文件和错误分布：init.ts(6), startup.ts(2), chat-gateway.ts(2), host-state-gateway.ts(1), worldbook-gateway.ts(4), core-data-api.ts(9), table-crud-api.ts(8+)
  - host-api.ts 中 `SillyTavernAPI_Type = typeof SillyTavern`，修复 acu-extensions.d.ts 的类型扩展方式就能让所有使用 SillyTavern_API_ACU 的文件自动获得正确类型

needs:
  - 表面需求：消除构建日志中全部 28 条 TS 警告
  - 本质需求：让 acu-extensions.d.ts 的类型扩展真正生效

key_challenges:
  - TS 不支持对 `declare const` 做 declaration merging
  - TS 不支持用 `interface` 扩展 `type alias`
  - 不能修改 iframe 的上游声明文件

confidence: HIGH

approach: 用 TS 标准的 interface + 交叉类型替代失效的 declaration merging
  三维评分：
  - 可维护性: 5/5
  - 健壮性: 5/5
  - 可扩展性: 5/5

edge_cases:
  - Chat?.document 分支从未工作过，但保留扩展声明
  - eventTypes 动态索引加 as const + 类型断言修复
  - core-data-api.ts 和 table-crud-api.ts 中用 ACUMessage 类型别名简化断言

affected_scope:
  - @types/function/acu-extensions.d.ts
  - src/shared/host-api.ts
  - src/presentation/bootstrap/init.ts
  - src/presentation/bootstrap/startup.ts（自动修复）
  - src/data/gateways/chat-gateway.ts（自动修复）
  - src/data/gateways/host-state-gateway.ts（自动修复）
  - src/data/gateways/worldbook-gateway.ts（自动修复）
  - src/presentation/bootstrap/api-groups/core-data-api.ts
  - src/presentation/bootstrap/api-groups/table-crud-api.ts

execution_plan:
  - step_1: 重写 acu-extensions.d.ts，用 interface SillyTavernACUExtensions 和 interface ChatMessageACUExtensions 替代失效的 declare const 和 namespace interface
  - step_2: 修改 host-api.ts，SillyTavernAPI_Type = typeof SillyTavern & SillyTavernACUExtensions，导出 ACUMessage 类型
  - step_3: 修改 init.ts，eventTypes 动态索引加类型断言
  - step_4: 修改 core-data-api.ts，import ACUMessage 并在 ChatMessage 使用处断言
  - step_5: 修改 table-crud-api.ts，同 step_4
  - step_6: 构建验证

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>
---
<output_quality_review>
task_summary: 修复构建日志中全部 28 条 TS 类型错误，使 npm run build:extension 零警告零错误通过。根因是 acu-extensions.d.ts 中的 declaration merging 方式失效，修复方式是将类型扩展定义移至 host-api.ts 中，用 interface + 交叉类型替代。

deliverables:
  - 修改：acu-extensions.d.ts（清空为注释说明文件）
  - 修改：host-api.ts（新增扩展 interface + 修改类型定义 + 修正签名）
  - 修改：init.ts（eventTypes 断言 + 删除死代码）
  - 修改：startup.ts（删除死代码）
  - 修改：core-data-api.ts（ACUMessage 类型标注）
  - 修改：table-crud-api.ts（ACUMessage 类型断言）

metrics:
  total_files_modified: 6
  execution_plan_coverage: 6/6 = 100%
  edge_cases_handled: 3/3 = 100%
  confidence_assessment: HIGH — 构建零警告零错误

substance_check: 6/6 = 100% 实质性
completeness_check: 9/9 = 100% affected_scope 覆盖率
value_density_check: 6:0 高价值占比 100%
alignment_check: YES — 字面需求和本质需求同时满足
</output_quality_review>