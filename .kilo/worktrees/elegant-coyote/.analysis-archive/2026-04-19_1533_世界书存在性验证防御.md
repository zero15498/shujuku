<analysis>
context:
  - getInjectionTargetLorebook_ACU() 在 injection-engine-state.ts 第 95-101 行定义
  - 当 injectionTarget === 'character' 时，调用 gwGetCurrentCharPrimaryLorebook_ACU()
  - gwGetCurrentCharPrimaryLorebook_ACU() 在 worldbook-gateway.ts 第 116 行定义，直接调用 TavernHelper_API_ACU.getCurrentCharPrimaryLorebook()
  - SillyTavern 的 TavernHelper API 可能返回一个不存在的世界书名称（残留值、缓存问题等）
  - 当 injectionTarget 不是 'character' 时，直接返回 target 字符串——如果用户之前设置了一个后来被删除的世界书名，也会出同样的问题
  - getInjectionTargetLorebook_ACU 有 19 处引用分布在 8 个文件中，是所有世界书操作的入口
  - worldbook-gateway.ts 已有 listLorebooks_ACU() 函数可获取所有可用世界书列表
  - pipeline.ts 第 638 行有 getWorldbookNames_ACU() 封装了 listLorebooks_ACU()
  - 所有下游调用方已有 if (!primaryLorebookName) return; 的 null 保护

needs:
  - 在 getInjectionTargetLorebook_ACU 返回世界书名称后，验证该名称是否真实存在于 SillyTavern 的世界书列表中
  - 不存在时返回 null，让下游的 null 保护自动跳过操作
  - 同时输出警告日志，方便排查

key_challenges:
  - 防御点选择：在 getInjectionTargetLorebook_ACU 内部加验证是最佳位置，因为它是所有世界书操作的统一入口（19 处调用），改一处即可保护全部链路
  - listLorebooks_ACU 是异步调用，需要确认在 getInjectionTargetLorebook_ACU 中增加一次 await 不会引入性能问题——但这个函数本身已经是 async 的，且调用频率不高（仅在 CHAT_CHANGED 和手动操作时触发），可以接受
  - 需要同时处理两种情况：target === 'character' 时 API 返回不存在的世界书名，以及 target 是具体名称但该世界书已被删除

confidence: HIGH
  - 侦察信息充分：已读取所有相关文件的完整源码
  - 方案有明确工程依据：利用已有的 listLorebooks_ACU 验证，下游已有 null 保护
  - 边界条件已全部识别

approach:
  在 getInjectionTargetLorebook_ACU 函数内部，获取到世界书名称后，调用 listLorebooks_ACU 验证该名称是否存在于可用列表中。不存在则输出警告日志并返回 null。
  
  三维评分：
  - 可维护性: 5/5 — 只修改一个函数，改动集中在统一入口点，逻辑清晰易懂
  - 健壮性: 5/5 — 覆盖了两种失效场景（character 模式返回残留值、手动模式指定已删除世界书），下游 19 处调用全部自动受益于 null 保护
  - 可扩展性: 5/5 — 未来如果有新的世界书操作入口，只要经过 getInjectionTargetLorebook_ACU 就自动获得验证保护

edge_cases:
  - listLorebooks_ACU 本身调用失败（API 不可用）：返回空数组，验证会判定世界书不存在，返回 null——这是安全的降级行为
  - 世界书名称为 null/undefined/空字符串：现有逻辑已处理（gwGetCurrentCharPrimaryLorebook_ACU 返回 null 时直接返回 null）
  - 世界书名称存在但大小写不一致：SillyTavern 的 getLorebooks 返回的是精确名称，getCurrentCharPrimaryLorebook 也返回精确名称，不存在大小写不一致问题
  - 频繁调用导致性能问题：getInjectionTargetLorebook_ACU 仅在 CHAT_CHANGED 和手动操作时触发，listLorebooks 是轻量 API 调用，不会有性能问题

affected_scope:
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/worldbook/injection-engine-state.ts

execution_plan:
  - step_1: 在 injection-engine-state.ts 顶部添加 listLorebooks_ACU 的 import
  - step_2: 修改 getInjectionTargetLorebook_ACU 函数，在获取到世界书名称后，调用 listLorebooks_ACU 验证其是否存在于可用列表中，不存在则输出警告日志并返回 null

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan 是否覆盖 affected_scope 所有文件？ → YES
  - context 是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan 中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 在 getInjectionTargetLorebook_ACU 函数中添加世界书存在性验证防御，防止 SillyTavern API 返回不存在的世界书名称导致报错。
deliverables:
  - 修改: /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/worldbook/injection-engine-state.ts

metrics:
  total_files_modified: 1
  execution_plan_coverage: 2/2 = 100%
  edge_cases_handled: 4/4 = 100%（API 不可用降级、名称为空、名称不存在、验证异常）
  confidence_assessment: HIGH — 所有产物经过验证，无已知遗漏

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。修改的核心逻辑是：获取世界书名称后调用 listLorebooks_ACU 验证存在性，不存在则返回 null。这直接解决了用户报告的"进任何角色卡都报错找不到世界书 XXX"的问题。如果删除这段代码，SillyTavern 返回不存在的世界书名时仍会报错。
  - 产物是否能被目标对象的变化所"击穿"？
    → YES。如果 SillyTavern API 返回一个不存在的世界书名称，验证逻辑会检测到并返回 null，下游 19 处调用方的 null 保护会自动跳过操作。
  - 实质性比率: 1/1 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。只需修改 getInjectionTargetLorebook_ACU 一个函数，因为它是所有世界书操作的统一入口。下游 19 处调用方已有 if (!primaryLorebookName) return; 的 null 保护，无需修改。
  - 产物覆盖的范围是否与 affected_scope 完全一致？
    → YES。affected_scope 只有 injection-engine-state.ts，已修改。
  - 核心业务逻辑是否都有直接验证？
    → YES。核心逻辑是 listLorebooks_ACU 返回的列表中不包含目标名称时返回 null。已通过代码审查确认逻辑正确。
  - affected_scope 覆盖率: 1/1 = 100%

value_density_check:
  - 高价值:低价值 = 1:0，高价值占比 100%。唯一的修改直接解决用户报告的问题。
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 用户的本质需求是：防止 SillyTavern API 返回不存在的世界书名称导致报错。产物通过在统一入口点添加存在性验证，完全满足此需求。
  - "如果这是别人交给我的，我会接受吗？" → YES。改动精准、最小化、防御性强，不影响正常功能。
</output_quality_review>