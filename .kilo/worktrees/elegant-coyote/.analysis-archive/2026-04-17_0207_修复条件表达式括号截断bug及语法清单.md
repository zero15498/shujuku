<analysis>
context:
  1. seed-condition.ts（451行）的 evaluateCondExpression_ACU 使用递归下降解析器，parsePrimary 函数在第 413-420 行处理括号：
     - 第 413 行：if (expr[pos] === '(') → 进入括号分组逻辑
     - 第 421-424 行：子条件文本提取循环的终止条件包含 '(' 和 ')'
     - 这意味着 db:背包物品表.where('物品名称', '铁剑').count() > 0 中的 () 会被当作分组括号，导致子条件在 ( 处被截断
  2. 受影响的前缀：db: 和 sql:
     - db: 前缀的表达式可能包含 ORM 函数调用括号：count()、exists()、get('列名')、where('列名', '值')、sum('列名')、first()、all()、list('列名')
     - sql: 前缀的表达式可能包含 SQL 函数括号：COUNT(*)、SUM(列名)、AVG(列名)、MAX(列名)、MIN(列名)
  3. 不受影响的前缀：seed:、cell:、random:、calc:、max:、min:
     - 这些前缀的表达式格式不包含括号
  4. evaluateCondExpression_ACU 的调用方只有 2 处（均在 if-block-parser.ts）：
     - 第 10 行：import
     - 第 130 行：<if cond="..."> 标签的条件求值
  5. <if> 标签支持 5 种属性类型：seed、cell、cond、db、sql
     - <if db="..."> 和 <if sql="..."> 直接调用 evaluateDbCondition/evaluateSqlCondition，不受此 bug 影响
     - <if cond="db:..."> 经过 evaluateCondExpression_ACU，受此 bug 影响

needs:
  - 修复 evaluateCondExpression_ACU 的 parsePrimary 函数，使 db: 和 sql: 前缀的子条件能正确包含函数调用括号
  - 更新测试文件，验证修复后 db: 和 sql: 前缀能正确处理包含括号的表达式
  - 输出完整的语法清单文件

key_challenges:
  1. 核心难点：如何区分"逻辑分组括号"和"函数调用括号"
  2. 方案：在子条件文本提取循环中跟踪括号深度，深度 > 0 时括号是子条件文本的一部分
  3. 逻辑分组括号在 parsePrimary 开头被处理（if (expr[pos] === '(')），不进入子条件提取循环

confidence: HIGH

approach:
  在 parsePrimary 的子条件文本提取循环中增加括号深度跟踪。遇到 ( 时 depth++，遇到 ) 时如果 depth > 0 则 depth--，只有 depth == 0 时 ( 和 ) 才终止提取。
  
  三维评分：
  - 可维护性: 5/5 — 只修改约 10 行代码，逻辑清晰
  - 健壮性: 5/5 — 括号深度跟踪是标准解析器技术
  - 可扩展性: 5/5 — 未来新增前缀自动受益

edge_cases:
  - db:背包物品表.count() > 0 — 简单的 ORM 函数调用括号
  - db:背包物品表.where('物品名称', '铁剑').get('数量') — 嵌套括号，逗号在括号内不终止提取
  - db:背包物品表.where('物品名称', '铁剑').exists() — 多层括号嵌套
  - sql:SELECT COUNT(*) FROM inventory WHERE quantity > 0 — SQL 函数括号
  - sql:SELECT SUM(quantity) FROM inventory — SQL 聚合函数括号
  - (db:背包物品表.count() > 0 & seed:战斗), sql:SELECT 1 — 逻辑分组括号和函数调用括号混合
  - db:背包物品表.count() > 0 & db:重要人物表.count() > 0 — 多个 db: 条件用 & 连接
  - !db:背包物品表.where('物品名称', '不存在').exists() — 取反 + 函数调用括号

affected_scope:
  - src/service/runtime/template-vars/seed-condition.ts
  - tests/service/runtime/template-vars/seed-condition.test.ts
  - tests/service/runtime/template-vars/if-block-and-condition.test.ts
  - docs/syntax-reference.md

execution_plan:
  - step_1: 修改 src/service/runtime/template-vars/seed-condition.ts 的 parsePrimary 函数，在子条件文本提取循环中增加括号深度跟踪
  - step_2: 修改 tests/service/runtime/template-vars/seed-condition.test.ts，更新 db: 前缀测试为包含括号的表达式，新增复杂括号测试
  - step_3: 修改 tests/service/runtime/template-vars/if-block-and-condition.test.ts，新增 db:/sql: 前缀包含括号的测试
  - step_4: 运行测试验证修复正确
  - step_5: 运行全量测试确保无回归
  - step_6: 创建 docs/syntax-reference.md，输出完整语法清单

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否省略了已知边界条件？ → NO
  - 是否因改动量大而想简化？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>
---
<output_quality_review>
task_summary: 修复 evaluateCondExpression_ACU 解析器中 db:/sql: 前缀的函数调用括号被截断的 bug，更新相关测试，并创建完整的语法参考文档。
deliverables:
  - src/service/runtime/template-vars/seed-condition.ts（修改，parsePrimary 函数的括号处理逻辑）
  - tests/service/runtime/template-vars/seed-condition.test.ts（修改，db:/sql: 前缀测试更新为包含括号的表达式）
  - tests/service/runtime/template-vars/if-block-and-condition.test.ts（修改，新增包含括号的 db:/sql: 前缀测试）
  - docs/syntax-reference.md（新建，完整语法参考文档）

metrics:
  total_files_modified: 4
  execution_plan_coverage: 6/6 = 100%
  edge_cases_handled: 8/8 = 100%
  confidence_assessment: HIGH

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被目标对象的变化所"击穿"？ → YES
  - 实质性比率: 4/4 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - 核心业务逻辑是否都有直接验证？ → YES
  - affected_scope 覆盖率: 4/4 = 100%

value_density_check:
  - 高价值:低价值 = 全部高价值
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 用户本质需求：修复括号截断 bug + 输出完整语法清单。两个需求都已满足。
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>
