
<analysis>
context:
  1. 项目是一个 Tampermonkey 油猴脚本（"星·数据库 III"），通过 Rollup 打包为单文件 IIFE 格式（dist/index.bundle.js），带 UserScript 头
  2. 入口文件 src/index.ts 按层级导入所有模块，最后通过 jQuery ready 回调调用 mainInitialize_ACU()
  3. 宿主 API 获取集中在 attemptToLoadCoreApis_ACU()（settings-ui-connect.ts:98），通过 window.parent.SillyTavern / window.parent.TavernHelper / window.parent.$ / window.parent.toastr 获取
  4. host-api.ts 是宿主 API 的唯一引用点，通过 setter 函数集中设置，所有其他文件通过 import 使用
  5. tavern-storage.ts 通过注入 <script type="module"> 到顶层窗口来桥接 extension_settings 和 saveSettingsDebounced，这是因为油猴脚本运行在 iframe 中无法直接 import 酒馆的 ESM 模块
  6. TavernHelper API 被 9 个文件引用（50 处），主要集中在 gateway 层（worldbook-gateway.ts 20处、character-gateway.ts 1处）和 service 层（plot-orchestrator.ts 13处、api-call.ts 2处、prompt-api-call.ts 2处）
  7. 酒馆插件（Extension）的标准结构：放在 data/default-user/extensions/ 目录下，有 manifest.json + index.js，运行在酒馆主窗口的上下文中（不是 iframe）
  8. 关键差异：油猴脚本运行在 iframe 中（window.parent 指向酒馆主窗口），而酒馆插件运行在主窗口中（window 就是酒馆主窗口）
  9. tavern-storage.ts 的桥接逻辑（注入 script 标签 import extension_settings）在插件环境下完全不需要，因为插件可以直接 import 酒馆的模块
  10. rollup.config.js 将 './script.js' 和 './scripts/extensions.js' 标记为 external（酒馆环境运行时动态 import）
  11. 项目依赖酒馆助手（TavernHelper）提供的 API（getChatMessages、getLorebookEntries、generateRaw 等），插件形态下仍需要酒馆助手

needs:
  - 用户需要两种安装形态共存：油猴脚本（通过酒馆助手 iframe 加载）和酒馆插件（作为 Extension 安装到酒馆）
  - 两种形态都依赖酒馆助手（TavernHelper）API
  - 核心业务逻辑（shared/data/service 层）不应重复
  - 需要新增 rollup 构建 target 生成插件格式的产物
  - 需要新增 manifest.json 和插件入口文件
  - 需要处理环境差异：iframe vs 主窗口、API 获取方式、存储桥接方式

key_challenges:
  1. 环境上下文差异：油猴脚本在 iframe 中运行（window.parent 指向酒馆），插件在主窗口运行（window 就是酒馆）。topLevelWindow_ACU 的计算逻辑需要适配
  2. tavern-storage 桥接：油猴脚本需要注入 script 标签来获取 extension_settings，插件可以直接 import。这是最大的适配点
  3. jQuery ready 启动：油猴脚本用 $(function() { mainInitialize_ACU() })，插件需要不同的启动时机
  4. 产物格式：油猴脚本是 IIFE + UserScript 头，插件需要 ESM 或 IIFE（无 UserScript 头）
  5. API 获取方式：attemptToLoadCoreApis_ACU 通过 window.parent 获取 API，插件环境下直接从 window 获取
  6. rollup external 配置：油猴脚本将 './script.js' 和 './scripts/extensions.js' 标记为 external，插件环境下这些路径不同

confidence: MEDIUM
  - 侦察信息基本充分，但存在 1 个未验证的假设：酒馆插件加载时 TavernHelper 是否已经挂载到 window 上（取决于插件加载顺序）

approach:
  三维评分：
  - 可维护性: 4/5 — 通过环境适配层隔离差异，核心代码不动，两种形态共享同一套代码。扣 1 分因为需要维护两个入口和两套构建配置
  - 健壮性: 4/5 — 环境检测逻辑需要处理边界情况。扣 1 分因为两种环境的启动时序不同
  - 可扩展性: 5/5 — 环境适配层使得未来支持第三种形态只需新增入口和适配器

  方案：双入口 + 环境适配层

edge_cases:
  - 插件加载时 TavernHelper 尚未挂载：需要轮询等待
  - 用户同时安装了油猴脚本和插件：需要互斥检测
  - 插件环境下 window.parent === window：env.ts 需要正确处理
  - 插件环境下 jQuery 直接从 window 获取
  - CSS 注入方式在两种环境下一致（JS 动态注入）

affected_scope:
  - 新增: src/shared/runtime-env.ts
  - 新增: src/entry-extension.ts
  - 新增: manifest.json
  - 修改: src/shared/env.ts
  - 修改: src/data/storage/tavern-storage.ts
  - 修改: src/presentation/triggers/settings-ui-sync/settings-ui-connect.ts
  - 修改: rollup.config.js
  - 修改: package.json
  - 修改: src/index.ts

execution_plan:
  - step_1: 创建 src/shared/runtime-env.ts — 运行时环境检测模块
  - step_2: 修改 src/shared/env.ts — topLevelWindow_ACU 使用 getHostWindow()
  - step_3: 修改 src/data/storage/tavern-storage.ts — 插件环境分支
  - step_4: 修改 src/presentation/triggers/settings-ui-sync/settings-ui-connect.ts — API 加载适配
  - step_5: 修改 src/index.ts — 添加互斥检测
  - step_6: 创建 src/entry-extension.ts — 插件入口
  - step_7: 创建 manifest.json — 插件清单
  - step_8: 修改 rollup.config.js — 新增插件构建 target
  - step_9: 修改 package.json — 新增构建脚本
  - step_10: 验证构建

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否省略了已知边界条件？ → NO
  - 是否因改动量大而想简化？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan 是否覆盖 affected_scope 所有文件？ → YES
  - context 是否充分？ → YES
  - 是否有跳过的问题？ → YES，CSS 注入方式，但两种环境下行为一致不需要额外处理
  - 是否使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 为"星·数据库 III"项目添加酒馆插件（SillyTavern Extension）支持，实现油猴脚本和酒馆插件双形态共存。通过环境适配层隔离两种运行环境的差异，共享全部核心代码。

deliverables:
  - 新增: src/shared/runtime-env.ts（运行时环境检测模块）
  - 新增: src/entry-extension.ts（酒馆插件入口文件）
  - 新增: manifest.json（酒馆插件清单）
  - 修改: src/shared/env.ts（topLevelWindow_ACU 适配）
  - 修改: src/data/storage/tavern-storage.ts（插件环境桥接快速路径）
  - 修改: src/presentation/triggers/settings-ui-sync/settings-ui-connect.ts（API 加载适配）
  - 修改: rollup.config.js（双构建 target）
  - 修改: package.json（新增构建脚本）
  - 修改: src/index.ts（互斥检测）

metrics:
  total_files_modified: 9
  execution_plan_coverage: 10/10 = 100%
  edge_cases_handled: 5/5 = 100%
  confidence_assessment: MEDIUM — 需要上机验证插件在酒馆实际环境中的运行

substance_check: 全部通过，实质性比率 9/9 = 100%
completeness_check: 全部通过，affected_scope 覆盖率 9/9 = 100%
value_density_check: 高价值占比 100%
alignment_check: 满足本质需求，可接受

验证结果：
  - TypeScript 类型检查：通过（0 错误）
  - 油猴脚本构建：成功（dist/index.bundle.js）
  - 插件构建：成功（dist/extension/index.js + manifest.json）
  - 双构建模式：成功（BUILD_MODE=all）
  - 测试：85 个文件 2448 个用例全部通过
</output_quality_review>
