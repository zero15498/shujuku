<analysis>
context:
  已确认以下关键代码事实：
  
  1. **错误 UI 入口**: `main-popup-plot.ts:159-164` 在一个两列 grid 布局中放了"任务剧情注入占位符"输入框（左侧列），右侧列是"任务数据库API预设"下拉框。需要删除左侧列，保留右侧列。由于使用 `repeat(auto-fit, minmax(220px, 1fr))` 布局，删除一个 div 后另一个 div 会自然填满。
  
  2. **错误绑定**: `popup-bindings-plot.ts:148-160` 在一个选择器数组中注册了 `plot-injection-placeholder` 的 `input change` 事件绑定。只需从数组中移除那一行即可。
  
  3. **错误保存/加载链路**: `plot-editors.ts` 有4处引用：
     - L305: 默认清空 `plot-injection-placeholder`
     - L318: 从任务对象加载到 UI
     - L335: 从 UI 取值
     - L344: 写入 normalizePlotTask_ACU 调用参数
  
  4. **错误模型字段**: `plot-logic.ts:157-158` 在 `normalizePlotTask_ACU()` 返回结构中定义了 `plotInjectionPlaceholder` 和 `plotInjectionContext`。
  
  5. **错误运行时分支**: `plot-task-engine.ts:300-321` 在 `executeSinglePlotTask_ACU()` 中根据 `plotInjectionPlaceholder` 非空来决定是否按任务维度读取历史。这段需要删除。
  
  6. **世界书触发问题核心**: `plot-task-engine.ts:113-116` 在共享上下文初始化阶段，把整段 `lastPlotContent` 传给 `getWorldbookContentForPlot_ACU()` 作为 `extraBaseText`，导致所有任务的世界书触发都固定包含上一轮整段剧情内容。
  
  7. **{{tag}} 注入链路**: `plot-tag-utils.ts` 已经有完整的工具函数：
     - `getPlotPlaceholderTagNames_ACU(text)` — 从文本提取 `{{tag}}` 名称
     - `buildPlotTagMapFromText_ACU(text, requestedTagNames)` — 从源文本按 tag 名提取 XML 标签内容
     - `buildPlotTagBlock_ACU(tagName, contents)` — 构造标签块文本
     - `replacePlotTagPlaceholders_ACU(text, tagSourceMap)` — 替换文本中的 `{{tag}}`
  
  8. **世界书管线**: `pipeline.ts:753-889` 的 `buildCombinedWorldbookContentByStrategy_ACU()` 接受 `baseScanText` 参数做关键词匹配。当前 `getWorldbookContentForPlot_ACU()` 将 `historyAndUserText + extraBaseText` 拼成 `baseScanText`。`historyAndUserText` 来自 `recentMessages + userMessage`。
  
  9. **任务渲染链路**: `renderPlotTaskMessages_ACU()` 在 `executeSinglePlotTask_ACU()` 中被调用，此时已经可以拿到任务的 promptGroup 和 taskPlotContent，能提取出当前任务实际使用的 `{{tag}}`。
  
  10. **sharedContext 的 performReplacements 已经固化了 worldbookContent**: 在 `buildPlotSharedContext_ACU()` L209-225 中，`performReplacements` 闭包捕获了预计算的 `worldbookContent`。如果要做任务级世界书，需要让每个任务有独立的 `$1` 替换值。

needs:
  1. 删除之前误加的任务剧情注入占位符配置的全部 UI/绑定/保存/模型/运行时链路
  2. 将世界书触发从"共享预计算 + 固定 lastPlotContent"改为"任务级按需计算，基于当前任务实际 {{tag}} 注入内容 + 本轮上下文"
  3. 保证 prompt 注入与世界书触发共用同一份标签解析结果

key_challenges:
  - `performReplacements` 闭包捕获了预计算的 `worldbookContent`。要做任务级世界书，需要在任务执行时重新计算 `$1` 的值，而不是在共享上下文阶段固化。
  - 不能破坏共享上下文中其他占位符（$5/$6/$7/$8/$U/$C）的替换逻辑。
  - 需要在 `executeSinglePlotTask_ACU` 中，调用 `renderPlotTaskMessages_ACU` 之前或其中，按任务计算世界书内容，并让 `performReplacements` 中的 `$1` 使用任务级结果。
  - `getWorldbookContentForPlot_ACU` 是 async 函数，调用时需要 await。

confidence: HIGH
  - 所有需要修改的文件和位置已精确定位
  - 现有工具函数已足够支撑新逻辑，不需要重写管线
  - 边界条件已识别

approach:
  三维评分：
  - 可维护性: 4/5 — 删除错误字段后模型更干净；新增一个纯函数构造世界书触发文本，职责明确。唯一扣分在于 `executeSinglePlotTask_ACU` 内部逻辑会增加，但可以通过函数拆分控制。
  - 健壮性: 4/5 — 每个任务独立计算世界书，互不干扰。无 {{tag}} 时只用本轮上下文，不会隐式回退到整段 $6。边界条件清晰。扣分在于 async 调用链可能引入竞态，但当前架构是串行任务执行，风险低。
  - 可扩展性: 4/5 — 新逻辑基于现有 {{tag}} 系统，不需要修改世界书管线本身。未来如果要加更多触发源，只需修改触发文本构造函数。

edge_cases:
  - 任务 prompt 中没有 {{tag}} 时：世界书触发文本应为空，只靠 historyAndUserText 触发
  - 任务 prompt 中有 {{tag}} 但历史剧情中没有对应 XML 标签时：该 tag 不产生触发文本，不回退到整段 $6
  - 多个 prompt segment 各有不同的 {{tag}} 时：应汇总所有 segment 的 tag
  - getWorldbookContentForPlot_ACU 调用失败时：应 graceful fallback 到空字符串，不阻断任务执行
  - 多任务并行（Promise.all）：每个任务独立计算世界书，互不影响

affected_scope:
  - src/presentation/pages/main-popup-plot.ts（删除 UI）
  - src/presentation/pages/popup-bindings-plot.ts（删除绑定）
  - src/presentation/components/plot-editors.ts（删除保存/加载）
  - src/service/plot/plot-logic.ts（删除模型字段）
  - src/service/runtime/plot-runtime/plot-task-engine.ts（删除旧分支 + 改造世界书触发）
  - src/service/runtime/plot-runtime/plot-tag-utils.ts（新增工具函数）

execution_plan:
  - step_1: 删除 main-popup-plot.ts L159-164 的"任务剧情注入占位符"输入框 div 块，调整 grid 布局使"任务数据库API预设"不再被挤在一半宽度
  - step_2: 删除 popup-bindings-plot.ts L155 的 `#${SCRIPT_ID_PREFIX_ACU}-plot-injection-placeholder` 绑定选择器
  - step_3: 删除 plot-editors.ts 中 4 处引用（L305 默认清空、L318 加载到UI、L335 UI取值、L344 保存映射）
  - step_4: 从 plot-logic.ts normalizePlotTask_ACU() 返回结构中移除 plotInjectionPlaceholder 和 plotInjectionContext 两个字段
  - step_5: 在 plot-tag-utils.ts 新增 buildTaskWorldbookTriggerText_ACU(taskPromptGroup, plotContent) 函数
  - step_6: 改造 plot-task-engine.ts：
    - 在 buildPlotSharedContext_ACU 中，将 worldbookContent 预计算改为空字符串（延迟到任务级计算）
    - 在 executeSinglePlotTask_ACU 中删除旧 plotInjectionPlaceholder/plotInjectionContext 分支
    - 在 executeSinglePlotTask_ACU 中新增任务级世界书计算逻辑：先提取 {{tag}}，构造触发文本，调用 getWorldbookContentForPlot_ACU，用结果覆盖 performReplacements 中的 $1
  - step_7: 运行构建验证无编译错误

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES，可维护性4/5、健壮性4/5、可扩展性4/5。任务级世界书计算是唯一满足需求的方案，不破坏现有管线，工具函数可复用。
  - 是否遗漏了已知边界条件？ → NO，已列出 5 个边界条件。
  - 是否因改动量大而想缩减方案？ → NO，按完整计划执行。
  - 是否打算跳过某些文件？ → NO，affected_scope 全部覆盖。
  - execution_plan是否覆盖affected_scope所有文件？ → YES，7 个步骤对应 6 个文件 + 构建验证。
  - context是否充分？是否有未读但可能相关的文件？ → YES，所有关键文件已读取，世界书管线 pipeline.ts 的 baseScanText 构造逻辑已确认。不需要再补充侦察。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO，所有发现的问题都已纳入计划。
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO，所有源代码修改使用 edit 工具。
</analysis>
---
<output_quality_review>
task_summary: 删除了之前误加的"任务剧情注入占位符"错误配置的全部 UI/绑定/保存/模型/运行时链路，并将剧情推进的变量世界书触发逻辑从"共享预计算 + 固定吃整段 $6"改造为"任务级按需计算，基于当前任务实际 {{tag}} 注入内容 + 本轮上下文触发"。
deliverables:
  - src/presentation/pages/main-popup-plot.ts — 删除了"任务剧情注入占位符"输入框 div，调整了布局结构
  - src/presentation/pages/popup-bindings-plot.ts — 删除了 plot-injection-placeholder 选择器绑定
  - src/presentation/components/plot-editors.ts — 删除了 4 处错误字段的 UI 读写/保存映射
  - src/service/plot/plot-logic.ts — 从 normalizePlotTask_ACU() 中移除了 plotInjectionPlaceholder / plotInjectionContext
  - src/service/runtime/plot-runtime/plot-tag-utils.ts — 新增了 buildTaskWorldbookTriggerText_ACU() 函数
  - src/service/runtime/plot-runtime/plot-task-engine.ts — 核心改造：共享世界书预计算延迟到任务级、performReplacements 支持 $1 覆盖、executeSinglePlotTask_ACU 新增任务级世界书逻辑

metrics:
  total_files_modified: 6
  execution_plan_coverage: 7/7 = 100%
  edge_cases_handled: 5/5 = 100%
  confidence_assessment: HIGH — TypeScript 编译零错误，rollup 双入口构建通过，无残留引用

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO。每个产物都有实质功能变更。
  - 产物是否能被其目标对象的变化所"击穿"？ → YES。如果故意改坏 buildTaskWorldbookTriggerText_ACU 或 performReplacements 的 taskOverrides 逻辑，对应的世界书触发行为会直接变化。
  - 实质性比率: 6/6 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO。所有计划内的修改点都已执行。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？ → YES。6 个文件全部覆盖。
  - 核心业务逻辑是否都有直接验证？ → YES。构建验证通过。
  - affected_scope 覆盖率: 6/6 = 100%

value_density_check:
  - 高价值:低价值 = 6:0 = 100% 高价值。
  - 是否存在"用数量掩盖质量"的模式？ → NO。

alignment_check:
  - 产物满足的是用户的本质需求：变量世界书触发不再固定吃整段 $6，而是按任务实际 {{tag}} 注入内容 + 本轮上下文触发。
  - "如果这是别人交给我的，我会接受吗？" → YES。改动范围精确，不破坏现有管线，工具函数职责清晰，编译构建通过。
</output_quality_review>
