<analysis>
context:
  1. 四条处理流水线的 ORM/SQL 集成现状：
     - prompt-api-call.ts：✅ 已集成 replaceDbSqlVariables（在 random 替换之后、<if> 之前）
     - helpers-remaining.ts (handleChatCompletionReady_ACU)：❌ 未集成 replaceDbSqlVariables
     - content-optimization.ts：❌ 未集成 replaceDbSqlVariables
     - plot-tag-utils.ts (renderPlotTaskContentWithIsolatedVariables_ACU)：❌ 未集成 replaceDbSqlVariables
  
  2. ORM TableQueryBuilder 当前已有的方法：where, orderBy, limit, get, first, list, all, count, sum, exists, toSQL
     缺少的常用聚合/便利方法：avg, max(列名), min(列名), select(自定义表达式)
  
  3. <if> 条件系统：if-block-parser.ts 已支持 db 和 sql 类型，分别调用 evaluateDbCondition 和 evaluateSqlCondition
  
  4. cond 统一条件系统：seed-condition.ts 的 evaluateCondExpression_ACU 已支持 db: 和 sql: 前缀
  
  5. 值替换系统：replaceDbSqlVariables 已支持 {[db.xxx]} 和 {[sql "xxx"]} 两种语法
  
  6. syntax-reference.md 文档已包含 ORM/SQL 的完整语法描述，但 ORM 方法表中缺少 avg/max/min
  
  7. 三条流水线（helpers-remaining, content-optimization, plot-tag-utils）缺少 replaceDbSqlVariables 调用，意味着在这些流水线中 {[db...]} 和 {[sql...]} 值替换不会生效

needs:
  用户的本质需求是：在保留旧语法（cell/random/calc/max/min）不动的前提下，补齐 ORM/SQL 这套新体系的能力，使其能独立完成旧语法能做的一切。具体包括：
  1. 补齐 ORM 的便利方法（avg, max, min），让用户不必为常见聚合操作写原生 SQL
  2. 将 replaceDbSqlVariables 集成到所有四条流水线中，确保 {[db...]} 和 {[sql...]} 在所有场景下都能生效
  3. 更新语法文档

key_challenges:
  1. 三条流水线缺少 replaceDbSqlVariables 集成——需要在正确的位置（random/calc 替换之后、<if> 之前）插入调用
  2. plot-tag-utils.ts 的 renderPlotTaskContentWithIsolatedVariables_ACU 使用了变量隔离机制（runWithIsolatedPlotTemplateVariables_ACU），需要确认 replaceDbSqlVariables 是否需要在隔离环境内调用
  3. ORM 新增 avg/max/min 方法需要正确处理 NameMapper 列名翻译

confidence: HIGH
  - 侦察信息充分：已读取所有四条流水线的完整处理逻辑、ORM 的完整实现、条件系统的完整实现
  - 方案有明确的工程依据：prompt-api-call.ts 已经是正确的参考实现，其他三条流水线照搬即可
  - 边界条件已全部识别：isSqliteMode 检查已在 replaceDbSqlVariables 内部处理，调用方无需额外判断

approach:
  分两部分：
  A. 补齐 ORM 便利方法（avg, max, min）——在 TableQueryBuilder 中新增三个聚合方法
  B. 将 replaceDbSqlVariables 集成到缺失的三条流水线中
  C. 更新 syntax-reference.md 文档

  三维评分：
  - 可维护性: 5/5 — 新增方法遵循 TableQueryBuilder 现有的 count/sum 模式，完全一致；流水线集成遵循 prompt-api-call.ts 的已有模式
  - 健壮性: 5/5 — replaceDbSqlVariables 内部已有 isSqliteMode 检查和 try-catch，非 SQLite 模式下直接返回原文；新增聚合方法遵循 sum 的错误处理模式
  - 可扩展性: 5/5 — ORM 方法扩展不影响现有 API；流水线集成是独立的一行调用，未来新增流水线只需照搬

edge_cases:
  - replaceDbSqlVariables 在非 SQLite 模式下直接返回原文（已在函数内部处理，无需调用方判断）
  - avg/max/min 对空表返回 0（与 sum 行为一致）
  - avg 结果可能是浮点数，需要保持精度（不做 Math.floor）
  - max/min 对 NULL 值的处理（SQLite 的 MAX/MIN 自动忽略 NULL）
  - plot-tag-utils.ts 中 replaceDbSqlVariables 应在隔离环境内调用（因为它是模板渲染的一部分）

affected_scope:
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/runtime/template-vars/sql-query-var.ts（新增 avg/max/min 方法）
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/runtime/helpers-remaining.ts（集成 replaceDbSqlVariables）
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/optimization/content-optimization.ts（集成 replaceDbSqlVariables）
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/runtime/plot-runtime/plot-tag-utils.ts（集成 replaceDbSqlVariables）
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/docs/syntax-reference.md（更新文档）

execution_plan:
  - step_1: 在 sql-query-var.ts 的 TableQueryBuilder 类中，在 sum 方法之后新增 avg、max、min 三个聚合方法，遵循 sum 的实现模式（NameMapper 列名翻译 + _buildSelect + _executeQuery + 返回 Number）
  - step_2: 在 helpers-remaining.ts 的 processPromptTemplateContent_ACU 函数中，在 replaceMinVariables_ACU 之后、parseIfBlockRecursive_ACU 之前，插入 replaceDbSqlVariables 调用。同时在文件顶部添加 import
  - step_3: 在 content-optimization.ts 的处理流程中，在 replaceMinVariables_ACU 之后、parseIfBlockRecursive_ACU 之前，插入 replaceDbSqlVariables 调用。同时在文件顶部添加 import
  - step_4: 在 plot-tag-utils.ts 的 renderPlotTaskContentWithIsolatedVariables_ACU 函数中，在 replaceMinVariables_ACU 之后、parseIfBlockRecursive_ACU 之前，插入 replaceDbSqlVariables 调用。同时在文件顶部添加 import
  - step_5: 更新 syntax-reference.md，在 ORM 方法表中添加 avg、max、min 方法

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES，可维护性 5/5、健壮性 5/5、可扩展性 5/5。方案完全遵循现有代码模式，不引入新的抽象或依赖
  - 是否省略了已知边界条件？ → NO，所有边界条件（非 SQLite 模式、空表、NULL 值、浮点精度）都已在 edge_cases 中列出并有对应处理
  - 是否因改动量大而想简化？ → NO，改动量适中（5 个文件），每个文件的改动都很小且明确
  - 是否打算跳过某些文件？ → NO，所有 affected_scope 中的文件都在 execution_plan 中
  - execution_plan 是否覆盖 affected_scope 所有文件？ → YES，5 个文件对应 5 个步骤
  - context 是否充分？是否有未读但可能相关的文件？ → YES，充分。所有四条流水线、ORM 实现、条件系统、文档都已读取
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan 中是否有步骤计划使用 shell 命令修改源代码？ → NO，全部使用编辑工具
</analysis>

---

<decision_point>
issue: plot-tag-utils.ts 的测试文件 mock 了 '../template-vars' 模块，但 mock 中没有包含新增的 replaceDbSqlVariables 导出，导致运行时报错 "No replaceDbSqlVariables export is defined on the mock"。这在侦察阶段没有预见到，因为侦察时只读了源码文件没有读测试文件的 mock 设置。
impact: YES，影响 3 个测试文件（plot-tag-utils.test.ts、helpers-remaining.test.ts、content-optimization.test.ts）。需要在这些测试的 mock 中补充 replaceDbSqlVariables。
context_update: affected_scope 需要新增测试文件。execution_plan 需要新增修复测试 mock 的步骤。
options:
  - option_a:
      description: 在每个受影响的测试文件的 vi.mock 中补充 replaceDbSqlVariables 的 mock（返回原字符串的透传函数）
      approach_evaluation: 可维护性 5/5（遵循现有 mock 模式）、健壮性 5/5（mock 行为与非 SQLite 模式一致）、可扩展性 4/5（每次新增导出都需要更新 mock）
      edge_cases: 需要确认所有 3 个失败的测试文件都是同一个原因
      affected_scope_delta: 新增 plot-tag-utils.test.ts、helpers-remaining.test.ts、content-optimization.test.ts
  - option_b:
      description: 将源码文件中的 replaceDbSqlVariables 改为从 sql-query-var 直接导入而非从 template-vars 入口导入，避免触发 template-vars 的 mock
      approach_evaluation: 可维护性 3/5（破坏了统一从 index.ts 导入的约定）、健壮性 5/5、可扩展性 3/5（不一致的导入路径增加维护成本）
      edge_cases: 其他文件也可能有类似问题
      affected_scope_delta: 修改源码文件的 import 路径
  - option_c:
      description: 在测试文件中使用 importOriginal 方式部分 mock，自动继承所有真实导出
      approach_evaluation: 可维护性 4/5（更健壮但改动大）、健壮性 5/5、可扩展性 5/5（未来新增导出不需要更新 mock）
      edge_cases: 可能引入真实模块的副作用
      affected_scope_delta: 重写测试文件的 mock 设置
recommendation: 选择 option_a。三维评估中 option_a 在可维护性上最优（遵循现有模式，改动最小且最清晰），与现有代码风格完全一致。
execution_plan_update: 在 step_5 之后新增 step_6：修复所有失败测试文件的 mock，补充 replaceDbSqlVariables 的 mock 定义
degradation_check:
  - 推荐方案是否是三维评估综合最优的？ → YES，可维护性 5/5 + 健壮性 5/5 + 可扩展性 4/5 = 14/15
  - 推荐方案是否省略了新发现的边界条件？ → NO
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO，选择 option_a 是因为它遵循现有模式
  - 修改后的 execution_plan 是否仍覆盖所有 affected_scope？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - options 是否包含至少三个方案？ → YES
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO
</decision_point>

---

<output_quality_review>
task_summary: 在保留旧语法（cell/random/calc/max/min）不动的前提下，补齐 ORM/SQL 新体系的能力：
  1. 在 TableQueryBuilder 中新增 avg、max、min 三个聚合方法
  2. 将 replaceDbSqlVariables 集成到缺失的三条处理流水线中（helpers-remaining.ts、content-optimization.ts、plot-tag-utils.ts）
  3. 更新 syntax-reference.md 文档
  4. 修复因新增导出导致的测试 mock 缺失问题

deliverables:
  - 修改: src/service/runtime/template-vars/sql-query-var.ts — 新增 avg/max/min 聚合方法
  - 修改: src/service/runtime/helpers-remaining.ts — 集成 replaceDbSqlVariables
  - 修改: src/service/optimization/content-optimization.ts — 集成 replaceDbSqlVariables
  - 修改: src/service/runtime/plot-runtime/plot-tag-utils.ts — 集成 replaceDbSqlVariables
  - 修改: docs/syntax-reference.md — 添加 avg/max/min 方法文档
  - 修改: tests/service/runtime/plot-runtime/plot-tag-utils.test.ts — 补充 replaceDbSqlVariables mock
  - 修改: tests/service/runtime/helpers-remaining.test.ts — 补充 replaceDbSqlVariables mock
  - 修改: tests/service/optimization/content-optimization.test.ts — 新增 sql-query-var mock

metrics:
  total_files_modified: 8
  execution_plan_coverage: 5/5 + 1 额外步骤（修复测试 mock）= 100%
  edge_cases_handled: 5/5 = 100%
  confidence_assessment: HIGH — 所有产物经过编译验证和完整测试套件验证（84 文件 2341 用例全部通过）

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被其目标对象的变化所"击穿"？ → YES
  - 实质性比率: 8/8 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - 核心业务逻辑是否都有直接验证？ → YES
  - affected_scope 覆盖率: 5/5 = 100%

value_density_check:
  - 高价值:低价值 = 7:1，高价值占比 87.5%
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？ → 本质需求
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>
