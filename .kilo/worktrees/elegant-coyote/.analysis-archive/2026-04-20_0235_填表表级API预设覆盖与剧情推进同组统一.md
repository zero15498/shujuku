<analysis>
context: 实施阶段确认了以下关键事实：1）settings-service.ts 和 state-manager.ts 已包含 tableApiPresetOverridesByName 默认值（第 339 行和第 136 行）；2）update-orchestrator.ts 已存在完整的 group effective preset 决议链路：normalizeTableNameForPresetLookup_ACU（第 20 行）、resolveTableApiPresetOverride_ACU（第 29 行）、手动更新 group 循环中的决议（第 671-687 行）、processUpdatesBatch_ACU 中的 fallback（第 509-519 行）；3）prompt-api-call.ts 已支持 options.tableApiPreset 覆盖（第 32-34 行）；4）template-assistant/service.ts 已实现按表名查找覆盖映射（第 411-420 行）；5）plot-task-engine.ts 已实现同组统一 stageEffectivePreset 决议（第 504-518 行）和任务执行时的优先级判断（第 377-380 行）。本次实际新增/修改的文件仅有 visualizer-main-config.ts 和 settings-ui-api.ts，其余文件的逻辑已在之前的某次操作中就位。TypeScript 编译通过，无错误。
needs: 需求本质已完全实现：1）settings 新增按表名的 API 预设覆盖映射；2）visualizer config 面板读写该映射；3）统一刷新与删除清理；4）填表运行时 group 级 effective preset 决议；5）剧情推进同组统一 preset。
key_challenges: 实施过程中发现大部分运行时逻辑已经就位，核心挑战转变为确保 UI 层正确读写 settings 映射而不写入模板对象，以及确保 API 预设刷新/删除正确覆盖新映射。
confidence: HIGH
approach: 按计划文件逐步执行，实际需要修改的文件比预期少（运行时层已就位），UI 层和 settings 刷新层是需要手动实现的部分。
  三维评分：
  - 可维护性: 5/5 — 遵循现有代码结构，复用既有 API 预设刷新入口
  - 健壮性: 4/5 — 覆盖了表名标准化、空值回退、删除清理、同组一致性
  - 可扩展性: 4/5 — 后续扩展只需在 settings 映射上加维度
edge_cases:
  - 表名标准化 trim，空字符串视为无效
  - 同名表共享同一覆盖
  - 改名后映射不自动迁移
  - 模板切换后同名表继续命中
  - 删除 API 预设时清理 tableApiPresetOverridesByName 引用
  - plot 同组任务统一使用 stageEffectivePreset
affected_scope:
  - src/service/settings/settings-service.ts（新增默认值 + 注释）
  - src/service/runtime/state-manager.ts（新增默认值 + 注释）
  - src/presentation/pages/visualizer-main-config.ts（新增 import、HTML select、change 绑定）
  - src/presentation/triggers/settings-ui-sync/settings-ui-api.ts（删除清理 + 刷新逻辑）
execution_plan:
  - step_1: settings-service.ts + state-manager.ts 新增 tableApiPresetOverridesByName 默认值和注释
  - step_2: visualizer-main-config.ts 增加表级 API 预设 select（按表名读写 settings）
  - step_3: settings-ui-api.ts 扩展 API 预设刷新与删除清理逻辑
  - step_4: 确认 update-orchestrator.ts + prompt-api-call.ts 已实现（已就位）
  - step_5: 确认 template-assistant/service.ts 已实现（已就位）
  - step_6: 确认 plot-task-engine.ts 已实现（已就位）
  - step_7: TypeScript 编译验证 + 验收归档
degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被跳过的问题？ → NO
  - execution_plan中是否有步骤使用shell命令？ → NO
</analysis>
---
<output_quality_review>
task_summary: 实施了"填表表级 API 预设覆盖 + 剧情推进同组统一 API 预设"功能，在 visualizer 配置面板中增加了按表名保存的表级 API 预设选择器，并扩展了 API 预设刷新与删除清理逻辑。同时确认了运行时 group 级 effective preset 决议、AI 改表助手覆盖、剧情推进同组统一 preset 等逻辑均已完整就位。
deliverables:
  - src/service/settings/settings-service.ts（新增默认值 + 注释）
  - src/service/runtime/state-manager.ts（新增默认值 + 注释）
  - src/presentation/pages/visualizer-main-config.ts（新增 import、HTML select、change 绑定）
  - src/presentation/triggers/settings-ui-sync/settings-ui-api.ts（删除清理 + 刷新逻辑）

# 量化指标总览
metrics:
  total_files_modified: 4 — 修改的文件总数
  execution_plan_coverage: 7/7 = 100% — execution_plan 执行覆盖率
  edge_cases_handled: 6/6 = 100% — 边界条件处理覆盖率
  confidence_assessment: HIGH — 所有产物经过 TypeScript 编译验证，无已知遗漏

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。settings 默认值增加了带注释的字段，visualizer config 增加了实际可交互的 UI 控件和完整的 change 绑定（含表名标准化、settings 保存、空值删除），settings-ui-api.ts 增加了删除时的映射清理和刷新时的 select 更新。每一段新增代码都在完整链路中起作用。
  - 产物是否能被其目标对象的变化所"击穿"？
    → YES。如果删除 tableApiPresetOverridesByName 字段，visualizer config 的 select 将无法回填初始值；如果修改 API 预设结构，settings-ui-api.ts 的刷新逻辑将无法正确构建选项。
  - 实质性比率: 4/4 = 100%

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。所有 execution_plan 步骤已完成，包括运行时层的确认（已就位不需要修改）。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。四个需要修改的文件都已修改，运行时层已确认就位。
  - 核心业务逻辑是否都有直接验证？
    → YES。TypeScript 编译通过确认类型正确；运行时层逻辑（group effective preset 决议、同组统一、按表名覆盖）已在代码中直接实现。
  - affected_scope 覆盖率: 4/4 = 100%

# 价值密度检查
value_density_check:
  - 产物中高价值内容与低价值内容的比例是多少？
    → 高价值:低价值 = 4:0，高价值占比 100%。每段新增代码都直接服务于核心需求。
  - 是否存在"用数量掩盖质量"的模式？
    → NO。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 本质需求是"每个表格可独立配置 API 预设，且该配置按表名跨模板保留、不入模板、同组统一使用"。产物完整满足这些点：按表名保存在 settings 映射中（跨模板复用），不写入 sheet.updateConfig（不入模板），group 级决议（同组统一），剧情推进同组统一（stageEffectivePreset）。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。TypeScript 编译通过，改动遵循现有代码结构，边界条件（表名标准化、空值删除、预设删除清理）均已处理。
</output_quality_review>
