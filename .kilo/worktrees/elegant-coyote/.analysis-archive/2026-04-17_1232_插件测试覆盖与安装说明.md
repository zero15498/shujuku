<analysis>
context:
  - runtime-env.ts 导出 7 个符号：ACU_INSTANCE_FLAG（常量）、RuntimeMode（const enum，编译后消失）、_forceExtensionMode()、detectRuntimeMode()、isUserscriptMode()、isExtensionMode()、getHostWindow()、checkAndMarkInstance()
  - runtime-env.ts 没有任何测试文件（tests/shared/ 下无 runtime-env.test.ts）
  - tavern-storage.test.ts 已有 201 行测试，但全部测试的是油猴模式下的逻辑（tryReadBridgeFromTop、getConfigStorage、configIdb 缓存操作），完全没有覆盖插件模式分支（isExtensionMode() 为 true 时的 initTavernSettingsBridge_ACU 逻辑）
  - entry-extension.ts 没有任何测试文件
  - 测试目录结构：tests/ 下按 src/ 的层级组织（shared/、data/storage/、service/ 等），使用 vitest + vi.mock
  - 已有测试风格：vi.hoisted 创建 mock，vi.mock 替换模块，beforeEach 清理状态
  - 插件构建配置：rollup.config.js 中 extensionConfig 使用 format: 'es'，有 node-builtins-shim 插件，external 保留 ./script.js 和 ./scripts/extensions.js
  - manifest.json：display_name="星·数据库 III"，loading_order=100，js="index.js"
  - package.json 构建脚本：build:extension="BUILD_MODE=extension rollup -c"，build:all="BUILD_MODE=all rollup -c"
  - tavern-storage.ts 插件分支逻辑：从 window.SillyTavern.extensionSettings 获取设置对象，从 window.saveSettingsDebounced/saveSettings 获取保存函数

needs:
  1. 为 runtime-env.ts 创建完整的单元测试（新文件）
  2. 为 tavern-storage.ts 的插件模式分支补充测试（在已有测试文件中追加）
  3. 告诉冈部怎么安装这个插件

key_challenges:
  1. runtime-env.ts 使用模块级 let _cachedMode 缓存检测结果，每个测试用例之间需要重置这个状态。由于 _cachedMode 不是导出的，需要通过 _forceExtensionMode() 和重新 import 来控制
  2. detectRuntimeMode() 依赖 window.parent 的行为，vitest 的 jsdom 环境中 window.parent === window（没有 iframe），需要 mock window.parent
  3. tavern-storage.ts 的插件分支依赖 isExtensionMode()，需要 mock runtime-env 模块
  4. checkAndMarkInstance() 会修改 window 上的全局标记，测试后需要清理

confidence: HIGH
  - 侦察信息充分：已读取所有源文件和已有测试文件
  - 方案有明确工程依据：遵循已有测试风格（vi.mock + vi.hoisted）
  - 边界条件已全部识别：_cachedMode 重置、window.parent mock、全局标记清理

approach:
  三维评分：
  - 可维护性: 5/5 — 遵循项目已有测试约定（vi.mock + vi.hoisted），测试文件放在对应的 tests/ 子目录下，命名一致
  - 健壮性: 5/5 — 覆盖正常路径、边界条件（跨域 iframe 抛错、重复调用、缓存行为）、错误路径（SillyTavern 不存在时）
  - 可扩展性: 4/5 — 测试结构清晰，未来新增运行模式或插件分支逻辑时可直接追加 describe 块

  方案：
  1. 创建 tests/shared/runtime-env.test.ts，为 runtime-env.ts 的所有导出函数编写单元测试
  2. 在 tests/data/storage/tavern-storage.test.ts 中追加插件模式分支的测试
  3. 由于 _cachedMode 是模块级缓存且不导出，使用 vi.resetModules() + 动态 import 来在每个测试用例中获取干净的模块实例

edge_cases:
  - detectRuntimeMode() 在 window.parent === window 时应返回 Extension（即 'extension'）
  - detectRuntimeMode() 在 window.parent !== window 且 parent.document 可访问时应返回 Userscript（即 'userscript'）
  - detectRuntimeMode() 在 window.parent !== window 但 parent.document 抛错（跨域）时应返回 Userscript
  - _forceExtensionMode() 调用后 detectRuntimeMode() 应始终返回 Extension，即使 window.parent !== window
  - checkAndMarkInstance() 首次调用返回 false 并设置标记，第二次调用返回 true
  - checkAndMarkInstance() 在插件模式下标记设在 window 上，在油猴模式下标记设在 window.parent 上
  - initTavernSettingsBridge_ACU 插件分支：window.SillyTavern 不存在时返回 false
  - initTavernSettingsBridge_ACU 插件分支：window.SillyTavern.extensionSettings 存在时返回 true
  - initTavernSettingsBridge_ACU 插件分支：saveSettingsDebounced 存在时被正确获取
  - initTavernSettingsBridge_ACU 插件分支：只有 saveSettings 存在时作为后备被获取

affected_scope:
  - tests/shared/runtime-env.test.ts（新建）
  - tests/data/storage/tavern-storage.test.ts（追加）

execution_plan:
  - step_1: 创建 tests/shared/runtime-env.test.ts，包含以下测试组：
    - ACU_INSTANCE_FLAG 常量验证
    - _forceExtensionMode() 测试：调用后 detectRuntimeMode 返回 'extension'，isExtensionMode 返回 true
    - detectRuntimeMode() 测试：默认环境（window.parent === window）返回 'extension'；mock window.parent !== window 时返回 'userscript'；跨域 iframe 抛错时返回 'userscript'；缓存行为（多次调用返回相同结果）
    - isUserscriptMode() / isExtensionMode() 测试：与 detectRuntimeMode 结果一致
    - getHostWindow() 测试：插件模式返回 window；油猴模式返回 window.parent
    - checkAndMarkInstance() 测试：首次返回 false 并设标记；第二次返回 true；清理标记后重新检测
  - step_2: 在 tests/data/storage/tavern-storage.test.ts 末尾追加插件模式分支的测试组：
    - mock isExtensionMode 返回 true
    - 测试 initTavernSettingsBridge_ACU 在 window.SillyTavern.extensionSettings 存在时返回 true 并正确设置 tavernExtensionSettingsRoot_ACU
    - 测试 initTavernSettingsBridge_ACU 在 window.SillyTavern 不存在时返回 false
    - 测试 saveSettingsDebounced 优先于 saveSettings 被获取
    - 测试只有 saveSettings 存在时作为后备
  - step_3: 运行 npx vitest run 确认所有测试通过
  - step_4: 运行 BUILD_MODE=all npx rollup -c 确认双构建正常

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES，可维护性 5/5、健壮性 5/5、可扩展性 4/5
  - 是否省略了已知边界条件？ → NO，edge_cases 中列出的 10 个条件全部在 execution_plan 中有对应测试
  - 是否因改动量大而想简化？ → NO，两个测试文件的工作量合理
  - 是否打算跳过某些文件？ → NO，affected_scope 中的两个文件都在 execution_plan 中
  - execution_plan 是否覆盖 affected_scope 所有文件？ → YES，step_1 覆盖 runtime-env.test.ts，step_2 覆盖 tavern-storage.test.ts
  - context 是否充分？ → YES，已读取所有相关源文件和已有测试文件
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan 中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>
