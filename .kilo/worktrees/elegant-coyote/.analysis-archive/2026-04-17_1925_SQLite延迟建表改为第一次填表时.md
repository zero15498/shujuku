<analysis>
context:
  1. 当前 loadFromChat() 在 mergedData 为 null 时（新开卡），立即从模板 parseTableTemplateJson_ACU({ stripSeedRows: true }) 建表灌数据
  2. 用户指出：模板 DDL 在第一次填表时才是最准确的——因为用户可能在进入聊天后、第一次填表前修改了模板
  3. 当前 applyEdits() 方法开头调用 _ensureInitialized()，如果引擎未初始化就抛错
  4. _ensureInitialized() 检查 _initialized 和 engine.isReady 两个条件
  5. loadFromChat() 中 engine.init() 创建内存数据库，_initialized = true 标记引擎已初始化
  6. 新开卡时的正确流程应该是：loadFromChat() 只初始化引擎（engine.init()）+ 标记 _initialized = true，但不建表；第一次 applyEdits() 被调用时，检测到 SQLite 中没有表，再从模板 DDL 建表
  7. SyncBridge.loadFromTableData(data) 遍历 sheet_ 开头的 key，对每张 sheet 执行 generateDDL → engine.run(ddl) → generateInserts → engine.runBatch(inserts)
  8. engine.getTableNames() 可以获取当前 SQLite 中的所有用户表名
  9. applyEdits 是同步方法，parseTableTemplateJson_ACU 和 syncBridge.loadFromTableData 也是同步的
  10. seedRows 和 deleteAllGeneratedEntries 是异步的，不在 applyEdits 中做

needs:
  - 将模板建表时机从 loadFromChat（进入聊天时）延迟到 applyEdits（第一次填表时）
  - loadFromChat 在新开卡时只初始化 SQLite 引擎，不建表
  - applyEdits 在执行 SQL 前检测到没有表时，自动从模板 DDL 建表

confidence: HIGH

approach:
  1. 修改 loadFromChat()：新开卡时只做 engine.init() + _initialized = true，不建表
  2. 新增 _ensureTablesFromTemplate()：检测 SQLite 中是否有用户表，没有则从模板建表
  3. 在 applyEdits/executeQuery/executeMutation 中调用 _ensureTablesFromTemplate()

  三维评分：
  - 可维护性: 5/5 — _ensureTablesFromTemplate 是独立的私有方法，职责单一
  - 健壮性: 5/5 — 延迟建表确保使用最新的模板 DDL
  - 可扩展性: 5/5 — 其他方法也可以调用 _ensureTablesFromTemplate()

edge_cases:
  - getTableNames 返回空但 _acu_sheet_meta 存在：getTableNames 应排除内部表
  - parseTableTemplateJson_ACU 返回 null：抛出错误，由上层重试捕获
  - executeQuery 在第一次填表前被调用：也需要建表
  - 多次调用 _ensureTablesFromTemplate：第二次有表了，直接返回

affected_scope:
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/service/table/sql-table-service.ts

execution_plan:
  - step_1: 修改 loadFromChat()：删除新开卡时的模板初始化逻辑，恢复为只做 engine.init() + _initialized = true
  - step_2: 新增 _ensureTablesFromTemplate()
  - step_3: 修改 applyEdits/executeQuery/executeMutation 中调用 _ensureTablesFromTemplate()
  - step_4: 清理不再需要的 import
  - step_5: TypeScript 编译检查 + lint 检查
  - step_6: 构建并推送制品

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → YES — seedRows 不在 applyEdits 中做，由 refreshMergedDataAndNotify 的兜底逻辑处理，这是正确的职责分离
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 将 SQLite 模式下新开卡时的建表时机从 loadFromChat（进入聊天时）延迟到第一次 applyEdits/executeQuery/executeMutation（第一次填表时）。

deliverables:
  - src/service/table/sql-table-service.ts — loadFromChat 回退 + 新增 _ensureTablesFromTemplate + applyEdits/executeQuery/executeMutation 调用 + 清理 import

metrics:
  total_files_modified: 1
  execution_plan_coverage: 6/6 = 100%
  edge_cases_handled: 4/4 = 100%
  confidence_assessment: HIGH

substance_check: 全部通过，实质性比率 1/1 = 100%
completeness_check: affected_scope 覆盖率 1/1 = 100%
value_density_check: 高价值占比 100%
alignment_check: 满足用户本质需求——建表时机改为第一次填表时，确保使用最新的模板 DDL
</output_quality_review>
