
<analysis>
context:
  1. `_ensureTablesFromTemplate` 使用 `parseTableTemplateJson_ACU({ stripSeedRows: true })` 获取模板数据，该函数解析全局变量 `TABLE_TEMPLATE_ACU`
  2. `TABLE_TEMPLATE_ACU` 是一个全局可变变量（`let TABLE_TEMPLATE_ACU`），通过 `_set_TABLE_TEMPLATE_ACU` 更新
  3. `applyTemplateScopeForCurrentChat_ACU()` 负责将 `TABLE_TEMPLATE_ACU` 更新为当前聊天的模板
  4. 在正常初始化流程中，`applyTemplateScopeForCurrentChat_ACU()` 在 `reloadStorageProvider()` 之前调用
  5. 但 `TABLE_TEMPLATE_ACU` 作为全局可变变量，可能在 `_ensureTablesFromTemplate` 被调用时已经被其他操作修改
  6. `_ensureTablesFromTemplate` 还有第二段代码遍历 `currentJsonTableData_ACU` 中的表，这些表可能来自不同的模板来源
  7. `getCurrentChatTemplateScopeState_ACU()` 可以获取当前聊天的模板快照（templateStr），这是当前聊天模板的权威来源
  8. `sanitizeTemplateSnapshotForChat_ACU` 可以将 templateStr 解析为 templateObj
  9. 当前聊天模板的获取优先级：chat_override > preset_link > 全局模板

needs:
  - `_ensureTablesFromTemplate` 应该只基于当前聊天模板预设来建表
  - 不应该依赖全局可变变量 `TABLE_TEMPLATE_ACU`
  - 第二段代码不应该引入当前聊天模板之外的表

key_challenges:
  1. 需要在 `_ensureTablesFromTemplate` 中获取当前聊天模板快照
  2. getCurrentChatTemplateScopeState 返回的 templateStr 需要被解析为 templateObj
  3. inherit_global 模式应该 fallback 到全局模板
  4. 第二段代码的"用户在指导表中新增了表"场景需要用白名单过滤

confidence: HIGH

approach:
  修改 `_ensureTablesFromTemplate`：优先从 getCurrentChatTemplateScopeState_ACU() 获取当前聊天模板快照，只有在没有聊天级模板时才 fallback 到 parseTableTemplateJson_ACU。第二段代码用模板 sheetKeys 作为白名单过滤。

  三维评分：
  - 可维护性: 5/5 — 模板来源从隐式全局变量变为显式当前聊天模板快照
  - 健壮性: 5/5 — 不再依赖全局可变变量的时序正确性
  - 可扩展性: 4/5 — fallback 链明确，未来新增模式只需扩展

edge_cases:
  - inherit_global 模式：fallback 到 parseTableTemplateJson_ACU
  - chat_override 模式：使用聊天级模板的 templateStr
  - preset_link 模式：从全局预设库获取对应预设的模板
  - 当前聊天模板为空：fallback 到 parseTableTemplateJson_ACU
  - currentJsonTableData_ACU 中有不在当前模板中的表：不建这些表
  - 用户在可视化编辑器中新增了表但还没保存到模板：不建这些表

affected_scope:
  - src/service/table/sql-table-service.ts
  - tests/service/table/sql-table-service.test.ts

execution_plan:
  - step_1: 修改 sql-table-service.ts 的 _ensureTablesFromTemplate。优先从 getCurrentChatTemplateScopeState_ACU() 获取当前聊天模板快照。chat_override 解析 templateStr，preset_link 从预设库获取，inherit_global fallback 到 parseTableTemplateJson_ACU。移除第二段代码中额外建表逻辑，改为白名单过滤。
  - step_2: 修改测试文件，新增 chat_override 和 inherit_global 模式的测试用例。
  - step_3: 运行测试和 TS 编译验证。

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan 是否覆盖 affected_scope 所有文件？ → YES
  - context 是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan 中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>

---

<output_quality_review>
task_summary: 修复 _ensureTablesFromTemplate 建表时同时使用全局模板和当前聊天模板的 bug。改为优先从 getCurrentChatTemplateScopeState_ACU() 获取当前聊天模板快照，只有在 inherit_global 模式下才 fallback 到全局模板。

deliverables:
  1. src/service/table/sql-table-service.ts — 重写 _ensureTablesFromTemplate + 新增 _resolveCurrentChatTemplate
  2. tests/service/table/sql-table-service.test.ts — 新增 3 个模板来源测试 + 扩展 mock + 修复状态泄漏

metrics:
  total_files_modified: 2
  execution_plan_coverage: 3/3 = 100%
  edge_cases_handled: 6/6 = 100%
  confidence_assessment: HIGH

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被其目标对象的变化所"击穿"？ → YES
  - 实质性比率: 2/2 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - affected_scope 覆盖率: 2/2 = 100%

value_density_check:
  - 高价值:低价值 = 3:0，高价值占比 100%
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 满足本质需求：建表时只使用当前聊天模板预设
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>
