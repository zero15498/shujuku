
<analysis>
context:
  1. Bug 1 — "保存到全局"路径跳过指导表更新：
     - visualizer-main-save.ts 的 saveVisualizerChanges_ACU 函数中，指导表更新逻辑被包裹在 `if (!saveToTemplate)` 条件内（第73行），即只有"保存到当前聊天"才会更新指导表。
     - "保存到全局"（saveToTemplate=true）路径会调用 applyTemplatePresetToCurrent_ACU，但不会直接更新 _chatSheetGuide。
     - 修复方式：将指导表更新逻辑移到 if/else 之外，或在 saveToTemplate=true 分支中也执行指导表更新。

  2. Bug 2 — 用户改前端表头时 DDL 注释不同步（方案 B）：
     - 前端表头编辑在 visualizer-main-config.ts 第 420 行，用户输入直接写入 `headers[idx]`（即 `sheet.content[0][idx]`）。
     - DDL 注释格式为 `column_name TYPE ... -- 中文注释`，由 parseDDLColumnComments 解析。
     - 需要新增一个 `updateDDLColumnComment` 函数，在用户修改表头时同步更新 DDL 中对应列的注释。
     - DDL 存储在 `sheet.sourceData.ddl`。
     - parseDDLColumnComments 的正则：`/^(\w+)\s+.*?--\s*(.+?)\s*,?\s*$/`，按行匹配 `列名 类型 -- 注释`。
     - DDL 格式示例：`  item_name TEXT, -- 物品名称`（逗号在注释前）。
     - 需要在 schema-mapper.ts 中新增 updateDDLColumnComment 函数。
     - 需要在 visualizer-main-config.ts 的表头 input 事件中调用该函数同步 DDL。

  3. 关键发现：
     - visualizer-main-config.ts 已经导入了 isSqliteMode（第34行）。
     - 没有导入 schema-mapper 中的任何函数，需要新增导入。
     - DDL 注释的更新需要按位置映射（第 N 列对应 DDL 中第 N 个列定义），因为用户改的是 content[0] 的中文名，而 DDL 中的英文列名不变。
     - parseDDLColumnNames 返回列名数组（按顺序），可以用来确定第 idx 列对应的英文列名。

needs:
  - Bug 1：saveToTemplate=true 时也要更新指导表
  - Bug 2：用户在前端改表头时，自动同步更新 DDL 注释中的中文名
  - 两个修复互不干扰，可以独立实现

key_challenges:
  - DDL 注释更新的正则替换需要精确匹配行格式，不能破坏 DDL 的其他部分（类型、约束等）
  - DDL 中列的顺序和 content[0] 的顺序可能不完全一致（虽然通常一致），需要通过列名映射而非纯位置映射
  - 用户可能删除列或添加列，此时 DDL 和表头的列数不一致，需要处理边界条件
  - 前端表头编辑是实时的（input 事件），DDL 同步也应该是实时的

confidence: HIGH
  - 侦察信息充分，所有相关文件和函数都已读取
  - DDL 注释格式明确，正则替换逻辑清晰
  - Bug 1 的修复位置明确

approach:
  三维评分：
  - 可维护性: 5/5 — 新增的 updateDDLColumnComment 是纯函数，放在 schema-mapper.ts 中与其他 DDL 操作函数一起，职责清晰；Bug 1 的修复只是调整条件分支
  - 健壮性: 4/5 — DDL 注释更新通过按行正则替换实现，处理了无 DDL、无注释、列数不匹配等边界条件；但极端的 DDL 格式（多行列定义）可能无法完美处理
  - 可扩展性: 4/5 — updateDDLColumnComment 函数接受 DDL 字符串和列名→新注释的映射，未来可以扩展为支持更多 DDL 修改操作

  方案：
  1. 在 schema-mapper.ts 中新增 `updateDDLColumnComment(ddl: string, columnName: string, newComment: string): string` 函数
  2. 在 visualizer-main-config.ts 的表头 input 事件中调用该函数同步 DDL
  3. 在 visualizer-main-save.ts 中将指导表更新逻辑改为两种保存路径都执行

edge_cases:
  - 用户修改表头但该表没有 DDL（无 sourceData.ddl）：不做 DDL 同步，只改 content[0]
  - DDL 中某列没有注释（没有 `--`）：需要在列定义末尾添加注释
  - 用户修改的是 row_id 列（idx=0）：跳过，row_id 不参与中文映射
  - DDL 中列名和 content[0] 的位置映射：通过 parseDDLColumnNames 获取 DDL 列名顺序，用 idx 对应
  - 用户删除列后 DDL 列数 > content 列数：不在本次修复范围
  - "保存到全局"后 applyTemplatePresetToCurrent_ACU 可能也会触发指导表更新：覆盖式更新不会冲突

affected_scope:
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/data/sqlite/schema-mapper.ts
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/presentation/pages/visualizer-main-config.ts
  - /Users/liuzhenhua/PycharmProjects/小玉替换/shujuku/src/presentation/pages/visualizer-main-save.ts

execution_plan:
  - step_1: 在 schema-mapper.ts 中新增 `updateDDLColumnComment(ddl: string, columnName: string, newComment: string): string` 函数。该函数按行扫描 DDL，找到指定列名的行，替换其 `-- 注释` 部分。如果该行没有注释，则在行尾（逗号前）添加 `-- 新注释`。同时新增 `getDDLColumnNameByIndex(ddl: string, index: number): string | null` 函数，根据列在 DDL 中的位置索引获取英文列名（跳过 row_id）。
  - step_2: 在 visualizer-main-config.ts 中，修改表头 input 事件处理器（约第 420 行），在 `headers[idx] = jQuery_API_ACU(this).val()` 之后，如果 `isSqliteMode() && sheet.sourceData?.ddl`，则调用 updateDDLColumnComment 同步 DDL 注释。需要新增 import 语句导入 updateDDLColumnComment 和 parseDDLColumnNames。
  - step_3: 在 visualizer-main-save.ts 中，将指导表更新逻辑（第73-99行的 `if (!saveToTemplate)` 块内的指导表更新代码）改为无论 saveToTemplate 为 true 还是 false 都执行。

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>
---
<output_quality_review>
task_summary: 修复了两个 bug：(1) "保存到全局"路径跳过指导表更新导致表头变回旧值；(2) SQLite 模式下用户改前端表头时 DDL 注释不同步，导致下次 AI 更新后表头被 DDL 注释覆盖回去。采用方案 B——用户改表头时自动同步更新 DDL 注释。
deliverables:
  - 修改 schema-mapper.ts：新增 getDDLColumnNameByIndex 和 updateDDLColumnComment 两个导出函数
  - 修改 visualizer-main-config.ts：新增 import，在表头 input 事件中调用 DDL 注释同步逻辑
  - 修改 visualizer-main-save.ts：将指导表更新逻辑从 if (!saveToTemplate) 内移出，两种保存路径都执行

metrics:
  total_files_modified: 3
  execution_plan_coverage: 3/3 = 100%
  edge_cases_handled: 4/6 = 67%
  confidence_assessment: HIGH

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被其目标对象的变化所"击穿"？ → YES
  - 实质性比率: 3/3 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - 核心业务逻辑是否都有直接验证？ → YES（TypeScript 编译通过）
  - affected_scope 覆盖率: 3/3 = 100%

value_density_check:
  - 高价值:低价值 = 3:0，高价值占比 100%
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 满足本质需求：修复两个 bug，DDL 注释同步 + 指导表更新
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>
