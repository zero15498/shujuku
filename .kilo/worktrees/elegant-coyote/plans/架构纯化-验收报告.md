# 架构纯化 — 验收报告

## T-01: 删除 optimization-ui.ts 37 个重复函数体

改动：37 个和 service/plot/plot-logic.ts 重复的函数（36 个导出函数 + 1 个常量 `DEFAULT_PRESET_OPTION_VALUE_ACU`）从函数体定义改为 re-export。同时 plot-logic.ts 中 `resolveActivePlotPresetName_ACU` 从内部函数改为导出。

【presentation 层】optimization-ui.ts
  逐项打勾:
    ✅ ①调用 service 层 — 通过 re-export 从 service/plot/plot-logic.ts 转发 37 个符号
    ✅ ②拿结果做 UI 操作 — 保留的 UI 函数（renderExcludeRuleRows_ACU、appendExcludeRuleRow_ACU、readExcludeRulesFromRows_ACU、renderLoopPromptsList_ACU、saveLoopPromptsFromUI_ACU 等）操作 DOM
    ✅ ③除①②外还有没有越权操作？— 无 ✅

  删除的函数体（37 个）:
    ensureLoopPromptsArray_ACU, ensureTagRulesCompat_ACU, getLegacyPromptFromThree_ACU,
    getLegacyPromptTextsFromPromptGroup_ACU, getPlotPromptGroupFromSource_ACU,
    getPlotFinalDirectiveFromSource_ACU, normalizePlotTask_ACU, normalizePlotTasks_ACU,
    syncLegacyPlotSettingsFromTask_ACU, ensurePlotTasksCompat_ACU,
    applyPlotPresetToSettings_ACU, normalizePlotPresetSelectionValue_ACU,
    isDefaultPlotPresetSelection_ACU, ensurePlotPresetBindingsStore_ACU,
    getPlotPresetBindingForChat_ACU, clearPlotPresetBindingForChat_ACU,
    findPlotPresetByName_ACU, resolveActivePlotPresetName_ACU,
    getCurrentRuntimePlotPresetName_ACU, setCurrentEditablePlotPresetState_ACU,
    syncCurrentEditablePlotPresetState_ACU, getActivePlotEditorSettings_ACU,
    setActivePlotEditorSettings_ACU, getPlotGlobalRevision_ACU,
    resetPlotSettingsToDefault_ACU, replaceCurrentPlotSettingsWithSnapshot_ACU,
    switchCurrentChatPlotPreset_ACU, persistPlotPresetSelectionState_ACU,
    applyGlobalPlotPresetSelectionForEditor_ACU, normalizePlotPresetExcludeRules_ACU,
    stripPlotPresetWorldbookEntrySelectionForExport_ACU, ensurePlotPromptsArray_ACU,
    getPlotPromptContentByIdFromSettings_ACU, setPlotPromptContentByIdForSettings_ACU,
    markPlotIntercept_ACU, shouldSkipPlotIntercept_ACU, getLastOptimizedMessageIndex_ACU

  同时删除的私有辅助函数（只被重复函数使用）:
    looksLikePromptGroupSegments_ACU, getMainSlotFromPlotSegment_ACU,
    buildLegacyWrappedPlotTask_ACU, syncPrimaryPlotTaskFromLegacySettings_ACU,
    normalizePlotPresetBindingChatId_ACU, hasPlotPresetBindingForChat_ACU,
    setPlotPresetBindingForChat_ACU, getCurrentChatScopedPlotPresetName_ACU,
    normalizePlotEditorScope_ACU, getCurrentEditablePlotPresetName_ACU,
    cloneDefaultPlotSettingsForPreset_ACU, buildPlotSettingsPreviewFromPreset_ACU,
    queueSaveCurrentChatPlotScope_ACU, lastPlotInterception_ACU(闭包变量)

  同时删除的重复常量:
    DEFAULT_PRESET_OPTION_VALUE_ACU

  保留的 presentation 层 UI 函数（未删除）:
    showOptimizationOverlay_ACU, hideOptimizationOverlay_ACU, hideOptimizationProgressToast_ACU,
    reoptimizeMessage_ACU, executeContentOptimization_ACU, showOptimizationDiff_ACU,
    renderExcludeRuleRows_ACU, appendExcludeRuleRow_ACU, readExcludeRulesFromRows_ACU,
    renderLoopPromptsList_ACU, saveLoopPromptsFromUI_ACU,
    schedulePlotSettingsUiRefresh_ACU(私有), getPlotPromptContentById_ACU(私有),
    setPlotPromptContentById_ACU(私有), buildPlotModifiedCharCardPrompt_ACU(私有),
    applyPlotPromptOverride_ACU(私有), restorePlotPromptOverride_ACU(私有)

  构建结果: ✅ 通过（npx rollup -c 成功，无新增错误）

【service 层】plot-logic.ts
  改动: resolveActivePlotPresetName_ACU 从 `function` 改为 `export function`
  逐项打勾:
    ✅ ①读数据 — L335: getCurrentChatPlotScopeState_ACU()
    ✅ ②业务决策 — L336-338: if (chatScopeState) / L340-346: if (binding)
    ✅ ③写数据 — 无
    ✅ ④构造返回值 — L337,345,349 等 return 语句
    ✅ ⑤除①②③④外还有没有其他操作？— 无 ✅

---

## T-02: 删除 state-manager.ts 3 个重复变量+setter

改动：state-manager.ts 中 activePlotEditorSettings_ACU / currentPlotTaskEditorId_ACU / currentEditablePlotPresetState_ACU 及其 setter 删除（plot-state.ts 保留为唯一定义）。helpers-remaining.ts import 改源。

【service 层】state-manager.ts
  删除内容:
    - 原 L258: export let activePlotEditorSettings_ACU
    - 原 L259: export let currentPlotTaskEditorId_ACU
    - 原 L260-265: export let currentEditablePlotPresetState_ACU
    - 原 L287: export function _set_currentEditablePlotPresetState_ACU
    - 原 L288: export function _set_activePlotEditorSettings_ACU
    - 原 L289: export function _set_currentPlotTaskEditorId_ACU
  这 3 个变量+3 个 setter 现在只在 service/plot/plot-state.ts 定义一份。

【service 层】helpers-remaining.ts
  改动: import { currentPlotTaskEditorId_ACU, _set_currentPlotTaskEditorId_ACU } 来源从 './state-manager' 改为 '../plot/plot-state'
  逐项打勾:
    ✅ ⑤除①②③④外还有没有其他操作？— 无 ✅（只改了 import 来源，不改函数行为）

构建结果: ✅ 通过

---

## T-03: shared/utils.ts 反向依赖清理

改动：(a) TABLE_ORDER_FIELD_ACU 从 state-manager.ts 搬到 shared/constants.ts (b) buildBoundaryRulesFromLegacyTags_ACU + parseTagList_ACU 从 helpers-remaining.ts 搬到 shared/utils.ts (c) getTemplateSheetKeys_ACU 从 shared/utils.ts 搬到 service/template/chat-scope.ts (d) 删除 shared/utils.ts 对 service/data 层的所有 import

【shared 层】utils.ts import 清单：
  逐项打勾:
    ✅ L11: import { TABLE_TEMPLATE_ACU } from './defaults-json.js' — shared 内部
    ✅ L12: import { DEBUG_MODE_ACU, SCRIPT_ID_PREFIX_ACU, TABLE_ORDER_FIELD_ACU } from './constants' — shared 内部
    ✅ L13: import { safeJsonParse_ACU } from './json-helpers' — shared 内部
    ✅ 除 shared 内部/外部库外还有没有其他来源的 import？— 无

【shared 层】utils.ts 搬走的函数：
    ✅ getTemplateSheetKeys_ACU 是否存在？— 不存在（已搬到 chat-scope.ts）

【shared 层】constants.ts：
    ✅ TABLE_ORDER_FIELD_ACU 是否存在？— 存在（L46: export const TABLE_ORDER_FIELD_ACU = 'orderNo'）

【service 层】state-manager.ts：
    ✅ TABLE_ORDER_FIELD_ACU 是否存在？— 不存在（已删除）

【shared 层】utils.ts 新增的函数 buildBoundaryRulesFromLegacyTags_ACU：
  逐项打勾:
    ✅ ①纯计算(输入→输出) — tagsText 字符串 → 规则数组
    ✅ ②import 来源 — 无外部 import，使用同文件内 parseTagList_ACU（shared 内部）
    ✅ ③除①外还有没有 IO、副作用、运行时状态依赖？— 无

【service 层】chat-scope.ts getTemplateSheetKeys_ACU：
  逐项打勾:
    ✅ ①读数据 — L1361: parseTableTemplateJson_ACU()
    ✅ ②业务决策 — L1364-1365: if (keys.length === 0) / L1370: if (changed)
    ✅ ③写数据 — L1373: _set_TABLE_TEMPLATE_ACU / L1388: setCurrentChatTemplateScopeState_ACU / L1395: saveCurrentProfileTemplate_ACU
    ✅ ④构造返回值 — L1401-1407: return keys.sort(...)
    ✅ ⑤除①②③④外还有没有其他操作？— 无

import 路径更新涉及的文件：
  ✅ visualizer-sidebar.ts: TABLE_ORDER_FIELD_ACU 从 state-manager → shared/constants
  ✅ visualizer-main.ts: TABLE_ORDER_FIELD_ACU 从 state-manager → shared/constants
  ✅ chat-scope.ts: TABLE_ORDER_FIELD_ACU 从 state-manager → shared/constants
  ✅ helpers-remaining.ts: TABLE_ORDER_FIELD_ACU 从 state-manager → shared/constants，getTemplateSheetKeys_ACU 从 shared/utils → chat-scope，buildBoundaryRulesFromLegacyTags_ACU 原定义已删除

构建结果: ✅ 通过

---

## T-04: saveSettings_ACU 调用链

改动：settings-service.ts saveSettings_ACU 从返回 void + 内部弹 3 次 toast，改为返回 `{ saved, storageType, warning?, error? }`。删除 showToastr import。presentation 层 11 个文件（118 处调用）改为调用 `saveSettingsAndNotify_ACU`（定义在 settings-ui-helpers.ts），该函数调 service 层 + 根据返回值弹 toast。

【service 层】settings-service.ts saveSettings_ACU：
  逐项打勾:
    ✅ ①读数据 — L33: getConfigStorage_ACU()、L35: isIndexedDbAvailable_ACU()
    ✅ ②业务决策 — L34: if (store && !store._isTavern)、L35: if (isIndexedDbAvailable)
    ✅ ③写数据 — L29: persistSettingsToStorage_ACU(settings_ACU)、L37/40: initTavernSettingsBridge_ACU()
    ✅ ④构造返回值 — L36/38/41/44: return { saved, storageType, warning/error }
    ✅ ⑤除①②③④外还有没有其他操作？— 无（原 L36 showToastr 已删、原 L38 showToastr 已删、原 L44 showToastr 已删、showToastr import 已删）

【presentation 层】settings-ui-helpers.ts saveSettingsAndNotify_ACU（新增）：
  逐项打勾:
    ✅ ①调用 service 层 — L24: const result = saveSettings_ACU()
    ✅ ②拿结果做 UI 操作 — L25-29: if (result.error) showToastr / if (result.warning) showToastr
    ✅ ③除①②外还有没有越权操作？— 无

【presentation 层】11 个调用方文件：
  改动: import 从 settings-service 改为 settings-ui-helpers，调用从 saveSettings_ACU() 改为 saveSettingsAndNotify_ACU()
  文件清单: settings-ui-sync.ts, api-registry.ts, admin-ui.ts, data-admin-ui.ts, import-process.ts, popup-bindings.ts, popup-helpers.ts, optimization-ui.ts, worldbook-selector.ts, plot-editors.ts, table-selector.ts
  逐项打勾:
    ✅ ①调用 service 层（通过 saveSettingsAndNotify_ACU）
    ✅ ②拿结果做 UI 操作（由 saveSettingsAndNotify_ACU 处理）
    ✅ ③除①②外还有没有越权操作？— 无

构建结果: ✅ 通过

---

## T-05: cancelContentOptimization_ACU 调用链

改动：content-optimization.ts cancelContentOptimization_ACU 从返回 boolean + 内部弹 toast，改为返回 `{ cancelled, reason }`。删除 showToastr import。presentation 层 3 处调用方改为拿返回值自己弹 toast。

【service 层】content-optimization.ts cancelContentOptimization_ACU：
  逐项打勾:
    ✅ ①读数据 — 无
    ✅ ②业务决策 — 无
    ✅ ③写数据 — L687: contentOptimizationAbortRequested_ACU = true
    ✅ ④构造返回值 — L688: return { cancelled: true, reason }
    ✅ ⑤除①②③④外还有没有其他操作？— 无（原 L689 showToastr 已删、showToastr import 已删）

【presentation 层】optimization-ui.ts（2 处）：
  逐项打勾:
    ✅ ①调用 service 层 — L116/L138: cancelContentOptimization_ACU(...)
    ✅ ②拿结果做 UI 操作 — L117/L139: if (cancelResult.cancelled) showToastr_ACU('warning', cancelResult.reason)
    ✅ ③除①②外还有没有越权操作？— 无

【presentation 层】api-registry.ts（1 处）：
  逐项打勾:
    ✅ ①调用 service 层 — L296: cancelContentOptimization_ACU(reason)
    ✅ ②拿结果做 UI 操作 — L297: if (result.cancelled) showToastr_ACU('warning', result.reason)
    ✅ ③除①②外还有没有越权操作？— 无

构建结果: ✅ 通过

---

## T-06: 自动合并纪要调用链（原 checkAndTriggerAutoMergeSummary + performAutoMergeSummary）

改动：原 `performAutoMergeSummary_ACU`（内部 for 循环 + toast 进度 + _notifyTableUpdate）拆成三段纯函数，状态通过参数和返回值传递，无回调、无 UI 操作。原 `checkAndTriggerAutoMergeSummary_ACU` 改为纯判断函数 `checkAutoMergeTrigger_ACU`。删除 showToastr/toastr_API_ACU/ACU_TOAST_CATEGORY/topLevelWindow import。

【service 层】merge-logic.ts `checkAutoMergeTrigger_ACU`（原 checkAndTriggerAutoMergeSummary 拆出的判断部分）：
  逐项打勾:
    ✅ ①读数据 — settings_ACU.autoMergeEnabled、currentJsonTableData_ACU
    ✅ ②业务决策 — if (!autoMergeEnabled) / if (!summaryKey) / if (summaryCount < triggerThreshold)
    ✅ ③写数据 — 无
    ✅ ④构造返回值 — return { shouldTrigger, mergeCount, summaryCount, reserve }
    ✅ ⑤除①②③④外还有没有其他操作？— 无

【service 层】merge-logic.ts `prepareAutoMergeBatches_ACU`：
  逐项打勾:
    ✅ ①读数据 — currentJsonTableData_ACU 读取纪要表行
    ✅ ②业务决策 — 按 batchSize 切分批次
    ✅ ③写数据 — 无
    ✅ ④构造返回值 — return { summaryKey, batches, targetCount, ... }
    ✅ ⑤除①②③④外还有没有其他操作？— 无

【service 层】merge-logic.ts `executeAutoMergeBatch_ACU`：
  逐项打勾:
    ✅ ①读数据 — currentJsonTableData_ACU、settings_ACU.charCardPrompt、调 AI API
    ✅ ②业务决策 — 重试逻辑、解析 AI 响应
    ✅ ③写数据 — 无（只累积到返回值，不写存储）
    ✅ ④构造返回值 — return { accumulatedSummary }（把本批次新行和之前累积合并）
    ✅ ⑤除①②③④外还有没有其他操作？— 无（原 showToastr 进度 toast 已删、原 progressToast.remove 已删）

【service 层】merge-logic.ts `finalizeAutoMerge_ACU`：
  逐项打勾:
    ✅ ①读数据 — currentJsonTableData_ACU 读取纪要表
    ✅ ②业务决策 — 计算 actualEndIndex、重组数据
    ✅ ③写数据 — saveIndependentTableToChatHistory_ACU、updateReadableLorebookEntry_ACU
    ✅ ④构造返回值 — return { mergedRows }
    ✅ ⑤除①②③④外还有没有其他操作？— 无（原 _notifyTableUpdate 已删）

【presentation 层】update-trigger.ts / settings-ui-sync.ts / update-process.ts（共 3 处）：
  逐项打勾:
    ✅ ①调用 service 层 — checkAutoMergeTrigger → prepareAutoMergeBatches → for 循环 executeAutoMergeBatch → finalizeAutoMerge
    ✅ ②拿结果做 UI 操作 — 每次循环自己弹进度 toast、完成后弹成功 toast、调 _notifyTableUpdate
    ✅ ③除①②外还有没有越权操作？— 无

构建结果: ✅ 通过

---

## T-07: applyAiTableEditsToData_ACU 调用链

改动：prompt-builder.ts 删除 showToastr，返回值新增 appliedEdits。删除 showToastr/ACU_TOAST_CATEGORY import。

【service 层】prompt-builder.ts applyAiTableEditsToData_ACU：
  逐项打勾:
    ✅ ①读数据 — currentJsonTableData_ACU
    ✅ ②业务决策 — 解析 AI 响应、匹配表名
    ✅ ③写数据 — 修改 currentJsonTableData_ACU 内容
    ✅ ④构造返回值 — return { success: true, modifiedKeys, appliedEdits }
    ✅ ⑤除①②③④外还有没有其他操作？— 无（原 L1545 showToastr 已删）

构建结果: ✅ 通过

---

## T-08: updateReadableLorebookEntry_ACU 调用链（2 个文件）

改动：pipeline.ts L225 和 injection-engine.ts L1139 各删 1 处 showToastr。两个文件删掉 showToastr import。

【service 层】pipeline.ts updateReadableLorebookEntry_ACU：
  逐项打勾:
    ✅ ①读数据 — TavernHelper_API_ACU.getLorebookEntries
    ✅ ②业务决策 — if (!existingEntry) 创建 / else 更新
    ✅ ③写数据 — createLorebookEntries / setLorebookEntryContent
    ✅ ④构造返回值 — 无显式返回（完成=成功，失败 throw）
    ✅ ⑤除①②③④外还有没有其他操作？— 无（原 L225 showToastr 已删）

【service 层】injection-engine.ts updateReadableLorebookEntry_ACU：
  逐项打勾:
    ✅ ①读数据 — 同上
    ✅ ②业务决策 — 同上
    ✅ ③写数据 — 同上
    ✅ ④构造返回值 — 同上
    ✅ ⑤除①②③④外还有没有其他操作？— 无（原 L1139 showToastr 已删）

构建结果: ✅ 通过

---

## T-09: refreshMergedDataAndNotify_ACU 调用链

改动：pipeline.ts refreshMergedDataAndNotify_ACU 删除 L606-630 的 UI 通知（_notifyTableUpdate + ACU_Visualizer_Refresh + ACU_WindowManager + setTimeout 800ms 等待），改为返回 `{ mergedData, integrityFixed }`。删除 topLevelWindow/SCRIPT_ID_PREFIX import。presentation 层 8 个文件（21 处调用）改为调 `refreshMergedDataAndNotifyWithUI_ACU`（pipeline-ui-helpers.ts），该函数调 service 层 + 做 UI 通知 + 800ms 等待。service 层 4 处内部调用方（chat-scope.ts×3、template-preset-service.ts×1）不改。

【service 层】pipeline.ts refreshMergedDataAndNotify_ACU：
  逐项打勾:
    ✅ ①读数据 — loadAllChatMessages_ACU、mergeAllIndependentTables_ACU、getChatSheetGuideDataForIsolationKey_ACU
    ✅ ②业务决策 — if (!mergedData) 降级处理、integrityFixed 检查
    ✅ ③写数据 — _set_currentJsonTableData_ACU、updateReadableLorebookEntry_ACU
    ✅ ④构造返回值 — return { mergedData, integrityFixed }
    ✅ ⑤除①②③④外还有没有其他操作？— 无（原 _notifyTableUpdate 已删、原 ACU_Visualizer_Refresh 已删、原 ACU_WindowManager 已删、原 setTimeout 等待已删、topLevelWindow import 已删）

【presentation 层】pipeline-ui-helpers.ts refreshMergedDataAndNotifyWithUI_ACU：
  逐项打勾:
    ✅ ①调用 service 层 — refreshMergedDataAndNotify_ACU()
    ✅ ②拿结果做 UI 操作 — _notifyTableUpdate / ACU_Visualizer_Refresh / renderManualTableSelector / updateCardUpdateStatusDisplay / 800ms 等待
    ✅ ③除①②外还有没有越权操作？— 无

【presentation 层】8 个调用方文件（21 处）：
  改动: import 从 pipeline 改为 pipeline-ui-helpers，调用从 refreshMergedDataAndNotify_ACU 改为 refreshMergedDataAndNotifyWithUI_ACU

构建结果: ✅ 通过

---

## T-10: runOptimizationLogic_ACU 调用链

改动：
1. helpers-remaining.ts `runOptimizationLogic_ACU` 删除全部 UI 操作（9 处 showToastr、4 处 toastr_API_ACU.clear、HTML 按钮字符串构造、$toast.find + $abortBtn.on('click') jQuery 事件绑定），改为返回结构化结果 `{ success, finalMessage?, errorType?, errorMessage?, aborted?, skipped?, ... }`
2. helpers-remaining.ts `runPlotTasksRuntime_ACU`（内部函数）删除 `$toast` 参数和 2 处 `$toast.find('.toastr-message').text(...)` 进度更新
3. helpers-remaining.ts 删除 `showToastr_ACU`、`ACU_TOAST_CATEGORY_ACU`、`toastr_API_ACU` import
4. 新建 presentation/components/plot-planning-ui.ts `runOptimizationLogicWithUI_ACU`：创建进度 toast + 中止按钮 → 调 service 层 → 根据结果弹 toast
5. init.ts 3 处调用改为 `runOptimizationLogicWithUI_ACU`

【service 层】helpers-remaining.ts runOptimizationLogic_ACU：
  改后签名: runOptimizationLogic_ACU(userMessage, options): Promise<object>
  逐项打勾:
    ✅ ①读数据 — L4059: loopState_ACU.isRetrying / L4081: settings_ACU.plotSettings / L4093: runPlotTasksRuntime_ACU(plotSettings, userMessage, {...})
    ✅ ②业务决策 — L4059: if (isRetrying) / L4064: if (__inFlight) 互斥 / L4087: if (!plotSettings.enabled) / L4098: if (!runtimeResult?.finalMessage) / L4099: if (abortedByStageFailure) / L4109: if (enabledTaskCount > 0) / L4126-4131: aggregatedTags 处理
    ✅ ③写数据 — L4074-4075: __inFlight/__inFlightText 设置 / L4079: planningGuard_ACU.inProgress = true / L4091: _set_abortController_ACU(new AbortController())
    ✅ ④构造返回值 — L4061: return { success:false, skipped:true, reason:'retrying' } / L4072: return { skipped:true, reason:'inflight' } / L4088: return { skipped:true, reason:'disabled' } / L4100-4123: return 各种失败结果 / L4133-4141: return 成功结果 / L4144/4147: return 中止结果 / L4150-4155: return 异常结果
    ✅ ⑤除①②③④外还有没有其他操作？— 无 ✅
    原 showToastr×9 → 全部删除，改为返回值中的 errorType/errorMessage/aggregatedTagNames 等字段
    原 toastr_API_ACU.clear×4 → 全部删除
    原 HTML 按钮字符串构造 → 删除
    原 $toast.find('.qrf-abort-btn') + $abortBtn.on('click') → 删除
    原 setTimeout(200) 事件绑定 → 删除
    原 _set_isProcessing_Plot_ACU(false) 在中止按钮 handler 中 → 移到 presentation 层

【service 层】helpers-remaining.ts runPlotTasksRuntime_ACU（内部函数，非导出）：
  改后签名: runPlotTasksRuntime_ACU(plotSettings, userMessage, runtimeOptions): Promise<object>
  逐项打勾:
    ✅ ①读数据 — getEnabledPlotTasks_ACU / groupPlotTasksByStage_ACU / buildPlotSharedContext_ACU / executeSinglePlotTask_ACU / aggregatePlotTaskTags_ACU / buildPlotSaveContentFromTaskResults_ACU / buildFinalPlotInjectionMessage_ACU
    ✅ ②业务决策 — if (!enabledTasks.length) / for 循环 stageGroups / if (stageFailedResults.length > 0) 阶段失败早停 / if (!successfulResults.length) 全失败
    ✅ ③写数据 — _set_tempPlotToSave_ACU / savePlotToLatestMessage_ACU
    ✅ ④构造返回值 — return { finalMessage, successfulResults, failedResults, aggregatedTags, enabledTaskCount, ... }
    ✅ ⑤除①②③④外还有没有其他操作？— 无 ✅
    原 $toast.find('.toastr-message').text(...)×2 → 已删除（$toast 参数已删）

【service 层 import 清理】helpers-remaining.ts：
    ✅ `import { showToastr_ACU } from '../runtime/toast-service'` — 已删除
    ✅ `import { ACU_TOAST_CATEGORY_ACU } from '../../shared/constants'` — 已删除
    ✅ `toastr_API_ACU` 从 state-manager import 中移除

【presentation 层】plot-planning-ui.ts runOptimizationLogicWithUI_ACU（新增）：
  逐项打勾:
    ✅ ①调用 service 层 — L76: const result = await runOptimizationLogic_ACU(userMessage, options)
    ✅ ②拿结果做 UI 操作 —
        L28-40: 创建进度 toast（HTML 按钮 + showToastr_ACU）
        L43-68: setTimeout 绑定中止按钮 click 事件（$abortBtn.on('click')）、中止后弹 info toast、_set_isProcessing_Plot_ACU(false)
        L79: 清除进度 toast toastr_API_ACU.clear($toast)
        L87: 跳过判断 → 静默返回
        L92: 中止判断 → 返回 { aborted }
        L97-103: 失败 → showToastr_ACU('error', errorMsg)
        L108-109: 聚合标签 → showToastr_ACU('info', '已成功聚合...')
        L111-121: 部分成功 → showToastr_ACU('warning') / 全成功 → showToastr_ACU('success')
    ✅ ③除①②外还有没有越权操作？— 无 ✅

【presentation 层】init.ts 调用方（3 处）：
  改动: import 从 helpers-remaining → plot-planning-ui，调用从 runOptimizationLogic_ACU → runOptimizationLogicWithUI_ACU
    ✅ L132: const finalMessage = await runOptimizationLogicWithUI_ACU(userMessage, {...}) — TavernHelper.generate 钩子
    ✅ L315: const finalMessage = await runOptimizationLogicWithUI_ACU(messageToProcess, {...}) — 策略1
    ✅ L390: const finalMessage = await runOptimizationLogicWithUI_ACU(originalInputText, {...}) — 策略2
  返回值兼容性:
    ✅ finalMessage.skipped 判断 — wrapper 返回 { skipped: true }，兼容
    ✅ finalMessage.aborted 判断 — wrapper 返回 { aborted: true, manual, restoreText }，兼容
    ✅ typeof finalMessage === 'string' 判断 — wrapper 返回 string，兼容
    ✅ !finalMessage 判断 — wrapper 返回 null，兼容

架构检查结果:
    ✅ service 层 showToastr 调用: 0 处
    ✅ service 层 toastr_API 直接操作: 0 处
    ✅ service 层 jQuery DOM 操作: 0 处

构建结果: ✅ 通过

---

## T-11: helpers-remaining.ts eventSource.emit + _notifyTableUpdate

改动：
1. `seedGreetingLocalDataFromTemplate_ACU`（非导出内部函数，L679）：删掉 `eventSource.emit(MESSAGE_UPDATED)` + `_notifyTableUpdate()`，返回值从 `true/false` 改为 `{ success, messageIndex? }`
2. `fillFirstLayerWithTemplateData_ACU`（导出函数，L758）：同上，返回值改为 `{ success, messageIndex?, sheetCount? }`
3. api-registry.ts 调用方：检查 `fillResult.success`，成功后自己做 `eventSource.emit` + `_notifyTableUpdate`

【service 层】helpers-remaining.ts seedGreetingLocalDataFromTemplate_ACU（L679，非导出）：
  改后签名: seedGreetingLocalDataFromTemplate_ACU(): Promise<{ success, messageIndex? }>
  逐项打勾:
    ✅ ①读数据 — L681: SillyTavern_API_ACU.chat / L682: isNewChatGreetingStage_ACU / L698: buildTemplateBaseStateDataForLocalStorage_ACU / L701: getCurrentIsolationKey_ACU
    ✅ ②业务决策 — L682: if (!isNewChatGreeting) / L699: if (!baseData) / L703: if (!greetingMsg.TavernDB_ACU_IsolatedData)
    ✅ ③写数据 — L718: tagData.independentData = indep / L725-727: 旧格式兼容写入 / L736: saveChat / L741: deleteAllGeneratedEntries / L748: _set_currentJsonTableData_ACU
    ✅ ④构造返回值 — L749: return { success: true, messageIndex: firstAiIndex } / L752: return { success: false }
    ✅ ⑤除①②③④外还有没有其他操作？— 无 ✅
    原 eventSource.emit(MESSAGE_UPDATED) → 已删除
    原 _notifyTableUpdate() → 已删除

【service 层】helpers-remaining.ts fillFirstLayerWithTemplateData_ACU（L758，导出）：
  改后签名: fillFirstLayerWithTemplateData_ACU(templateObj, options): Promise<{ success, messageIndex?, sheetCount? }>
  逐项打勾:
    ✅ ①读数据 — L769: SillyTavern_API_ACU.chat / L784: Object.keys(templateObj) / L789: chat.findIndex
    ✅ ②业务决策 — L770-778: 各种前置检查 / L800: if (existingData) 避免覆盖
    ✅ ③写数据 — L806: firstMsg.TavernDB_ACU_IsolatedData 写入 / L817-818: 旧格式兼容 / L842: saveChat / L845: _set_currentJsonTableData_ACU
    ✅ ④构造返回值 — L848: return { success: true, messageIndex: firstAiIndex, sheetCount } / L851: return { success: false }
    ✅ ⑤除①②③④外还有没有其他操作？— 无 ✅
    原 eventSource.emit(MESSAGE_UPDATED) → 已删除
    原 _notifyTableUpdate() → 已删除

【presentation 层】api-registry.ts 调用方（1 处）：
  逐项打勾:
    ✅ ①调用 service 层 — L1474: const fillResult = await fillFirstLayerWithTemplateData_ACU(templateObj, {...})
    ✅ ②拿结果做 UI 操作 — L1481-1488: if (fillResult.messageIndex != null) → eventSource.emit(MESSAGE_UPDATED) + _notifyTableUpdate()
    ✅ ③除①②外还有没有越权操作？— 无 ✅
  改前: if (fillResult) { ... }
  改后: if (fillResult.success) { eventSource.emit + _notifyTableUpdate; ... }

架构检查结果:
    ✅ service 层 eventSource.emit: 0 处（从 2 降到 0）
    ✅ service 层 _notifyTableUpdate: 1 处（从 3 降到 1，剩余 injection-engine.ts:682 属于其他任务）

构建结果: ✅ 通过

---

## T-12: installSendIntentCaptureHooks_ACU 搬到 presentation

改动：`installSendIntentCaptureHooks_ACU`（纯 DOM 操作：getElementById + addEventListener）从 service/runtime/state-manager.ts 搬到 presentation/bootstrap/init.ts。state-manager.ts 中删除该函数。init.ts 改为 import `markUserSendIntent_ACU`（service 层状态写入函数）。

【service 层】state-manager.ts：
    ✅ installSendIntentCaptureHooks_ACU：不存在（已删除）
    ✅ markUserSendIntent_ACU：仍存在（L62-64，只写 generationGate_ACU.lastUserSendIntentAt）

【presentation 层】init.ts installSendIntentCaptureHooks_ACU（L26-70，局部函数）：
  逐项打勾:
    ✅ ①调用 service 层 — L42/43/44/54: markUserSendIntent_ACU()（写 generationGate 时间戳）
    ✅ ②拿结果做 UI 操作 — 无（markUserSendIntent_ACU 无返回值）
    ✅ ③直接 UI 操作（DOM/事件） — L40: doc.getElementById('send_but') / L42-44: addEventListener('click'/'pointerup'/'touchend') / L48: doc.getElementById('send_textarea') / L49: addEventListener('keydown')
    ✅ ④除①②③外还有没有越权操作（直接读写 data 层等）？— 无 ✅

架构检查结果:
    ✅ service 层 addEventListener: 0 处（从 4 降到 0）

构建结果: ✅ 通过

---

## T-13: toast-service.ts 搬回 presentation

改动：`service/runtime/toast-service.ts` 全部内容搬到 `presentation/theme/toast.ts`。原 toast.ts 的 re-export 门面改为完整实现。6 个直接从 `service/runtime/toast-service` import 的 presentation 文件改为从 `../theme/toast` import。init.ts 的动态 `require('../../service/runtime/toast-service')` 改为使用已 import 的 `showToastr_ACU`。删除 `service/runtime/toast-service.ts`。

验收:
    ✅ service/runtime/toast-service.ts：不存在（已删除）
    ✅ presentation/theme/toast.ts：存在（含完整实现：ensureAcuToastStylesInjected、_acuNormalizeToastArgs、_acuShouldShowToast、showToastr_ACU）
    ✅ service 层所有 .ts 文件的 import 行：无任何一行 import toast-service 或 showToastr
       （di-setup.ts L25/L46 的 showToastr 是 DI 接口类型声明，不是 import toast 模块，属于 T-14 范围）

import 路径更新涉及的文件（6 个）:
    ✅ init.ts: `../../service/runtime/toast-service` → `../theme/toast`（+ require 改为直接调用）
    ✅ api-registry.ts: `../../service/runtime/toast-service` → `../theme/toast`
    ✅ update-process.ts: `../../service/runtime/toast-service` → `../theme/toast`
    ✅ admin-ui.ts: `../../service/runtime/toast-service` → `../theme/toast`
    ✅ import-process.ts: `../../service/runtime/toast-service` → `../theme/toast`
    ✅ auto-loop.ts: `../../service/runtime/toast-service` → `../theme/toast`

构建结果: ✅ 通过

---

## T-14: table-repo.ts → table-service.ts

改动：
1. 4 个函数（saveIndependentTableToChatHistory、loadOrCreateJsonTableFromChatHistory、checkIfFirstTimeInit、initializeJsonTableInChatHistory）从 `data/repositories/table-repo.ts` 搬到 `service/table/table-service.ts`
2. 去掉 `_deps` DI 机制，函数体内直接 import service 层依赖
3. `showToastr`（L219/L224）→ 删除，改为返回值 `{ error: '...' }`
4. `refreshMergedDataAndNotify`（L170/L266/L273）→ 删除，调用方按需自行刷新
5. 删除 `table-repo.ts`、从 `data/index.ts` 删掉 re-export、从 `src/index.ts` 删掉 side-effect import
6. `di-setup.ts` 删掉 `_injectTableRepoDeps` 和所有 table-repo 相关 deps
7. `init.ts` 的 `initializeDataLayerDeps_ACU` 调用精简（只保留 getSettings/getChatFileId）
8. `merge-logic.ts` 和 `injection-engine.ts` import 路径改为 `service/table/table-service`

验收:
    ✅ table-repo.ts：不存在（已删除）
    ✅ di-setup.ts 中 _injectTableRepoDeps：不存在（已删除）

【service 层】table-service.ts saveIndependentTableToChatHistory_ACU：
  改后签名: saveIndependentTableToChatHistory_ACU(...): Promise<{ saved, messageIndex?, error? }>
  逐项打勾:
    ✅ ①读数据 — currentJsonTableData_ACU / SillyTavern_API_ACU.chat / getCurrentIsolationKey_ACU / getChatSheetGuideDataForIsolationKey_ACU / getSortedSheetKeys_ACU / settings_ACU
    ✅ ②业务决策 — if (!currentJsonTableData) / if (!chat) / 目标消息定位 / if (!existingGuide) 首次填充 / modifiedKeys/updateGroupKeys 追踪
    ✅ ③写数据 — targetMessage.TavernDB_ACU_* 字段写入 / saveChat / setChatSheetGuideDataForIsolationKey
    ✅ ④构造返回值 — return { saved: true/false, messageIndex, error? }
    ✅ ⑤除①②③④外还有没有其他操作？— 无 ✅
    原 _deps.showToastr → 已删除（改为返回值 error 字段）
    原 _deps.refreshMergedDataAndNotify → 已删除（调用方自行刷新）

【service 层】table-service.ts loadOrCreateJsonTableFromChatHistory_ACU：
  改后签名: loadOrCreateJsonTableFromChatHistory_ACU(): Promise<{ loaded, source, error? }>
  逐项打勾:
    ✅ ①读数据 — SillyTavern_API_ACU.chat / mergeAllIndependentTables_ACU
    ✅ ②业务决策 — if (!chat) → 初始化 / if (mergedData) → 已有数据 / else → 初始化
    ✅ ③写数据 — _set_currentJsonTableData_ACU / applyTemplateScopeForCurrentChat_ACU
    ✅ ④构造返回值 — return { loaded, source: 'merged'|'initialized'|'empty', error? }
    ✅ ⑤除①②③④外还有没有其他操作？— 无 ✅
    原 _deps.refreshMergedDataAndNotify → 已删除

【service 层】table-service.ts checkIfFirstTimeInit_ACU：
  改后签名: checkIfFirstTimeInit_ACU(): Promise<boolean>
  逐项打勾:
    ✅ ①读数据 — SillyTavern_API_ACU.chat / getCurrentIsolationKey_ACU / settings_ACU
    ✅ ②业务决策 — 遍历聊天记录查找已有表格数据
    ✅ ③写数据 — 无
    ✅ ④构造返回值 — return true/false
    ✅ ⑤除①②③④外还有没有其他操作？— 无 ✅

【service 层】table-service.ts initializeJsonTableInChatHistory_ACU（非导出）：
  改后签名: initializeJsonTableInChatHistory_ACU(): Promise<{ initialized, error? }>
  逐项打勾:
    ✅ ①读数据 — parseTableTemplateJson_ACU
    ✅ ②业务决策 — try/catch 解析失败判断
    ✅ ③写数据 — _set_currentJsonTableData_ACU / ensureChatSheetGuideSeeded / attachSeedRowsToCurrentDataFromGuide / deleteAllGeneratedEntries
    ✅ ④构造返回值 — return { initialized: true/false, error? }
    ✅ ⑤除①②③④外还有没有其他操作？— 无 ✅
    原 _deps.showToastr → 已删除

架构检查结果:
    ✅ data 层注入/调用 UI 相关依赖: 0 处（从 9 降到 0）
    总计违规: 4 条（从 21 降到 4）

构建结果: ✅ 通过

---

## T-15: config-storage.ts 业务编排搬到 service

改动：`persistSettingsToStorage_ACU`（data 层）的隔离码同步逻辑（normalizeIsolationCode → globalMeta 更新 → addDataIsolationHistory → normalizeDataIsolationHistory → saveGlobalMeta）搬到 `saveSettings_ACU`（service 层）。`persistSettingsToStorage_ACU` 精简为纯存储：接收 settingsObj + isolationCode → sanitize → JSON.stringify → store.setItem。3 处调用方改为传入 isolationCode 参数。

【data 层】config-storage.ts persistSettingsToStorage_ACU：
  改后签名: persistSettingsToStorage_ACU(settingsObj?, isolationCode?): void
  逐项打勾:
    ✅ ①接收参数 — L29: settingsObj, isolationCode
    ✅ ②读/写存储 — L32: getConfigStorage_ACU() / L37: store.setItem(key, payload)
    ✅ ③返回数据 — 无（void，失败 logError）
    ✅ ④除①②③外还有没有其他操作？— 无 ✅
    原 normalizeIsolationCode_ACU → 搬到 service 层
    原 globalMeta_ACU 更新 → 搬到 service 层
    原 addDataIsolationHistory_ACU → 搬到 service 层
    原 normalizeDataIsolationHistory_ACU → 搬到 service 层
    原 saveGlobalMeta_ACU → 搬到 service 层

【service 层】settings-service.ts saveSettings_ACU（T-04 + T-15 合并）：
  逐项打勾:
    ✅ ①读数据 — L29: settings_ACU.dataIsolationCode / L29: globalMeta_ACU.activeIsolationCode / L43: getConfigStorage_ACU()
    ✅ ②业务决策 — L30-35: 隔离码同步（normalizeIsolationCode → globalMeta → addHistory → normalize → save） / L44: if (!store._isTavern) / L45: if (isIndexedDbAvailable)
    ✅ ③写数据 — L38: persistSettingsToStorage_ACU(settings_ACU, code) / L46/49: initTavernSettingsBridge
    ✅ ④构造返回值 — L47/50/53/56: return { saved, storageType, warning/error }
    ✅ ⑤除①②③④外还有没有其他操作？— 无 ✅

构建结果: ✅ 通过

---

## T-16: template-preset-ui.ts re-export 链

改动：`template-preset-ui.ts` L16 的 15 个 service 层符号 re-export 行删除。6 个消费方文件改为直接从 `service/template/template-preset-service` import service 函数，UI 函数（loadTemplatePresetSelect、refreshTemplatePresetSelectInUI、getTemplatePresetSelectJQ）保留从 template-preset-ui import。

【presentation 层】template-preset-ui.ts：
  逐项打勾:
    ✅ ①调用 service 层 — L13: import service 函数用于自身 UI 函数内部
    ✅ ②拿结果做 UI 操作 — L20+: getTemplatePresetSelectJQ、loadTemplatePresetSelect、refreshTemplatePresetSelectInUI 等 DOM 操作函数
    ✅ ③直接 UI 操作 — 所有 export function 都是 DOM 操作（jQuery 选择器、.empty()、.append()）
    ✅ ④除①②③外还有没有 re-export 行？— 无 ✅（原 L16 的 15 个 re-export 已删除）

消费方 import 更新:
    ✅ visualizer-main.ts: service 函数 → `template-preset-service`，UI 函数 → `template-preset-ui`
    ✅ popup-bindings.ts: service 函数 → `template-preset-service`，UI 函数 → `template-preset-ui`
    ✅ visualizer.ts: `getActiveTemplatePresetMeta_ACU` → `template-preset-service`
    ✅ update-status-display.ts: `getActiveTemplatePresetMeta_ACU` → `template-preset-service`
    ✅ data-admin-ui.ts: service 函数 → `template-preset-service`，UI 函数 → `template-preset-ui`

构建结果: ✅ 通过

---

## T-17: plot-editors.ts re-export 链

改动：`plot-editors.ts` L14 的 8 个 service 层符号（activePlotEditorSettings、currentPlotTaskEditorId、currentEditablePlotPresetState、3个setter、buildDefaultPlotPromptGroup、ensurePlotPromptGroup）re-export 行删除。消费方改为直接从 `service/plot/plot-state` import。

【presentation 层】plot-editors.ts：
  逐项打勾:
    ✅ ①调用 service 层 — L12: import 用于自身 UI 函数内部
    ✅ ②拿结果做 UI 操作 — renderPromptSegments、renderPlotTaskList 等 DOM 操作
    ✅ ③直接 UI 操作 — 所有 export function 都是 DOM 操作
    ✅ ④除①②③外还有没有 re-export 行？— 无 ✅（原 L14 的 8 个 re-export 已删除）

消费方 import 更新:
    ✅ optimization-ui.ts: 8 个符号 → `service/plot/plot-state`
    ✅ popup-bindings.ts: `buildDefaultPlotPromptGroup_ACU` → `service/plot/plot-state`

构建结果: ✅ 通过

---

## T-18: settings-service.ts 死 re-export

改动：删除 `globalMeta_ACU`/`saveGlobalMeta_ACU` 的死 re-export（无消费方）。保留有消费方的 re-export（getConfigStorage、persistTavernSettings、saveCurrentProfileTemplate、getDataIsolationHistory、removeDataIsolationHistory）——这些是 presentation 层通过 service 层访问 data 层的合法中转。`template-preset-service.ts` 的 `getConfigStorage_ACU`/`saveCurrentProfileTemplate_ACU` 改为直接从 data 层 import（service→data 合法）。

验收:
    ✅ settings-service.ts 死 re-export（globalMeta_ACU/saveGlobalMeta_ACU）：已删除
    ✅ 有消费方的 re-export：保留（附注释说明用途）

构建结果: ✅ 通过

---

## T-19: 删除死代码/空壳文件

改动：删除 `character-settings-repo.ts`（空壳 DI 注入点，注入的 `_settingsRef`/`_chatFileIdRef` 无任何读取方）。清理 di-setup.ts（删除 `_injectCharSettingsDeps`、简化 deps 参数）。清理 data/index.ts 和 src/index.ts 的引用。

注：table-repo.ts 已在 T-14 删除，toast-service.ts 已在 T-13 删除。

验收:
    ✅ src/data/repositories/character-settings-repo.ts — 不存在
    ✅ src/data/repositories/table-repo.ts — 不存在（T-14 已删）
    ✅ src/service/runtime/toast-service.ts — 不存在（T-13 已删）
    ✅ di-setup.ts 中 _injectCharSettingsDeps — 不存在

构建结果: ✅ 通过

---

## T-20: data/index.ts 清理

改动：删除 data/index.ts 中 shared 层的 re-export（`../shared/data-constants`、`../shared/idb-import-temp`）。

验收:
    ✅ data/index.ts 每个 export 来源都是 data 层内部？— 是（models/*、storage/*、repositories/*）
    ✅ 无 `../shared/...` 的 re-export

构建结果: ✅ 通过

---

## T-21: helpers-remaining.ts document.createElement → 纯正则

改动：`sanitizeHtml` 局部函数（L3288-3292）从 `document.createElement('div') + innerHTML + textContent` 改为纯正则 HTML 剥离（`<br>` → `\n`，`<tag>` 移除，HTML 实体解码）。

【service 层】helpers-remaining.ts sanitizeHtml（局部函数）：
  逐项打勾:
    ✅ ①读数据 — 接收 htmlString 参数
    ✅ ②业务决策 — 无
    ✅ ③写数据 — 无
    ✅ ④构造返回值 — return 处理后的纯文本字符串
    ✅ ⑤除①②③④外还有没有其他操作？— 无 ✅
    原 document.createElement('div') → 已删除（改为 .replace 正则链）
    原 .innerHTML = htmlString → 已删除
    原 .textContent → 已删除

架构检查结果:
    ✅ service 层 DOM API: 0 处（从 1 降到 0）
    ✅ service 层 .innerHTML: 0 处（从 1 降到 0）

构建结果: ✅ 通过

---

## T-22: content-optimization.ts window.* → shared/host-api

改动：`content-optimization.ts` 中 3 处 `window.SillyTavern` / `window.power_user` 改为 `(topLevelWindow_ACU as any).SillyTavern` / `(topLevelWindow_ACU as any).power_user`（topLevelWindow_ACU 已在 shared/env.ts 中定义并 import）。

验收:
    ✅ content-optimization.ts 中无直接 `window.` 引用（`topLevelWindow_ACU` 统一访问）
    ✅ import { topLevelWindow_ACU } from '../../shared/env' 已存在（L3）

构建结果: ✅ 通过

---

## T-23: chat-scope.ts refreshUi 参数删除

改动：`activateChatTemplatePresetSelection_ACU` 和 `restoreChatTemplateArchiveEntry_ACU` 签名中删除 `refreshUi` 参数（函数体内从未使用该参数）。调用方传入的 `refreshUi: true/false` 被 JS 解构忽略，不报错。

验收:
    ✅ activateChatTemplatePresetSelection_ACU 签名包含 refreshUi？— 不包含
    ✅ restoreChatTemplateArchiveEntry_ACU 签名包含 refreshUi？— 不包含
    ✅ 函数体内有没有因为 refreshUi 做的 UI 操作？— 无

构建结果: ✅ 通过

---

## 总结

**T-01 ~ T-23 共 23 条任务全部完成。**

架构检查最终结果（总计违规: 2 条，全部不在 T-01~T-23 范围内）:

| 检查项 | 结果 |
|--------|------|
| service → presentation | ✅ 0 条 |
| data → service | ✅ 0 条 |
| data → presentation | ✅ 0 条 |
| presentation → data | ✅ 0 条 |
| shared → service | ❌ 1 条（shared/index.ts service-locator，不在任务范围） |
| service 层 showToastr | ✅ 0 处 |
| service 层 toastr_API | ✅ 0 处 |
| service 层 _notifyTableUpdate | ❌ 1 处（injection-engine.ts:682，不在任务范围） |
| service 层 Visualizer/WindowManager | ✅ 0 处 |
| service 层 jQuery DOM | ✅ 0 处 |
| service 层 DOM API | ✅ 0 处 |
| service 层 addEventListener | ✅ 0 处 |
| service 层 .innerHTML | ✅ 0 处 |
| service 层 eventSource.emit | ✅ 0 处 |
| data 层注入/调用 UI 依赖 | ✅ 0 处 |
