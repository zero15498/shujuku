<analysis>
context: 已确认三个问题的根因都不在表层 UI，而在编排层与发送前拦截层。
  1. 多批次填表的提示信息缺口来自 [`processUpdatesBatch_ACU()`](src/service/table/update-orchestrator.ts:436) 与 [`proceedWithCardUpdate_ACU()`](src/presentation/triggers/update-process.ts:93) 之间只传了“单次卡片更新”的进度事件，没有传“当前批次/总批次”上下文。当前 [`handleProgressEvent()`](src/presentation/triggers/update-process.ts:54) 只能显示单次尝试号，无法显示整个多批次任务的进度，也不会刷新 toast 文案本身。
  2. 当前“终止”逻辑只对当前请求做了本地中止，没有形成稳定的任务级终止语义。证据有两个：
     - [`proceedWithCardUpdate_ACU()`](src/presentation/triggers/update-process.ts:93) 的停止回调在点击后 3 秒会把 [`wasStoppedByUser_ACU`](src/service/runtime/state-manager.ts:232) 自动复位，这对单次请求勉强可用，但对多批次/多组任务是脆弱设计。
     - [`processUpdatesBatch_ACU()`](src/service/table/update-orchestrator.ts:436) 外层批次循环没有在每批开始前检查 [`wasStoppedByUser_ACU`](src/service/runtime/state-manager.ts:232)，手动多组编排 [`orchestrateManualUpdate_ACU()`](src/service/table/update-orchestrator.ts:547) 也没有任务级短路检查。
  3. 手动更新的“分组”目前只是逻辑分桶，不是并发调度。根因在 [`orchestrateManualUpdate_ACU()`](src/service/table/update-orchestrator.ts:547) 内部对每个 group 使用串行 `for + await`，而自动更新在 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:215) 中已经按 [`settings.maxConcurrentGroups`](src/service/table/update-scheduler.ts:226) 分块并发执行 `Promise.allSettled`。所以用户看到“自动更新能并发，手动更新不能”，不是错觉，是两条编排路径真的分叉了。
  4. 手动更新当前分组构造还存在一个隐含问题：它使用全局 [`uiBatchSize`](src/service/table/update-orchestrator.ts:591) 参与分组键，而不是完全遵循自动路径的调度维度。这不是本次投诉的直接根因，但如果继续手工复制自动调度逻辑，后续还会再次漂移。
  5. 向量模式的“启动即先生成关键词”问题根因明确在 [`orchestrateVectorRecallBeforeSend_ACU()`](src/service/plot/vector-recall-orchestrator.ts:144)。当前流程是：先无条件执行 [`generateVectorRecallKeywords_ACU()`](src/service/vector/vector-recall-keyword-service.ts:206) → 再检查 [`getActiveRemoteMemorySnapshot_ACU()`](src/service/plot/vector-recall-orchestrator.ts:232)。也就是说，即使当前根本没有任何远记忆批次，系统仍然会先做关键词生成。
  6. 当没有快照时，当前向量发送前流程还会调用 [`syncVectorMemoryLorebookEntry_ACU()`](src/service/worldbook/vector-memory-entry-service.ts:310) 传入空数组，以“同步空结果到世界书”的方式继续走注入链。这与用户要求“暂时没有向量数据录入，就不进行关键词生成并注入流程”直接冲突。
  7. 远记忆归档链路本身在 [`buildSummaryVectorIndexIfNeeded_ACU()`](src/service/vector/vector-index-orchestrator.ts:310) 中会在构建完成后调用 [`syncVectorMemoryLorebookEntryFromState_ACU()`](src/service/worldbook/vector-memory-entry-service.ts:320)，这属于“归档后同步状态到世界书”。用户提到的“启动时”问题不在这里，而在发送前召回编排 [`orchestrateVectorRecallBeforeSend_ACU()`](src/service/plot/vector-recall-orchestrator.ts:144)。这两个场景不能混为一谈，否则会把跳过条件加错层级。
confidence: HIGH
  - 已直接读到手动更新、自动更新、批处理 UI 壳、远记忆归档、向量发送前召回的核心实现。
  - 三个问题都能在现有代码中定位到明确的触发点和缺失点，不依赖猜测。
  - 目前唯一需要在执行阶段顺手校正的是“共享并发执行器”的落点设计，而不是需求本身的不确定性。
approach: 采用“补齐任务级上下文 + 对齐手动/自动调度语义 + 提前短路无向量数据路径”的方案。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 不把提示文案、终止逻辑、并发逻辑分别打补丁，而是补齐批处理上下文对象、抽出共享的分组并发执行器，让手动路径与自动路径复用同一套并发语义，避免再次分叉。
  - 健壮性: 5/5 — 终止语义提升到任务级，要求“停止后不再派发新批次/新组，并立即中断所有在途请求”；向量模式在“无快照或无远记忆批次”时直接短路，避免无意义 AI 调用和空注入。
  - 可扩展性: 5/5 — 后续如果还要展示“当前组/总组”“已完成批次数”“失败重试来源”等信息，只需扩展上下文字段；后续若要让更多入口走同一套分组并发策略，也有共享执行器可复用。
edge_cases:
  - 用户在第 N/M 批调用 AI 期间点击终止：当前请求必须中断，当前任务不得继续派发第 N+1 批，也不得继续后续 group。
  - 用户在一个批次刚完成、下一个批次尚未开始时点击终止：不能因为没有在途请求就忽略终止标记，外层循环必须在批次边界立刻停下。
  - 手动更新存在多个 group 且并发数大于 1：终止时必须同时阻止未开始的 chunk，并中断已启动的所有 group 请求。
  - 多批次场景下，第 2/5 批进入第 3/3 次重试：toast 与状态文本都必须同时显示“批次进度 + 当前重试次数”，不能只显示其一。
  - 手动更新 group 中某一组失败：需要收敛为整体失败结果，并执行与当前实现一致的数据恢复/刷新逻辑，不能因为改成并发就留下脏状态。
  - 自动更新路径必须保持现有并发行为不退化，不能因为抽共享执行器把 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:215) 的并发调度降级成串行。
  - 向量模式已启用但当前没有远记忆快照：发送前召回应直接跳过并放行，而不是阻断，也不是继续生成关键词。
  - 向量模式存在快照对象但 `remoteMemoryBatches` 为空：也应视为“暂无向量数据”，同样跳过关键词生成与世界书注入。
  - 向量模式已有远记忆批次：仍然必须保留现有“关键词生成 → 向量召回 → 世界书同步”的严格 gate 行为，不能误伤正常召回。
affected_scope:
  - [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts) — 扩展批处理上下文、补充任务级终止检查、改造手动分组并发执行。
  - [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts) — 使用新的批处理上下文刷新 toast/状态文本，修正停止按钮的任务级终止语义。
  - [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) — 抽取或复用共享的分组并发执行逻辑，让自动与手动路径对齐。
  - [`src/service/plot/vector-recall-orchestrator.ts`](src/service/plot/vector-recall-orchestrator.ts) — 在关键词生成前增加“无向量数据”短路判断，跳过空数据下的关键词生成与世界书同步。
execution_plan:
  - step_1: 修改 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts) 的进度类型与批处理执行逻辑，为每个批次生成明确的 `currentBatch`、`totalBatches` 上下文，并在外层批次循环与手动 group 执行边界补充 [`wasStoppedByUser_ACU`](src/service/runtime/state-manager.ts:232) 检查。
  - step_2: 修改 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts)，让 [`proceedWithCardUpdate_ACU()`](src/presentation/triggers/update-process.ts:93) 和 [`handleProgressEvent()`](src/presentation/triggers/update-process.ts:54) 同时更新 toast 与状态文本，展示“当前批次/总批次 + 当前尝试/最大重试”，并移除 3 秒后自动清除终止标记的脆弱做法，改为由任务编排层统一复位。
  - step_3: 修改 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 与 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts)，抽取共享的 group 并发执行器或等价复用点，让手动更新按 [`settings.maxConcurrentGroups`](src/service/table/update-scheduler.ts:226) 分块并发执行，同时保留现有失败恢复与刷新时机。
  - step_4: 修改 [`src/service/plot/vector-recall-orchestrator.ts`](src/service/plot/vector-recall-orchestrator.ts)，在进入 [`generateVectorRecallKeywords_ACU()`](src/service/vector/vector-recall-keyword-service.ts:206) 之前先检查有效远记忆快照/批次数；若当前没有任何远记忆数据，则直接返回 `skipped + shouldProceed=true`，不再执行关键词生成，也不再调用 [`syncVectorMemoryLorebookEntry_ACU()`](src/service/worldbook/vector-memory-entry-service.ts:310) 做空注入。
  - step_5: 运行针对性的构建或类型检查，确认新增上下文字段、共享执行器和向量短路逻辑没有破坏现有调用方。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。它同时解决了 UI 展示、任务终止、手动并发、向量空数据短路四个层面的根因，没有把问题拆成彼此独立的临时补丁。
  - 是否遗漏了已知边界条件？ → NO。已覆盖批次边界终止、并发 group 终止、失败恢复、空快照、空批次、正常快照保留严格 gate 等关键路径。
  - 是否因改动量大而想缩减方案？ → NO。真正的风险点是编排层；如果只改 toast 文案或只改一条 `if`，只是把故障藏起来，不是解决。
  - 是否打算跳过某些文件？ → NO。四个文件正好覆盖三个问题的根因位置，没有发现必须同步修改却被故意跳过的调用方文件。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。四个受影响文件都在执行计划中有对应步骤。
  - context是否充分？是否有未读但可能相关的文件？ → NO。当前上下文已经足以实施；执行阶段若发现共享并发执行器需要额外调用方校正，再通过 decision_point 处理，而不是现在凭空扩 scope。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。手动分组使用全局 batchSize 的一致性问题已在 context 中记录，并将在并发改造时一并审视，避免留下新的语义漂移。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。所有源代码修改都将使用标准编辑工具完成。
  → YES项必须就地写出修正内容，然后以修正后的方案进入执行
</analysis>
---
<decision_point>
issue: 在修改 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts) 以展示批次进度时，我引入了一个执行期错误：把字段名误写成了 `totalBatchs`，而真实字段是 [`totalBatches`](src/service/table/update-orchestrator.ts:67)。这个问题没有在侦察/分析阶段预见到，因为它不是架构不确定性，而是执行阶段的人为拼写错误。
impact: NO + 这不影响原方案的可行性，但会直接导致 TypeScript 编译错误，影响范围限定在 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts)。
context_update: 本次新发现不改变原 analysis 中的任何架构假设；受影响字段只有执行状态中的“`step_2` 已开始但存在编译错误，必须先修复后继续”。
options:
  - option_a:
      description: 直接修正 [`buildBatchProgressLabel()`](src/presentation/triggers/update-process.ts:50) 中的字段拼写，把 `totalBatchs` 改回 `totalBatches`，保持当前方案不变后继续执行。
      approach_evaluation: 可维护性 5/5，修改最小且与类型定义保持一致；健壮性 5/5，立即消除当前编译错误，不引入新分支；可扩展性 5/5，后续所有批次显示逻辑仍围绕统一字段工作。
      edge_cases: 需要同时检查该函数是否还有其他拼写或重复分支残留，避免“修一个字母漏一个分支”。
      affected_scope_delta: 无新增文件，仅继续修改 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts)。
  - option_b:
      description: 在 [`CardUpdateProgressEvent`](src/service/table/update-orchestrator.ts:60) 中新增兼容字段 `totalBatchs?: number`，让 UI 层错误拼写也能通过编译。
      approach_evaluation: 可维护性 1/5，等于把错误字段正式写进类型，制造长期语义污染；健壮性 2/5，虽然能编译，但会让同一语义存在两个字段名；可扩展性 1/5，后续调用方会继续混用错误拼写，债务扩散。
      edge_cases: 未来任一调用方可能只填一个字段，导致显示逻辑不一致；类型层无法再帮助发现同类错误。
      affected_scope_delta: 新增对 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts) 的无意义兼容修改。
  - option_c:
      description: 回退 [`buildBatchProgressLabel()`](src/presentation/triggers/update-process.ts:50) 的批次显示增强，仅保留原有尝试次数展示，暂时绕开出错分支，后续再重做。
      approach_evaluation: 可维护性 2/5，回退会让需求重新未完成；健壮性 3/5，能消除当前报错，但把用户明确要求的“显示当前进度”又丢掉；可扩展性 2/5，后续仍要重新开工一次。
      edge_cases: 用户最关心的“多批次进度”继续缺失；后续再次修改时还要重复走同一段路径。
      affected_scope_delta: 表面减少改动，但实质让 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts) 回退成不满足需求的状态。
recommendation: 选择 option_a。它在可维护性、健壮性、可扩展性三个维度都是综合最优：只修正执行期拼写错误，不污染类型定义，也不回退已确定的需求实现。
execution_plan_update: 原 execution_plan 不变，仅在当前的 step_2 中先插入一个修复动作：修正 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts) 内 `buildBatchProgressLabel()` 的字段名错误，然后继续完成该文件剩余的 toast/状态文本/终止语义修改。
deviation_audit:
  original_plan_excerpt: 
    - step_2: 修改 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts)，让 [`proceedWithCardUpdate_ACU()`](src/presentation/triggers/update-process.ts:93) 和 [`handleProgressEvent()`](src/presentation/triggers/update-process.ts:54) 同时更新 toast 与状态文本，展示“当前批次/总批次 + 当前尝试/最大重试”，并移除 3 秒后自动清除终止标记的脆弱做法，改为由任务编排层统一复位。
  current_proposal: 在继续完成 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts) 的 step_2 之前，先修正 `buildBatchProgressLabel()` 中 `totalBatchs` → `totalBatches` 的拼写错误，然后继续按原方案完成批次展示与终止语义改造。
  diff_summary:
    - 新增了一个“先修编译错误”的前置小步骤。
    - 没有删减原 step_2 的任何目标，也没有缩小 affected_scope。
    - 没有替换技术方案，只是修正执行期错误后回到原路径。
  deviation_motive_check:
    - **措辞替换规则逐类检查**（基于 decision_point 前置步骤中已读取的十类规则）：全部未命中，当前偏离只是修复执行期拼写错误，不存在借口式降级措辞。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。修复拼写错误后继续原方案，三维评分不变。
    - 偏离是否导致 affected_scope 缩小？→ NO。受影响范围未缩小。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_a 直接修复错误且不污染类型，不回退需求。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。新问题仅是字段拼写错误，已明确要求同时检查同函数内是否还有同类残留。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。这里选择最小修改是因为它同时是质量最高的方案，而不是为了偷工减料。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。原 affected_scope 完全保留。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。当前编译错误必须立即处理，没有被跳过。
  - options 是否包含至少三个方案？ → YES。已提供 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。仍使用标准编辑工具修复。
  - deviation_audit 是否触发了 self_dissection？ → NO。没有触发偷懒判定。
  → YES项必须就地修正
</decision_point>
---
<decision_point>
issue: 在修改 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 时，我尝试一次性替换较大代码块，但 [`edit_file`](src/service/table/update-scheduler.ts:187) 的 `old_string` 没有和文件当前内容精确匹配，导致编辑失败。这不是架构问题，而是执行阶段的工具参数构造不够精确：替换范围过大，包含了我没有逐字校对的上下文。
impact: YES + 当前 step_2.1 被阻塞，无法继续抽取并发执行骨架；但整体方案仍然成立，影响范围限定在 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 的当前编辑步骤，以及后续依赖该共享执行器的 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts) 改造顺序。
context_update:
  - 已确认自动更新完整收尾逻辑位于 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:215) 后半段，不能用整段重写的粗暴方式处理，否则容易把自动合并和旧数据清理一起误改。
  - 已确认本次失败不改变原 analysis 的技术方案，只改变“如何修改文件”的执行方法：从大块替换改为分段精确编辑。
  - 已确认不能因为工具匹配失败就退回到 shell 文本替换；那种做法绕过安全检查，纯属低级失格。
options:
  - option_a:
      description: 继续沿用原方案，但先将 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 的修改拆成多个小范围、精确上下文的编辑步骤；必要时使用 [`apply_diff`](src/service/table/update-scheduler.ts:187) 按行号做外科手术式替换。
      approach_evaluation: 可维护性 5/5，修改边界清晰，便于复查；健壮性 5/5，能避免误伤自动更新收尾逻辑；可扩展性 5/5，后续仍可把共享并发骨架稳定复用于手动更新。
      edge_cases: 需要确保新增的共享执行器不吞掉 group 级失败信息，也不能改变自动更新原有的刷新、自动合并、旧数据清理顺序。
      affected_scope_delta: 无新增文件；只是在 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 上改用更小粒度的编辑方式，后续仍按原计划继续修改 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts)、[`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts)、[`src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts`](src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts) 与 [`src/service/plot/vector-recall-orchestrator.ts`](src/service/plot/vector-recall-orchestrator.ts)。
  - option_b:
      description: 放弃局部修改，直接用 [`write_to_file`](src/service/table/update-scheduler.ts:1) 重写整个 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts)。
      approach_evaluation: 可维护性 2/5，整文件重写会放大 diff，审查成本很高；健壮性 2/5，容易把未触及逻辑一起改坏；可扩展性 3/5，虽然一次性可整理结构，但对当前这个中等规模文件来说属于过度操作。
      edge_cases: 一旦重写时漏掉注释、导入或后半段收尾逻辑，回归范围会明显扩大。
      affected_scope_delta: 影响仍只在 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts)，但风险从“局部可控修改”升级为“整文件回归风险”。
  - option_c:
      description: 因为标准编辑工具一次失败，就改用 shell 命令做文本替换，或者跳过共享执行器抽取，转而在 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts) 内复制一份自动更新并发骨架。
      approach_evaluation: 可维护性 1/5，前者破坏可追溯性，后者复制逻辑制造双份债务；健壮性 1/5，工具链绕过或逻辑复制都会把 bug 面积扩大；可扩展性 1/5，未来自动/手动两条并发链继续分叉。
      edge_cases: shell 替换可能误伤相似文本；复制并发骨架会让 stop 语义、失败恢复、刷新顺序在两条路径上继续漂移。
      affected_scope_delta: 前者表面不变，实则失去精确编辑保障；后者会让 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts) 承担不必要复杂度，并增加后续同步维护成本。
recommendation: 选择 option_a。它在可维护性、健壮性、可扩展性三个维度上都是综合最优：保留原技术方案，只修正当前执行手法，按小范围精确修改继续推进，不扩大回归面，也不制造重复逻辑。
execution_plan_update:
  - 保留原 step_2.1 的目标不变：在 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 抽取共享的组级并发执行骨架。
  - 将其执行方法细化为两个子步骤：
    - step_2.1.a: 先在 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 中插入共享类型与 [`executeGroupedUpdateChunks_ACU()`](src/service/table/update-scheduler.ts:187)。
    - step_2.1.b: 再单独修改 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:215) 去复用新骨架，并保持自动合并/刷新/清理逻辑原顺序不变。
  - 后续 step_2.2 ~ step_2.5 保持原计划：处理 stop 标记复位位置、手动 group 并发、自动调用点同步、向量空数据短路。
deviation_audit:
  original_plan_excerpt:
    - 新增 step_2.1：补读 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 后半段，确认自动更新完整收尾逻辑。
    - step_2.2：修改 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts)，移除 [`processUpdates_ACU()`](src/presentation/triggers/update-process.ts:200) 内的 stop 标记重置。
    - step_2.3：在自动与手动任务入口统一设置 stop 初始值。
    - step_2.4：实现手动 group 并发执行并对齐自动更新 chunk 策略。
    - step_2.5：实现向量空数据短路。
  current_proposal:
    - 在已完成“补读后半段”的前提下，继续原 step_2.1，但把对 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 的修改拆成 step_2.1.a 与 step_2.1.b 两个精确编辑动作；step_2.2 ~ step_2.5 不变。
  diff_summary:
    - 没有替换技术方案。
    - 没有缩小 affected_scope。
    - 只是在原 step_2.1 内新增了更细的执行拆分，以适配标准编辑工具的精确匹配要求。
  deviation_motive_check:
    - **措辞替换规则逐类检查**（基于刚刚刷新过的规则）：全部未命中。当前偏离只是把大块替换改为分段精确编辑，不存在用“简化”“折中”“先这样”之类措辞给降级找借口。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。方案本身不变，只提升执行精度，三维评分不降反升。
    - 偏离是否导致 affected_scope 缩小？→ NO。涉及文件范围保持不变。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_a 在三个维度都优于整文件重写和绕过工具链。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已经明确要求保留自动更新收尾顺序、group 失败信息与 stop 语义。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。这里选择小范围编辑不是为了省事，而是为了避免误改大段稳定逻辑。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。所有原定文件仍在计划内。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。工具匹配失败本身就是当前阻塞点，已正面处理。
  - options 是否包含至少三个方案？ → YES。已提供 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。明确排除了这种低质量做法。
  - deviation_audit 是否触发了 self_dissection？ → NO。没有触发偷懒判定。
  → YES项必须就地修正
</decision_point>
---
<decision_point>
issue: 在继续修改 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:291) 时，我使用了一次包含两个 SEARCH/REPLACE 块的 [`apply_diff()`](src/service/table/update-scheduler.ts:291)。其中前半段已经成功把共享执行骨架插入文件，但后半段对返回段的匹配失败，导致这次修改处于“部分成功”的状态。这个问题没有在上一轮 analysis 中预见到，因为它不是业务或架构不确定性，而是执行阶段对补丁粒度和当前文件基线判断不够精确。
impact: YES + 这会直接阻塞 step_2.1.b：如果现在继续按旧基线补丁修改 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts)，很容易在部分已改、部分未改的状态下再次打歪补丁。影响范围目前仍限定在 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts)，但如果处理失当，会波及后续依赖它的 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts) 手动并发改造。
context_update:
  - 已确认 [`executeGroupedUpdateChunks_ACU()`](src/service/table/update-scheduler.ts:198) 已插入文件，因此 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 的当前基线已经不同于上一轮读取时的内容。
  - 已确认自动更新收尾逻辑（刷新数据、自动合并、清理旧层）仍保留在 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:291) 后半段，不能因为这次补丁失败就粗暴重写整段函数。
  - 已确认原方案本身不需要变更；需要变更的是 step_2.1.b 的执行方式：必须先基于当前最新内容重新定位，再做更小粒度的精确替换。
options:
  - option_a:
      description: 先重新读取 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:291) 的当前完整内容，然后把接线改造拆成 2-3 个更小的精准补丁，分别替换函数开头变量区、并发执行区、返回结果区。
      approach_evaluation: 可维护性 5/5，补丁边界清楚，便于审查；健壮性 5/5，能基于最新文件状态精确修改，避免再次叠加错位补丁；可扩展性 5/5，完成后共享执行器仍能稳定复用于手动更新路径。
      edge_cases: 需要确保小补丁不会误改自动更新原有的刷新、自动合并和清理顺序；还要确保 `shouldStop` 只是透传，不改变自动路径原有的失败统计行为。
      affected_scope_delta: 无新增文件；只是将 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 的接线步骤拆细，后续 affected_scope 仍保持 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts)、[`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts)、[`src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts`](src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts)、[`src/service/plot/vector-recall-orchestrator.ts`](src/service/plot/vector-recall-orchestrator.ts) 不变。
  - option_b:
      description: 不重新读取最新内容，直接基于当前记忆再次尝试一个更大的 [`apply_diff()`](src/service/table/update-scheduler.ts:291)，把整个 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:291) 一把替换掉。
      approach_evaluation: 可维护性 2/5，大块替换会放大 diff，复查成本高；健壮性 2/5，当前文件已部分改动，再凭旧上下文大替换很容易继续失配或误改；可扩展性 3/5，理论上能一次完成，但风险明显高于小步精修。
      edge_cases: 一旦把自动合并或旧数据清理段一起覆盖错，回归范围会从“并发骨架”扩大到整个自动更新收尾链。
      affected_scope_delta: 表面仍只影响 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts)，实质上扩大了该文件内部的回归面。
  - option_c:
      description: 放弃让自动更新复用共享执行器，保留自动路径原状，只在 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts) 内单独实现一套手动分组并发。
      approach_evaluation: 可维护性 1/5，这会把自动/手动再次分叉成两套并发语义；健壮性 2/5，短期似乎能继续推进，但 stop 语义、失败恢复、并发策略很快又会漂移；可扩展性 1/5，后续每次改并发策略都要双改，纯属主动制造维护债务。
      edge_cases: 用户第二条要求就是“参考自动更新并优化”；如果继续保留双轨逻辑，未来一边修复另一边遗漏的情况还会反复出现。
      affected_scope_delta: 表面缩小了 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 的修改，实质把复杂度硬塞回 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts)，属于范围转移，不是问题解决。
recommendation: 选择 option_a。它在可维护性、健壮性、可扩展性三个维度上都是综合最优：先尊重当前已变化的文件基线，再做小粒度精确补丁，不扩大回归面，也不放弃共享执行器的正确方向。
execution_plan_update:
  - 保留原 step_2.1 的目标不变：让 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:291) 复用 [`executeGroupedUpdateChunks_ACU()`](src/service/table/update-scheduler.ts:198)。
  - 将 step_2.1.b 明确细化为：
    - step_2.1.b.1：重新读取 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:291) 的当前内容，确认部分成功后的最新基线。
    - step_2.1.b.2：只替换函数开头的 `tablesToUpdate/groupKeys/maxConcurrentGroups` 初始化与内联 chunk 循环。
    - step_2.1.b.3：只替换函数尾部返回值，改为基于共享执行结果返回。
  - 原 step_2.2 ~ step_2.5 保持不变。
deviation_audit:
  original_plan_excerpt:
    - 保留原 step_2.1 的目标不变：在 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 抽取共享的组级并发执行骨架。
    - 将其执行方法细化为两个子步骤：
      - step_2.1.a: 先在 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 中插入共享类型与 [`executeGroupedUpdateChunks_ACU()`](src/service/table/update-scheduler.ts:187)。
      - step_2.1.b: 再单独修改 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:215) 去复用新骨架，并保持自动合并/刷新/清理逻辑原顺序不变。
    - 后续 step_2.2 ~ step_2.5 保持原计划：处理 stop 标记复位位置、手动 group 并发、自动调用点同步、向量空数据短路。
  current_proposal:
    - step_2.1.a 已完成；step_2.1.b 不改变目标，只增加“先重新读取当前函数，再拆成更小补丁”的执行约束，随后继续原计划中的 step_2.2 ~ step_2.5。
  diff_summary:
    - 没有替换技术方案。
    - 没有缩小 affected_scope。
    - 只是在 step_2.1.b 内新增了“刷新当前基线 + 分三段精确替换”的执行细化，防止在部分成功状态下继续错补丁。
  deviation_motive_check:
    - **措辞替换规则逐类检查**（基于刚刷新过的规则）：全部未命中。当前偏离不是降级方案，而是修正补丁执行方式。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。方案本身不变，执行精度更高，三维评分不下降。
    - 偏离是否导致 affected_scope 缩小？→ NO。涉及文件范围与主线任务保持一致。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_a 明显优于继续盲打大补丁，也优于放弃共享执行器。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已覆盖“部分成功后的新基线”“自动收尾顺序保留”“失败统计不变”三个关键边界。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。这里选择更小补丁是为了提高精确性，不是为了偷省工作量。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。原定文件和步骤都保留。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。部分成功状态本身就是当前最高优先级问题，已正面处理。
  - options 是否包含至少三个方案？ → YES。已提供 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。不会用这种低质量做法绕开标准编辑链。
  - deviation_audit 是否触发了 self_dissection？ → NO。没有触发偷懒判定。
  → YES项必须就地修正
</decision_point>
---
<decision_point>
issue: 在把 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:291) 的前半段切换到 [`executeGroupedUpdateChunks_ACU()`](src/service/table/update-scheduler.ts:209) 后，TypeScript 立即报出三个错误：返回段仍在引用已经不存在的 `failedGroupKeys` 与 `totalGroups`。这个问题之所以在上一轮没有完全消除，是因为我按“小补丁分段修改”推进时，先替换了执行区，但尚未同步收尾返回段，导致函数处于短暂的不完整状态。
impact: NO + 这不影响原方案的可行性，也不要求改变架构；它只是 step_2.1.b.3 尚未执行完成造成的中间态编译错误。影响范围严格限定在 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 的返回段。
context_update:
  - 当前 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:291) 的前半段已经使用 `const execution = await executeGroupedUpdateChunks_ACU(...)`。
  - 当前函数尾部仍保留旧字段：
    - `success: failedGroupKeys.length === 0`
    - `failedGroups: failedGroupKeys.length`
    - `totalGroups`
  - 因此本轮真正需要做的不是新增设计，而是完成原计划中遗漏的“尾部返回值切换到 `execution.*`”这一步。
options:
  - option_a:
      description: 直接修正 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:374) 的返回段，把旧的 `failedGroupKeys` / `totalGroups` 全部切换为 `execution.failedGroupKeys` / `execution.totalGroups`。
      approach_evaluation: 可维护性 5/5，和前半段已改逻辑完全一致；健壮性 5/5，能立刻消除当前编译错误且不引入额外分支；可扩展性 5/5，后续自动/手动都围绕统一执行结果结构工作。
      edge_cases: 需要确保只改返回段，不误伤自动合并与旧数据清理逻辑；还要确认 `execution.totalGroups` 在空 group 情况下已由前置 return 处理。
      affected_scope_delta: 无新增文件，仅继续修改 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts)。
  - option_b:
      description: 回退前半段对 [`executeGroupedUpdateChunks_ACU()`](src/service/table/update-scheduler.ts:209) 的接入，重新恢复旧的 `failedGroupKeys/totalGroups` 变量和内联 chunk 循环。
      approach_evaluation: 可维护性 1/5，等于撤销共享执行器接线，继续保留重复逻辑；健壮性 2/5，虽然能暂时消除当前报错，但又回到自动/手动分叉的问题；可扩展性 1/5，后续仍然要再做一次同样的改造。
      edge_cases: 撤回后手动并发仍无法复用自动执行骨架，用户第二条需求继续卡住。
      affected_scope_delta: 表面缩小当前修改，但实质把复杂度推回后续步骤，属于回退，不是修复。
  - option_c:
      description: 在函数前半段重新声明一个伪造的 `failedGroupKeys` 与 `totalGroups` 变量，兼容旧返回段，先让编译通过。
      approach_evaluation: 可维护性 1/5，制造重复状态源；健壮性 1/5，返回结果可能与 `execution` 实际值不一致；可扩展性 1/5，后续所有调用方都要承担双状态维护风险。
      edge_cases: 一旦两个状态源不同步，失败组数量和总组数会出现错报，日志和 UI 都会被污染。
      affected_scope_delta: 仍只影响 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts)，但会在函数内部引入无意义冗余状态。
recommendation: 选择 option_a。它在可维护性、健壮性、可扩展性三个维度都是综合最优：只是把原计划中尚未完成的尾部返回段补齐，与新的执行结果结构保持一致，没有理由再兜圈子。
execution_plan_update:
  - 原 execution_plan 不变。
  - 当前仅补完 step_2.1.b.3：修改 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 的返回段，统一改为使用 `execution.failedGroupKeys` 与 `execution.totalGroups`。
  - 该修复完成后，继续原计划中的 step_2.2：处理 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts) 的 stop 标记复位位置；随后再做手动并发与向量短路。
deviation_audit:
  original_plan_excerpt:
    - 保留原 step_2.1 的目标不变：让 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:291) 复用 [`executeGroupedUpdateChunks_ACU()`](src/service/table/update-scheduler.ts:198)。
    - 将 step_2.1.b 明确细化为：
      - step_2.1.b.1：重新读取 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:291) 的当前内容，确认部分成功后的最新基线。
      - step_2.1.b.2：只替换函数开头的 `tablesToUpdate/groupKeys/maxConcurrentGroups` 初始化与内联 chunk 循环。
      - step_2.1.b.3：只替换函数尾部返回值，改为基于共享执行结果返回。
    - 原 step_2.2 ~ step_2.5 保持不变。
  current_proposal:
    - 继续执行尚未完成的 step_2.1.b.3，仅修复 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:374) 的尾部返回段，不变更任何其他步骤。
  diff_summary:
    - 没有新增方案变更。
    - 没有缩小 affected_scope。
    - 只是把之前已经计划但尚未完成的尾部返回改造补完。
  deviation_motive_check:
    - **措辞替换规则逐类检查**（基于刚刷新过的规则）：全部未命中。当前只是补齐未完成的小步骤，不存在任何借口式降级措辞。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。实际上没有偏离方案，只是在完成原步骤。
    - 偏离是否导致 affected_scope 缩小？→ NO。范围未变。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_a 直接修复中间态错误，明显优于回退或伪造兼容变量。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已明确限定只改返回段，并保留现有收尾逻辑。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。这里的最小修改恰好就是正确修改，因为问题本身只在尾部返回段。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。主线计划没有变化。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。当前编译错误就是必须立即处理的问题。
  - options 是否包含至少三个方案？ → YES。已提供 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。仍然使用标准编辑工具处理。
  - deviation_audit 是否触发了 self_dissection？ → NO。没有触发偷懒判定。
  → YES项必须就地修正
</decision_point>
---
<decision_point>
issue: 新发现了一个会直接破坏 stop 语义一致性的结构问题：[`src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts`](src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts:7) 仍从 [`src/presentation/components/plot-editors.ts`](src/presentation/components/plot-editors.ts:475) 引用 `isAutoUpdatingCard_ACU` / `_set_isAutoUpdatingCard_ACU` 这一套旧状态，而表格更新编排、停止按钮和请求中止链路实际使用的是 [`src/service/runtime/state-manager.ts`](src/service/runtime/state-manager.ts:231) 里的同名状态。也就是说，当前项目里并行存在两套“是否正在自动更新 / 是否被用户终止”的状态源。这个问题在前面的侦察中没有显式暴露，因为我当时主要聚焦在 [`processUpdatesBatch_ACU()`](src/service/table/update-orchestrator.ts:449)、[`orchestrateManualUpdate_ACU()`](src/service/table/update-orchestrator.ts:587) 和 [`processUpdates_ACU()`](src/presentation/triggers/update-process.ts:200) 的调用链，没有逐一核对所有上层入口导入的状态来源模块。
impact: YES + 这会直接影响 step_2.2 到 step_2.4 的正确性：即使把 [`processUpdates_ACU()`](src/presentation/triggers/update-process.ts:200) 的 stop reset 去掉、把手动 group 改成并发，如果自动入口 [`triggerAutomaticUpdateIfNeeded_ACU()`](src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts:31) 还在读写另一套状态，任务级终止和“正在更新”状态就仍然会漂移。影响范围明确落在 [`src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts`](src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts:31) 的状态导入与自动更新入口接线。
context_update:
  - 已确认 [`src/presentation/components/plot-editors.ts`](src/presentation/components/plot-editors.ts:475) 与 [`src/service/runtime/state-manager.ts`](src/service/runtime/state-manager.ts:231) 各自都声明了一份 `isAutoUpdatingCard_ACU` / `wasStoppedByUser_ACU`。
  - 已确认 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts:4) 与 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts:7) 使用的是 [`src/service/runtime/state-manager.ts`](src/service/runtime/state-manager.ts:231) 那套状态。
  - 已确认 [`src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts`](src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts:7) 仍把自动更新入口绑在 [`src/presentation/components/plot-editors.ts`](src/presentation/components/plot-editors.ts:475) 这套旧状态上。
  - 因此当前真正需要修正的不是业务流程，而是“状态单一真源”的接线位置。
options:
  - option_a:
      description: 将 [`src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts`](src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts:7) 中与表格更新任务相关的状态引用统一切换到 [`src/service/runtime/state-manager.ts`](src/service/runtime/state-manager.ts:231)，只保留真正属于 UI/plot 编辑器的函数从 [`src/presentation/components/plot-editors.ts`](src/presentation/components/plot-editors.ts:475) 导入；同时在自动更新入口设置 [`_set_wasStoppedByUser_ACU()`](src/service/runtime/state-manager.ts:256) 初始值，并把 [`shouldStop`](src/service/table/update-scheduler.ts:282) 透传给 [`executeAutoUpdatePlan_ACU()`](src/service/table/update-scheduler.ts:291)。
      approach_evaluation: 可维护性 5/5，恢复单一状态源，后续停止语义不再跨模块漂移；健壮性 5/5，自动与手动入口都基于同一套 stop/autoUpdating 状态；可扩展性 5/5，未来若再新增入口，只需遵循 runtime state 即可，不必同步两套镜像状态。
      edge_cases: 需要确保 [`src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts`](src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts:7) 中其余来自 [`src/presentation/components/plot-editors.ts`](src/presentation/components/plot-editors.ts:475) 的 UI 辅助函数不受影响；还要确保自动更新开始前重置 stop 标记，不会误伤手动终止刚发生的其他流程。
      affected_scope_delta: 不新增新文件，但明确要求修改 [`src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts`](src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts:31) 的导入和执行委托接线；原 affected_scope 不变。
  - option_b:
      description: 不调整自动入口导入，转而在 [`src/presentation/components/plot-editors.ts`](src/presentation/components/plot-editors.ts:475) 与 [`src/service/runtime/state-manager.ts`](src/service/runtime/state-manager.ts:231) 之间增加手工同步逻辑，让两套状态在更新开始/结束/终止时彼此赋值。
      approach_evaluation: 可维护性 1/5，继续保留双状态源，只是靠额外同步代码硬绑；健壮性 2/5，任何一个入口漏同步就会再次漂移；可扩展性 1/5，后续每加一个入口都要记得双写状态。
      edge_cases: 并发场景下两套状态写入顺序不同步，会出现 UI 认为已停止但编排层继续派发任务，或者反过来。
      affected_scope_delta: 需要新增对 [`src/presentation/components/plot-editors.ts`](src/presentation/components/plot-editors.ts:475) 的状态同步改造，把问题扩散到更多文件。
  - option_c:
      description: 暂时忽略这个双状态源问题，继续只改 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts:200) 和 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts:587) 的并发与终止逻辑。
      approach_evaluation: 可维护性 1/5，等于容忍结构性裂缝继续存在；健壮性 1/5，stop 语义在自动入口依旧可能读错状态；可扩展性 1/5，后续每次定位“为什么自动更新状态不一致”都得重复踩坑。
      edge_cases: 自动更新入口的前置检查仍可能基于错误的 `isAutoUpdatingCard_ACU` 判断重复触发任务；stop 标记也可能在一个模块已置位、另一个模块仍为 false 的情况下失效。
      affected_scope_delta: 表面不扩 scope，实质是把已发现的结构问题留在系统里继续发酵。
recommendation: 选择 option_a。它在可维护性、健壮性、可扩展性三个维度上都是综合最优：不靠双写同步糊住裂缝，而是把自动入口接回与编排层一致的单一状态源。这才像样，不是把 bug 从一个模块推到另一个模块。
execution_plan_update:
  - 原 execution_plan 目标不变。
  - 在原 step_2.2 前插入一个前置修正动作：修改 [`src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts`](src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts:31)，统一自动更新入口的状态源到 [`src/service/runtime/state-manager.ts`](src/service/runtime/state-manager.ts:231)，并透传 [`shouldStop`](src/service/table/update-scheduler.ts:282)。
  - 随后继续原计划：
    - 修改 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts:200)，移除批处理层的 stop reset。
    - 修改 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts:587)，让手动 group 改为复用 [`executeGroupedUpdateChunks_ACU()`](src/service/table/update-scheduler.ts:205) 并发执行，并重新梳理任务级状态归属。
    - 最后处理 [`src/service/plot/vector-recall-orchestrator.ts`](src/service/plot/vector-recall-orchestrator.ts:144) 的空向量数据短路。
deviation_audit:
  original_plan_excerpt:
    - step_2: 修改 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts)，让 [`proceedWithCardUpdate_ACU()`](src/presentation/triggers/update-process.ts:118) 和 [`handleProgressEvent()`](src/presentation/triggers/update-process.ts:92) 同时更新 toast 与状态文本，展示“当前批次/总批次 + 当前尝试/最大重试”，并移除 3 秒后自动清除终止标记的脆弱做法，改为由任务编排层统一复位。
    - step_3: 修改 [`src/service/table/update-scheduler.ts`](src/service/table/update-scheduler.ts) 与 [`src/service/table/update-orchestrator.ts`](src/service/table/update-orchestrator.ts)，抽取共享的 group 并发执行器或等价复用点，让手动更新按 [`settings.maxConcurrentGroups`](src/service/table/update-scheduler.ts:226) 分块并发执行，同时保留现有失败恢复与刷新时机。
  current_proposal:
    - 在继续原 step_2 与 step_3 之前，先修正 [`src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts`](src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts:31) 的状态源与 `shouldStop` 接线；然后按原计划继续 stop reset、手动并发和向量短路改造。
  diff_summary:
    - 新增了一个“自动入口状态源统一”的前置步骤。
    - 没有删减原 step_2 / step_3 的目标。
    - 没有缩小 affected_scope，只是把之前侦察时未显式暴露的结构性前提补上。
  deviation_motive_check:
    - **措辞替换规则逐类检查**（基于刚才已刷新的规则）：全部未命中。当前偏离是修正新发现的结构性问题，不存在“先这样”“折中”“简化”之类降级措辞。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。补上状态源统一后，健壮性和可维护性反而更高。
    - 偏离是否导致 affected_scope 缩小？→ NO。范围未缩小，且仍在原有 affected_scope 内。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_a 消除双状态源，明显优于双写同步或故意忽略问题。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已覆盖自动入口前置检查、任务开始时 stop 复位、`shouldStop` 透传与 UI helper 保留边界。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。这里选择的是结构上正确的方案，不是表面最省事的方案。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。原定文件仍全部在计划内。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。双状态源会直接破坏 stop 语义，绝对不是可以忽略的小问题。
  - options 是否包含至少三个方案？ → YES。已提供 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。仍然使用标准编辑工具推进。
  - deviation_audit 是否触发了 self_dissection？ → NO。没有触发偷懒判定。
  → YES项必须就地修正
</decision_point>
---
<decision_point>
issue: 在进入验收前，我确认到一个新的运行期风险，而且它不是小问题：[`proceedWithCardUpdate_ACU()`](src/presentation/triggers/update-process.ts:118) 在每个 toast 中都使用固定的 `acu-stop-update-btn` 作为停止按钮 id，而 [`bindTableFillStopButton_ACU()`](src/presentation/components/status-display.ts:31) 又通过全局选择器 [`#acu-stop-update-btn`](src/presentation/components/status-display.ts:32) 绑定事件。同时，[`updateLoadingToastMessage()`](src/presentation/triggers/update-process.ts:82) 会在每次进度更新时直接重写整个 `.toast-message` 的 HTML。三件事叠在一起的结果是：手动分组并发后会出现重复 id；后一个 toast 的绑定可能覆盖前一个；而一旦进度文本刷新，原按钮节点被替换，点击事件也可能直接丢失。这个问题在前面的侦察里没有暴露，是因为当时重点在编排层并发与 stop 状态语义，直到验收前回查 UI 绑定实现时，才把“并发 toast + 全局固定 id + 重写 HTML”这组组合风险完整拼出来。
impact: YES + 这会直接影响用户第一条需求的真实可用性：即使任务级终止语义在 service 层已经正确，UI 上的“终止”按钮也可能在并发手动更新时绑定错对象、丢失绑定，或者行为不稳定。影响范围落在 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts) 与 [`src/presentation/components/status-display.ts`](src/presentation/components/status-display.ts)。
context_update:
  - 已确认 [`renderStopButton_ACU('acu-stop-update-btn', '终止')`](src/presentation/triggers/update-process.ts:138) 会为每个并发 toast 渲染相同 id。
  - 已确认 [`bindTableFillStopButton_ACU()`](src/presentation/components/status-display.ts:31) 当前只支持全局固定选择器，不支持“只绑定当前 toast 内的按钮”。
  - 已确认 [`updateLoadingToastMessage()`](src/presentation/triggers/update-process.ts:82) 每次更新都重写按钮 HTML，这会让初始 `onShown` 阶段绑定的事件失效。
  - 因此这里不能只看 service 层 stop 语义是否正确，还必须把 toast 按钮绑定方式修到能承受并发和动态刷新。
options:
  - option_a:
      description: 在 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts) 中为每个 toast 生成唯一按钮 id，并把 toast 内容改成“稳定的消息节点 + 独立停止按钮”结构，使 [`updateLoadingToastMessage()`](src/presentation/triggers/update-process.ts:82) 只更新消息文本而不重建按钮；同时修改 [`bindTableFillStopButton_ACU()`](src/presentation/components/status-display.ts:31)，让它按传入的按钮 id 或作用域节点绑定，而不是硬编码全局 `#acu-stop-update-btn`。
      approach_evaluation: 可维护性 5/5，绑定范围明确，toast 更新与按钮绑定解耦；健壮性 5/5，能同时解决重复 id、全局重绑和按钮节点被替换三类问题；可扩展性 5/5，未来即使再增加并发 toast 或别的停止按钮，也能复用同一套作用域绑定方式。
      edge_cases: 需要确保单次更新与并发更新都能使用同一套绑定逻辑；还要保证按钮文本更新不影响当前 toast 的关闭与全局 abort 行为。
      affected_scope_delta: 仅新增对 [`src/presentation/components/status-display.ts`](src/presentation/components/status-display.ts) 与 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts) 的收尾修正，仍在原 affected_scope 范围内。
  - option_b:
      description: 保持固定按钮 id 不变，只在 [`updateLoadingToastMessage()`](src/presentation/triggers/update-process.ts:82) 每次重写 HTML 后再次调用 [`bindTableFillStopButton_ACU()`](src/presentation/components/status-display.ts:31) 重新绑定事件。
      approach_evaluation: 可维护性 2/5，重复 id 与全局绑定问题仍在，只是用重复绑定掩盖；健壮性 2/5，多个并发 toast 仍会互相抢绑定；可扩展性 2/5，后续任何新的并发场景都还会撞上同样的问题。
      edge_cases: 最后一次绑定仍可能覆盖前面 toast 的 onStop 闭包，导致点击任意按钮都触发“最后一个 toast”的局部逻辑。
      affected_scope_delta: 只改 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts)，但问题实质没有被完整解决。
  - option_c:
      description: 不处理这个 UI 问题，认为反正 [`abortAllActiveRequests_ACU()`](src/service/runtime/state-manager.ts:245) 会中止全部请求，按钮偶发绑错也“影响不大”。
      approach_evaluation: 可维护性 1/5，这是在拿偶然正确替代稳定设计；健壮性 1/5，按钮失效就是失效，不能靠 service 层兜底假装没问题；可扩展性 1/5，未来 UI 并发只会更不稳定。
      edge_cases: 一旦按钮在动态更新后失去绑定，用户将看见“有终止按钮但点了没反应”的伪控制界面，这比没有按钮还糟。
      affected_scope_delta: 表面不新增改动，实质是把已发现的缺陷留在交付物里。
recommendation: 选择 option_a。它在可维护性、健壮性、可扩展性三个维度都是综合最优：把按钮绑定从“全局固定 id + 重建 HTML”的脆弱模式改成“唯一 id + 作用域绑定 + 稳定按钮节点”的模式，才配得上并发手动更新。
execution_plan_update:
  - 原 execution_plan 主线不变，只在进入验收前增加一个必要修正步骤。
  - 新增 step_5.precheck_fix:
    - 修改 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts)，为每个 toast 生成唯一停止按钮 id，并让 [`updateLoadingToastMessage()`](src/presentation/triggers/update-process.ts:82) 只更新消息文本而不是重建按钮节点。
    - 修改 [`src/presentation/components/status-display.ts`](src/presentation/components/status-display.ts:31)，让 [`bindTableFillStopButton_ACU()`](src/presentation/components/status-display.ts:31) 接收按钮 id/作用域参数，避免全局重复 id 绑定冲突。
  - 修复后重新跑类型检查，再进入验收与归档。
deviation_audit:
  original_plan_excerpt:
    - step_2: 修改 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts)，让 [`proceedWithCardUpdate_ACU()`](src/presentation/triggers/update-process.ts:93) 和 [`handleProgressEvent()`](src/presentation/triggers/update-process.ts:54) 同时更新 toast 与状态文本，展示“当前批次/总批次 + 当前尝试/最大重试”，并移除 3 秒后自动清除终止标记的脆弱做法，改为由任务编排层统一复位。
    - step_5: 运行针对性的构建或类型检查，确认新增上下文字段、共享执行器和向量短路逻辑没有破坏现有调用方。
  current_proposal:
    - 在正式验收前，先补一个 UI 稳定性修正步骤：把 [`src/presentation/triggers/update-process.ts`](src/presentation/triggers/update-process.ts) 的固定停止按钮 id 与整块 HTML 重建逻辑改掉，并同步修改 [`src/presentation/components/status-display.ts`](src/presentation/components/status-display.ts) 的绑定方式；然后重新执行原 step_5 的类型检查与验收。
  diff_summary:
    - 新增了一个验收前的 UI 稳定性修正步骤。
    - 没有删减原 step_2 对批次进度与 stop 语义的目标，只是补上并发 toast 下的真正可用性缺口。
    - 没有缩小 affected_scope；只是把之前未显式暴露的并发 UI 风险补进来。
  deviation_motive_check:
    - **措辞替换规则逐类检查**（基于已刷新规则）：全部未命中。当前偏离是为了修复新发现的运行期缺陷，不是为了偷懒或降级方案。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。补上这一步后，健壮性明显提高，可维护性也更好。
    - 偏离是否导致 affected_scope 缩小？→ NO。受影响文件仍在原主线范围附近，只是补充必要修正。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_a 同时消除重复 id、全局重绑、按钮节点被替换三类问题。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已覆盖单 toast、并发 toast、动态进度刷新、全局 abort 兜底这几类关键边界。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。这里拒绝了 option_b/c 这种表面更省事但质量明显更差的路径。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。原有主线文件全部保留，额外修正仅落在相关 UI 文件。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。终止按钮在并发任务里失效绝不是可以忽略的问题。
  - options 是否包含至少三个方案？ → YES。已提供 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。仍然使用标准编辑工具修改。
  - deviation_audit 是否触发了 self_dissection？ → NO。没有触发偷懒判定。
  → YES项必须就地修正
</decision_point>
---
<output_quality_review>
task_summary: 完成三项优化：补齐多批次填表的批次/重试进度展示并将终止提升为任务级；让手动更新与自动更新共享同一组级并发调度语义；让向量模式在无远记忆数据时跳过关键词生成与注入链路。
deliverables: D1-D6，见上方交付清单映射。

# 量化指标总览
metrics:
  total_files_modified: 6 — 修改了 6 个生产文件
  execution_plan_coverage: 5/5 = 100% — 原 analysis 的 5 个主步骤全部完成；执行期新增修正步骤也已收敛
  edge_cases_handled: 9/9 = 100% — analysis 中列出的 9 个边界条件均已在实现中覆盖
  confidence_assessment: MEDIUM — 代码链路、状态流和无输出类型检查已验证，但仍缺少浏览器级/真实业务流手工冒烟，因此置信度不应虚报为 HIGH
    - 未验证假设 1：并发 toast 在真实界面中的显示节奏与点击终止反馈符合当前 DOM 更新逻辑
    - 未验证假设 2：手动分组并发在 SQL 模式与原生模式下都能按统一调度骨架表现一致

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO + D1 改了批次上下文与任务级终止的编排；D2 改了 toast/状态文本与停止交互；D3 抽了组级并发执行骨架并让自动路径复用；D4 统一了自动入口状态源；D5 增加了无向量数据短路；D6 修了并发 toast 停止按钮的唯一 id 与作用域绑定。删掉任一项，至少一条用户需求会直接失效或退化。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → YES + 这些产物直接控制目标行为：如果重新改坏批次上下文透传、任务级 stop 传播、组级并发调度或无数据短路，用户界面的进度展示、终止行为、手动并发或向量发送前流程会立刻退化，说明产物不是摆设。
  - 实质性比率: 6/6 = 100%

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO + analysis 中识别到的核心路径都已覆盖，没有把 UI 展示、service 编排、自动入口状态源、向量发送前短路中的任何一类故意留空。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES + 原 analysis 的 D1-D5 已全部覆盖；执行期通过 decision_point 合法扩 scope 纳入 D6，最终交付与更新后的 affected_scope 一致。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES + E1-E4 对应了三条用户需求和一个执行期补洞点的直接代码证据；另外已完成两轮无输出类型检查，确认调用链与类型面没有被改坏。
  - affected_scope 覆盖率: 6/6 = 100%

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证 trivial 行为）的比例是多少？
    → 高价值:低价值 = 6:0，高价值占比 100%。本次改动全部落在编排、状态传播、并发控制、无数据短路与 UI 绑定稳定性上，没有用 trivial 改动凑数。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO + 每个产物都对应明确根因，数量不多但密度高。当前剩余风险在运行期冒烟深度，而不在交付物掺水。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足的是本质需求。用户表面上要的是“显示批次与重试”“手动更新像自动更新一样并发”“无向量数据就别生成关键词”。本质上要的是任务可观察、可终止、手动/自动调度语义一致，以及无数据时不做无意义工作。D1-D6 都围绕这些根因完成。
  - "如果这是别人交给我的，我会接受吗？"
    → YES + 这版代码已经达到可提交标准：根因已修、调用链已对齐、无输出类型检查已通过。它还不是最漂亮的验收闭环，因为缺少 UI 级实跑记录，所以我给的是 MEDIUM 置信度，而不是自欺欺人的 HIGH。
→ 任何检查项发现问题，必须就地修正，修正完成后重新检查该项。
</output_quality_review>
